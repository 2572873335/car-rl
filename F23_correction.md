# F23 更正 — 被利用的状态是"封堵者已命令内圈但尚未到位"

日期：2026-09-21
性质：对 `F23_transient_window.md` 的**修正**（机制表述纠正，非全盘推翻）。
触发：作者在送审 M1 plan 期间**自查**"是否只利用了开局瞬态"，结果推翻了原表述。
关联：`plan_phaseC_m1.md` §2.1、`reviews/` 待出的 M1 评审

---

## 1. 原表述（`F23_transient_window.md`）**不准确**

原文称：
> 承诺窗口 = 封堵者**物理横移**的 0.61–0.76 s；命中率 44.6% vs 基线 13.4%，lift 3.32×

**两处问题**：

### 问题一：3.32× 的 lift 是**开局瞬态**造成的伪影

自查实验（`_opening_check.py`）——把**开局瞬态消掉**
（让封堵者先"settle"1.5 s 再开始）：

| 条件 | 完成率 | 命中率 | 基线率 |
|---|---|---|---|
| 正常开局 | 0.77 | **0.45** | 0.13 |
| **消掉开局瞬态** | **0.87** | **0.00** | 0.09 |

**⇒ 完成率反而上升（0.77→0.87），而命中率归零。**
**说明反制脚本的获胜不依赖"横移命中"**，原先的 lift 来自开局那一次换道。

### 问题二：正确口径下的**真正被利用状态**是另一个信号

按**状态分布**核对（`_corrected_mech.py`，3 seed 集，正常开局）：

```
      封堵者状态              切入占比   占据占比   lift
  (lane0, target0, settled)    55.4%    73.7%    0.75   ← 非目标，只是常见
  (lane0, target1, moving)     44.6%     6.5%   6.88   ← ★ 真正被瞄准
  其余 5 种状态                 0.0%     —        0.00
```

**⇒ 真正被瞄准的状态是 `(lane=0, target=1, moving)`**：
**封堵者"已命令切内圈、但车身还在外圈"**——
即**内圈的【再入】瞬态**，而不是任意横移。

**原口径的错**：我用 `switch[1] > 0`（任意横移）判定"命中"，
把**出圈**的横移也算进去了，于是"命中率"里混入了非目标的动作。

---

## 2. 修正后的 F23

> **封堵者的可被利用状态**：
> **`lane_target = 1` 且 `lane = 0`（已命令内圈、尚未到达）**——
> 此时封堵者**无法回头**（冷却）**且尚未到位**，是唯一的真空窗。
> **lift = 6.88**（占 6.5% 的时间，却承接 44.6% 的切入）。

**这是更强的陈述**：它不只是"横移要时间"，而是
**"冷却 + 到位延迟"叠加出的不可回头期**——
封堵者一旦命令切内圈，就必须走完（冷却锁定），
而走完的这 0.6–0.8 s 内它的拒止是**悬空的**。

---

## 3. 对 M1 的影响（**须改**）

| 项 | 原（`plan_phaseC_m1.md` §2.1） | 修正 |
|---|---|---|
| M1.3 判定口径 | hit = 切入时 `switch[1] > 0`（任意横移） | **hit = 切入时 `lane_target=1 ∧ lane=0`**（内圈再入瞬态） |
| 锚定 | ≥0.20（= 基线 13.4% × 1.5） | **须按新口径重测基线**；新口径的命中率应显著更高（44.6% 已是新口径下的值） |
| F23 表述 | "横移窗口" | **"内圈再入的不可回头窗"** |

> **注**：`_corrected_mech.py` 的 44.6% 恰好**已是新口径**下的占比
> （`(0,1,moving)` 的 dive% = 44.6%），与旧口径的 44.6% 数值巧合相同——
> **但含义完全不同**（旧的是"任意横移"，新的是"内圈再入"）。
> **基线须按新口径重测**（占据占比 6.5% → lift 6.88）。

---

## 4. 自查的价值（流程记录）

**这是在送审M1的同一时间做的自查，且结果推翻了送审稿的一节。**

- 触发点：评审 prompt 里我自己写的怀疑——"反制脚本在 t=0.02 就切入，
  是否只是在利用开局瞬态？"
- 若不自查：M1.3 会带着**错误口径**开训，训练出的策略即便用了正确机制
  也可能被判"未命中窗口"（或反之），**整轮 M1 的机制诊断失效**。
- **教训（并入 F21 家族）**：**送审不是终点**——
  作者在评审期间继续自查，是对送审稿的负责任做法，
  但**送审稿必须与自查结果同步**，否则评审在核一个已过时的版本
  （这正是 R3 双副本问题的精神同类）。

---

## 5. 复现

```bash
cd /home/zy/car_rl/code0919
PYTHONPATH=$PWD uv run python results/20260920_phaseC_probe/scripts/_opening_check.py
PYTHONPATH=$PWD uv run python results/20260920_phaseC_probe/scripts/_corrected_mech.py
PYTHONPATH=$PWD uv run python results/20260920_phaseC_probe/scripts/_dive_states.py
```
