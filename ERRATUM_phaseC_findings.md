# ERRATUM — Phase C 探针/plan 中的失效结论（2026-09-21）

> **性质**：对本仓库已提交内容中**失效结论**的主动更正。
> 触发：W6 plan 的独立评审（`reviews/20260921_phaseC_w6_review1.md`）指出
> 适配器 bug；**作者已独立复现确认**（脚本见本目录 `scripts/`，
> 复现命令见下）。
>
> **严重度**：其中一条已随 commit `a0dae34` **推送到公开仓库**，
> 且被用作 W6 plan 的核心依据。**必须更正。**

---

## 1. 撤回的结论（**有误，勿再引用**）

### 1.1 ❌「冻结规则机不是博弈对手」

**已提交的原话**（`findings_phaseC_selftest.md` F5 后续、
`research/ASSUMPTIONS.md` P1–P3 段、`AGENT_HANDOFF.md` §4 警告、`research/roadmap.md` W5 注）：

> 「规则 vs 规则 40/40 局**零次领先易手、零次使用内圈**——它根本不是博弈对手」

**错因**：我在 `_verify_f5_fix.py::selfplay_to_frozen` 里**把 gap 硬编码为 1.0 m**：

```python
# 有 bug 的版本（已归档）
def selfplay_to_frozen(o):
    delta_n, v_self, v_other, e_lat_n, lane, _ = o
    return np.array([(1.0 - 0.2) / 0.5, delta_n, v_self, e_lat_n, lane, v_other],
                    np.float32)
```

而 `baseline_action_ot` 的**切入分支要求 `gap < 0.45`**、刹车分支要求 `gap < 0.15`。
**gap 被钉在 1.0 ⇒ 两个分支永不触发 ⇒ 规则机永不换道**。
这是**工具 bug**，不是规则机的性质。

**复现（实测，本仓库脚本）**：

```
FAKE gap=1.0（原提交所用）      : inner-lane  0/40    lead changes  0
CORRECT gap=(-delta)%L          : inner-lane 31/40    lead changes 18
```

**更正后的结论**：规则机在**正确观测**下**会**使用内圈（31/40）、**会**发生领先易手（18 次）。
「规则机不是对手」**不成立**（至少不能由该实验支持）。

### 1.2 ❌ F11 表中的「用内圈 0/40、领先易手 0 次」两列

**已提交的原表**（`findings_phaseC_selftest.md` §F11 / `plan_phaseC_w6.md` §1.2）：

| car 1 策略 | 碰撞 | car1 用内圈 | 领先易手 | 终局领先 |
|---|---|---|---|---|
| 冻结 checkpoint | 22/40 | ~~0/40~~ | ~~0~~ | 18/40 |

**错因**：`_ckpt_as_opponent.py::frozen_layout` 写了
`gap_ref = (L/2 - delta) % L`，而冻结 env 的定义是 `gap_ref = (s_other - s_self) % L`
⇒ **前向 gap 被算成 `(-delta) % L` 才对**，原式**偏差 L/2 = 2.742 m**
（把"紧跟 0.3 m"读成"前方 3.04 m 没人"）。

**复现（实测）**：

```
对手在前 0.30 m:  CORRECT gap = 0.3000 m   原式 = 3.0425 m   偏差 2.7425 m
car1 = 冻结 checkpoint (正确适配器): coll=22/40 inner=22/40 lead_chg=22 ahead=18/40
```

**更正后的结论**：
- 「**22/40 碰撞**」「**终局领先 18/40 = 中立基线**」两列**仍然成立**；
- 「用内圈 0/40」「领先易手 0」两列**是适配器伪影，撤回**。

### 1.3 ⚠️ 由 1.1/1.2 连锁失效的下游结论

| 位置 | 受影响内容 | 处置 |
|---|---|---|
| `plan_phaseC_w6.md` §2 **R1** | 依据 =「规则机不是对手」 | **依据被抽掉**，须以正确适配器重测后再定 |
| `findings_phaseC_selftest.md` **F5 后续** | 「P2 判据不可修复」的论证 | 须重估（P2 仍**可能**饱和，理由需重建） |
| `research/ASSUMPTIONS.md` P1–P3 | 「P2 不能用对规则对手胜率」 | 理由须改为「**判据饱和**」（见 §2.2），而非「规则机不是对手」 |
| `AGENT_HANDOFF.md` §4 | 「规则机不是博弈对手」警告 | 须改写 |

---

## 2. 本轮**新增**的实测发现（独立复现评审意见）

### 2.1 ⚠️ W5 的「不收敛」**形态不可迁移到 V2V 基准**（评审 B1，已独立复现）

W5 全部结论取自**自身速度基准**（`v_cmd = v_self + 0.8a`），且**无曲率限速**；
而 W6 plan §3.1 拟改用冻结 env 的**对手速度基准**（`v_cmd = v_opp + 0.8a`）。

**独立复现**（312 更新，与 W5 同预算）：

| 步窗 | W5（自身基准） | V2V（对手基准） |
|---|---|---|
| 0–200k | 1.1% | 4.2% |
| 200–400k | 0.0% | 30.2% |
| 400–600k | 0.0% | 39.0% |
| 600–800k | 30.8% | 28.6% |
| 800k–1M | **100.0%** | 35.7% |
| 1M–1.2M | **99.8%** | 26.4% |
| 1.2M–1.4M | **88.1%** | 18.2% |
| **末段（≥800k）均值** | **96.0%** | **26.8%** |

**结论**：
- 「**完整自博弈不收敛**」的**定性**方向**迁移成立**（V2V 下同样先好后坏：
  首 20% 12.5% → 末 20% 22.2%）；
- 但「**钉死 88–100%**」这一**定量形态不迁移**（V2V 下为 ~27% 振荡）。
- **部分差异是机械的**：V2V 使两车互为速度基准，实测**最大速度不对称从
  1.162 压缩到 0.765 m/s（−34%）**，故碰撞机会本身减少——
  **两个基准定义的是不同的博弈，碰撞率不可直接横向比较**。

**处置**：W6 plan 不得以「W5 的 joint 不收敛」作 W1 的对照基线；
须在 W6 自己的 env 内**重新建立**基线。

### 2.2 ⚠️ W6 plan §4 W2 的 `[35%, 65%]` 判据**饱和**（评审 B4，已独立复现）

**实测（中性起跑，60 局）**：

```
do-nothing vs do-nothing : 0.52   ← 退化策略落在带内
random     vs random     : 0.52   ← 退化策略落在带内
do-nothing vs 冻结ckpt   : 0.38   ← 落在带内
do-nothing vs 规则机     : 1.00   ← 唯一脱带
```

**一个"站着不动"的策略即可满足 W2**。这与 W5 F5 指出的「胜率判据饱和」**同类**——
plan 只把对手从「规则机」换成「快照」，**判据形态未变**。
（注意：**饱和的根因是判据本身**，与 1.1 的适配器 bug 无关，故该结论**不受更正影响**。）

### 2.3 ⚠️ 锚点门 A 按字面写必然失败（评审 B3，已确认）

```
冻结 checkpoint observation_space = Box(-inf, inf, (6,), float32)
W6 plan §3.2 拟定的新 env      = 8 维
```

**实测**：该 checkpoint 在**原冻结 env、任务模式**下确实 **10/10、0 碰撞**（seeds 2000–2009）。
但 plan §5.1 把它当作 **8 维新 env 的即插即用锚点**——8 维输入会直接抛
`ValueError: Unexpected observation shape (8,) ... please use (6,)`。
**须建 6 维 shim env** 才能用。

### 2.4 ⚠️ 碰撞**肇事者不可归因**（评审 B5，机制性结论）

两个世界的碰撞判定均为 `2D 距离 < 0.12 m`（`_selfplay_design_probe.py:113`、
`overtake_env.py:234`），**不记录相对速度/航向/侵入方**。
故 plan §3.4「一车碰撞即该车判负」**物理上不可计算**。
**出界可归因**（`e_lat` 是每车自身的量），二者**不能并列成同一条规则**。

### 2.5 ⚠️ 探针世界**另缺曲率限速**（评审 B1b，plan 差异清单不完整）

冻结 env 有曲率限速（`overtake_env.py:205-207` 的 `R_min`），
探针世界 `_selfplay_design_probe.World.step` **没有**。
故「两环境只有速度基准一处不同」**不成立**，差异清单至少缺一项。

---

## 3. 仍然成立、**不受**本次更正影响的结论

| 结论 | 状态 | 依据 |
|---|---|---|
| SB3 自定义 `VecEnv` + 共享参数**机制可用** | ✅ 成立 | `features_dim` 校正、同 obs 同动作 |
| **冻结一方能学习** | ✅ 成立，**且跨基准可迁移** | W5：100%→24%；V2V 复现：100%→0%（两基准均成立） |
| 完整自博弈**定性**方向（先好后坏） | ✅ 成立 | W5 与 V2V 复现一致 |
| 冻结 checkpoint 在**原 env 任务模式**= 10/10 | ✅ 成立 | 本轮实测 seeds 2000–2009 |
| 冻结 checkpoint **作对等对手表现差** | ✅ 成立（保留列） | 正确适配器下仍 22/40 碰撞、终局领先 = 中立基线 |
| 避坑：SB3 摊平 obs 前导维 / `SubprocVecEnv` 需 `__main__` 守卫 / 测量须隔离 | ✅ 成立 | 均独立复现 |

---

## 4. 复现方式

```bash
cd /home/zy/car_rl/code0919
# 适配器 bug 的独立复现（fake vs correct gap）
PYTHONPATH=$PWD:/tmp uv run python results/20260920_phaseC_probe/scripts/_verify_adapter_bugs.py
# 其余断言（checkpoint 10/10、W2 饱和、正确适配器下的对局统计）
PYTHONPATH=$PWD:/tmp uv run python results/20260920_phaseC_probe/scripts/_verify_rest.py
# V2V 迁移测试（312 更新，~50 分钟）
PYTHONPATH=$PWD:/tmp uv run python -u results/20260920_phaseC_probe/scripts/_v2v_v4.py
```

---

## 5. 流程教训（**本项目新增**）

**F12：适配器是"静默失效"的高发区——必须单测。**

本次两条 bug（`gap` 硬编码 1.0、`gap` 偏 L/2）**都不报错**：
策略照常动作、训练照常收敛/发散、指标照常有数——只是**读到了错的物理量**。
这与已知坑「环境持续漂移」「观测镜像写错→静默学不动」**同类**。

**对策（应写入 `DATA_MANAGEMENT.md`）**：任何**跨布局/跨语义的适配器**
（观测重映射、动作重映射、状态投影）必须附**保真度单测**：
构造已知真值状态，断言适配器输出与**权威定义**逐项一致
（本次若做此单测，`gap` 的 L/2 偏差会立即暴露）。

**F13：评审意见本身也要验。** 本轮评审的 5 条拦截级中，
作者**独立复现确认了 4 条**（B1/B2/B3/B4），1 条为机制性论证（B5）。
评审方向正确，但**"评审说的"不构成证据**——仍以复现为准。

---

## 6. 对已推送内容的处置（**已执行**）

- 本文档随本次 commit 入库，**公开更正** `a0dae34` 中的失效结论；
- `findings_phaseC_selftest.md`、`research/ASSUMPTIONS.md`、
  `research/roadmap.md`、`AGENT_HANDOFF.md` 中的相关表述**已就地加注撤回标记**；
- **不删除**原错误内容（铁律 7：旧文件归档不删除），以加注方式更正；
- **代码修复**：`_verify_f5_fix.py` 的 `selfplay_to_frozen` 已改为
  `gap_ref = (-delta) % L_REF`（从真实赛道几何求 `L_REF`，不再硬编码），
  并附 F12 警示注释；
- **新增守卫**：`scripts/adapter_fidelity_test.py`（全过）——
  以已知真值状态断言适配器输出与冻结 env 定义逐项一致，
  并显式检查"硬编码 gap 会关掉切入分支"这一失效模式；
- **补齐归档**（评审 I2）：`_ckpt_as_opponent.py`、`_isolate_opponent.py`、
  `_v2v_*.py`、`_verify_*.py`、`adapter_fidelity_test.py` 等
  10 个脚本由 `/tmp` 移入 `results/20260920_phaseC_probe/scripts/`
  （11 → 21 个），原始输出 `probe_run4/5` 一并入库。

## 7. 附带更正：一处**非** bug 的澄清

评审还指出 `plan_phaseC_w6.md` §1.2 表格中「终局领先 18/40 = 地板」的
**参照构造偏弱**：中性起跑下两相同策略本就接近 50/50（实测 do-nothing vs
do-nothing 后车终局领先 0.52），故「18/40」实为**对称中性基线**，
而非"弱策略地板"。**该列数字无误，但解释需修正**——
不能据此断言"checkpoint 与 do-nothing 等价"。
