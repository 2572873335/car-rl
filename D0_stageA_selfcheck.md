# D0 plan — 技术前提自查（作者，评审期间）

日期：2026-09-22
性质：作者在送审 D0 plan 期间，对**阶段 A（零样本、仅测距）**的技术前提的实测自查。
关联：`plan_D0_scenario_audit.md` §2.1；评审 `reviews/20260921_D0_review1.md`（进行中）

---

## 1. 实测结论：运行时覆盖 `D_DES` 是可行的（阶段 A 的间距维成立）

`follow_env.py` 里 `D_DES = 0.20` 是模块级常量，在 `_obs`/`step` 中直接引用。
**在不编辑文件的前提下，运行时覆盖 `fe.D_DES` 即可重新瞄准任务**（不违反铁律 2）。

实测（冻结 checkpoint `follow_stage2_final_v1.zip`，nominal，10 ep）：

```
  d_des=0.20 m -> 终态间距 0.206 m
  d_des=0.50 m -> 终态间距 0.505 m
  d_des=1.00 m -> 终态间距 1.005 m
```

**⇒ 终态间距严格跟踪 d_des** —— "仅测距的零样本评估"**技术上成立**。

> **预期内**：保持 0.50/1.00 m 的大间距比 0.20 更容易
> （刹车裕量更大），故 checkpoint 在这个方向**应当**表现良好——
> 这**不构成**"RL 学会了新任务"的证据，只是"大间距是更宽松的工况"。

---

## 2. 一个必须写进 plan 的**解释陷阱**

策略训练于 `D_DES=0.20`。在 `d_des=1.00` 下，
其观测 `e = (gap − D_DES)/0.5` **数值上超出训练分布**。
"间距能跟住"因此是 **"分布内任务的松弛化"**，不是**新分布下的泛化**。

**plan 须明确**：D0 间距扫描的**零样本结果只能证明"大间距不更难"**，
**不能主张"策略学会了大间距跟随"**。后者须阶段 B（重训）。

---

## 3. 阶段 A 的**速度维**做不到"仅测距"

`FollowEnv.reset` 里 `v_set ~ U(0.25, 0.50)`（代码写死）。
**不改文件就无法在评估时改变速度分布** ⇒ 阶段 A **只能扫间距、不能扫速度**。

**含义**：plan §2.1 的 9 格矩阵里，**速度维必须走阶段 B**
（改冻结文件 → 铁律 2 流程），否则 1.0 m/s 档**无法在阶段 A 测量**。

**→ plan 需修订**：把矩阵改为——
- **间距维（0.20/0.50/1.00）**：阶段 A 可做（运行时覆盖 D_DES）；
- **速度维（1.00 m/s）**：**只能在阶段 B**，且须先过铁律 2 评审。

---

## 4. 物理耦合（plan §2.3）实测预检

1.00 m/s 时刹车距离 v²/2a = 0.33 m > 0.20 m 间距 ⇒ **1.00×0.20 格子物理必撞**。
这个结论**不依赖实验**（是运动学必然），矩阵应**剪掉该格**而非浪费算力去撞。

---

## 5. 对 D0 plan 的影响（自查修订建议）

| 项 | 原 plan | 修订 |
|---|---|---|
| 阶段 A 范围 | 9 格全测 | **仅间距维 × {0.3} 速度**（3 格） |
| 速度维 | 阶段 A 内含 | **移入阶段 B**（须铁律 2 流程） |
| 零样本解释 | "测它能否工作" | **限缩为"大间距不更难"**，不主张泛化 |
| 1.00×0.20 格 | 矩阵含 | **剪掉**（物理必撞） |

> 这些是作者自查结论，**并入评审意见后统一修订**，不在评审前单方面改 plan。
