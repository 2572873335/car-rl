# Plan — Phase C W6: 联赛机制与环境完整化

日期：2026-09-21
对象：`results/2026092x_phaseC_w6/`
关联：`research/roadmap.md` §Phase C W6；`plan_phaseC_probe_v2.md`（W5 结论）；
      `research/ASSUMPTIONS.md` P1–P3；`DATA_MANAGEMENT.md` §10 评审流程

---

## 0. 本轮要做什么

W5 探针已回答：**障碍是非平稳性，不是机制**；主路径定为**冻结一方的联赛模式**。

W6 的任务（roadmap 原文）：环境完整化——零和奖励 + 混合模式（70% 任务 + 30% 对抗）
+ 观测加入对手意图；验收 = "规则基线在新环境行为合理"。

**本 plan 据 W5 的实测发现，对 roadmap 的 W6 定义提出 3 处修订**（§2），
因为其中一条验收标准的地基已被实测证伪。

---

## 1. 前置事实核实（**本轮新增实测**）

### 1.1 ★新发现 F10：冻结 env 与自博弈世界的**动作语义不同**

这是本轮最重要的技术发现，**在 W6 动手前必须解决**：

```
冻结 overtake_env.py:199-200   v_cmd = v_l   + 0.8*a    ← 以【对手速度】为基准
探针 selfplay world:101        v_cmd = v_self + 0.8*a    ← 以【自身速度】为基准
```

冻结 env 的 docstring 明写这是 "V2V prior"（`v_cmd = v_l + 0.8a`，**a=0 表示与对手同速**）。
探针世界我写成了**自身速度**基准（**a=0 表示保持当前速度**）。

**后果**：同一个动作 `a` 在两个环境里含义相反。这直接导致
**冻结 checkpoint 无法直接复用作联赛成员**（见 1.3），
且是典型的"**静默语义错位**"——正是本项目已知坑表所警示的那一类。

### 1.2 ★新发现 F11：**既有 checkpoint 不能直接当联赛对手**

隔离测试（car 0 = do-nothing 弱参照，故 car 1 的行为可单独归因）：

| car 1 策略 | 碰撞 | car1 用内圈 | 领先易手 | 终局领先 | mean v |
|---|---|---|---|---|---|
| do-nothing（地板） | 0/40 | 0/40 | 0 | 18/40 | 0.000 |
| **冻结 checkpoint** | **22/40** | **0/40** | **0** | **18/40** | 0.401 |
| 规则机（**本行无效，见下**） | 40/40 | 0/40 | 18 | 0/40 | 0.986 |

**冻结 checkpoint 的表现恰好等于地板**：终局领先 18/40（与 do-nothing 的
初始条件基线完全相同）、用内圈 0/40、领先易手 0 次，且**多出 22/40 碰撞**。

**根因**：该 checkpoint 训练时是**追慢车的外圈跟随者**（leader 固定 0.15–0.30 m/s、
只跑外圈），从未作为**同速对等竞争者**或**防守者**训练过——属"构造上就在分布外"。

> **ARM B（规则机）无效声明（诚实披露）**：该行 40/40 碰撞是**我的适配器造成的**——
> `selfplay_to_frozen` 把 gap 伪造成常数 1.0 m，规则机读作"距离很远"→ 全油门接近，
> 在自身速度基准下变成猛冲撞车。**这一行不能作为规则机的证据**。
> （规则机不是对手这一结论由 W5 的 rule-vs-rule 40/40 零领先易手独立支撑。）

### 1.3 结论：联赛成员必须**从头生产**，不能复用旧 checkpoint

W6 的对手池需要**专门训练**的对手，而非 `ckpt_ot/*.zip`。
好消息是 W5 已证明"冻结一方"训练**成本很低**（409,600 步 / 832 s 即从 100% 降到 24% 碰撞），
故生产联赛成员是**分钟级**的事。

---

## 2. 对 roadmap W6 定义的三处修订

| # | roadmap 原文 | 修订 | 依据 |
|---|---|---|---|
| **R1** | 验收 = "**规则基线**在新环境行为合理" | **改为**："**用专门训练的快照**验证环境自洽（冻结对局下碰撞率可下降），规则基线**仅作参考锚点**" | F11/W5：规则机**不是博弈对手**（40/40 零领先易手、零内圈使用）——拿它做验收标准测不出新环境的对抗质量 |
| **R2** | 奖励"零和（超车+1/被超−1）" | **明确区分**：**非终止项**零和；**终止罚（碰撞/出界）同时施加两车故不零和**——这是**有信息量的失败率信号**，须**保留并记录** `mean(r0+r1)` | W5 自查 F4 实测：`mean(r0+r1) = −(1000·p_coll + 200·p_off)` 逐行吻合 |
| **R3** | "观测加入对手意图" | **给出可执行定义**：对手意图 = **对手换道倾向的可观测代理**（如 `lane_other`、`|Δlane|` 近窗），**禁止**用需要真值动作的"密谋"式特征 | 保持自视角可观测性（否则自博弈变为集中式训练，偏离 IPPO 设定） |

---

## 3. 环境设计：`selfplay_env.py`

### 3.1 动作语义：**采用冻结 env 的 V2V 基准**（解决 F10）

为保住"与冻结基线可比"这一 W6 核心诉求，**动作语义统一到冻结定义**：

```
v_cmd = v_opponent + ACT_GAIN * a      (a = 0  <=> 与对手同速)
```

**理由**：
1. 与冻结 `overtake_env.py:199-200` **逐字一致**，可比性成立；
2. 保留"V2V 广播"这一项目核心叙事（`a=0` 永不追尾的几何先验）；
3. 使**冻结 checkpoint 有可能**在适配后复用（虽然 F11 表明仍需重训）。

**须在 plan 与代码注释中显式声明此选择**，并作为 §1.1 的修复记录。

### 3.2 观测：保留冻结 6 维布局 + 追加对手意图维度

**保留**冻结 6 维（`[(gap−0.2)/0.5, delta/2.5, v_f/1.3, e_lat/0.25, lane, v_l/0.5]`），
使 `baseline_action_ot` **可直接复用**（W5 评审 I2 的教训：接口错位=静默学不动）。

**追加**（R3 的可执行定义，自视角可观测）：
- slot6：`lane_other`（对手当前车道，0/1）
- slot7：`|lane_other − lane_self|`（是否已在异道）

**共 8 维**。**必须出"冻结 7 槽 vs 新 8 槽"逐槽对照表**，
并**下调"直接可比"措辞**为"前 6 槽语义一致，第 7–8 槽为新增"。

### 3.3 测量参考路径（W5 评审 §五-2 的静默 bug 风险）

`gap`/`delta` **必须把两车 s 都投影到外圈**（共同参考），
与冻结 `overtake_env.py:123-127` 逐字一致。
**若各车在自己车道上算 s，两车 s 属不同总长（5.4849 vs 4.5424 m）的坐标系，
差值无意义**——这是最可能的静默失效点，须写单测。

### 3.4 终止与计分（W5 评审 I3/I4 的落地）

- **套圈**：胜负引入**累积圈数**（不用裸 `delta>0`——30 s 内车跑 ~3.8 圈，
  delta 在 ±L/2 回绕）；参照冻结 `ahead_flag` 永不重置的做法。
- **单方失败**：定义"**一车碰撞/出界即该车判负，另一车判胜**"，
  **不**强制同生共死（W5 评审 I4：同生共死抹掉"封堵/致对手出局"这一制胜路径，
  而这正是不对称性使封堵有意义之处）。
  **实现注意**：SB3 `DummyVecEnv` 每行独立 auto-reset，跨行同 world 会相位错——
  须用**单进程自定义 VecEnv 统一管理 world 生命周期**（W5 已实现该模式）。

### 3.5 混合模式（roadmap：70% 任务 + 30% 对抗）

- **任务模式**（70%）：对手 = 脚本慢车（冻结 leader 行为），奖励 = 冻结 env 的超车奖励；
- **对抗模式**（30%）：对手 = 联赛快照，奖励 = 零和（R2 口径）。

**须在 episode 级随机切换并记录模式标签**，以便分模式报告指标
（否则两个分布混在一起，指标无法解释）。

---

## 4. 假设登记（**训练前定死**）

| # | 假设 | 判定阈值（**定死**） | 若成立 | 若证伪 |
|---|---|---|---|---|
| **W1** | 混合模式下训练**稳定**（不重演 W5 的 joint 不收敛） | 末 20% 训练的 `train/std` 不单调趋 0，且**对抗模式**碰撞率**不高于**首 20%（相对 ≤ 1.2×） | 混合模式缓解非平稳性 | 退回纯冻结一方 |
| **W2** | 策略在**对抗模式**下具备竞争力 | 对**训练中快照**胜率 ∈ **[35%, 65%]**（近均衡）——**过高/过低都说明对手池失效** | 联赛健康 | 检查对手池陈旧度 |
| **W3** | 观察到**涌现对抗行为** | 出现 ≥1 类非平凡行为（**封堵**：对手变道时内线跟防；**或**关门：占住内圈入口），**须视频/轨迹佐证** | RQ3 核心素材 | 如实记录"无涌现" |
| **W4** | 任务模式**不退化** | 任务模式超车成功率 **≥ 8/10**（对照冻结 10/10，允许小幅退化） | 混合未牺牲原任务 | 加任务模式权重 |

**W4 特别重要**：混合模式的**主要风险是灾难性遗忘**——加入对抗后
把原本 10/10 的超车能力弄丢。必须**分模式评估**才能发现。

---

## 5. 执行方案

### 5.1 Day 1 — 环境 + 锚点门
1. 写 `selfplay_env.py`（8 维观测、V2V 动作语义、外圈共同参考、单方失败计分）；
2. **锚点门 A**：冻结慢车场景下，**冻结 checkpoint** 作后车须复现 ~10/10
   （证明新 env 与冻结 env **在任务模式下等价**——这是可比性的硬证据）；
3. **锚点门 B**：快照 vs 快照 40 局，验证环境自洽（不瞬间碰撞、终止分布合理）；
4. **单测**：`_measure` 参考路径、观测前导维（`features_dim==8`）、
   `SubprocVecEnv` 不需要（单进程）。

### 5.2 Day 2–3 — 联赛训练
- 对手池：初始 = [脚本慢车]；每次训练结束**存快照入池**，池上限 5，**淘汰最旧**；
- 训练 ≥300 更新（**W6 是产出结论的训练，铁律 4 适用**：`8 worlds × 2 = 16 行`，
  `n_steps=256` ⇒ ≥1,228,800 步）；
- 周期评估：对池中每个对手 + 随机对手，**分模式**报告。

### 5.3 Day 4–5 — 评估 + 归档
- W1–W4 逐条判定；对抗行为视频/轨迹；RUNLOG；评审归档。

---

## 6. 风险与预案

| 风险 | 概率 | 影响 | 预案 |
|---|---|---|---|
| 混合模式仍不收敛 | 中 | W6 失败 | 退回纯冻结一方；降低对抗比例到 10% |
| **灾难性遗忘**（W4 失效） | **中高** | 丢原任务 | 加任务模式权重 / 冻结部分网络 / 早停 |
| 观测扩充破坏与冻结可比性 | 中 | 可比性失效 | §3.2 对照表 + 锚点门 A 硬验证 |
| 参考路径写错 → 静默学不动 | 中 | 白烧时间 | §3.3 单测（W5 评审点名的最可能触发点） |
| 对手池陈旧 → 无对抗压力 | 中 | W2 失衡 | 池上限 5 + 淘汰最旧 + 快照频率 |
| 动作语义再次错位 | 中 | 静默失效 | §3.1 显式声明 + 锚点门 A |

---

## 7. 时间预算

| 项 | 估计 |
|---|---|
| Day 1：环境 + 两个锚点门 + 单测 | 1 天 |
| Day 2–3：联赛训练（~1.23M 步，分钟级）+ 周期评估 | 1–2 天 |
| Day 4–5：评估 + 行为分析 + 归档 | 1–2 天 |
| **合计** | **~5 天**（roadmap 给 W6 一周，余量充足） |

---

## 8. 关联

- 输入：`car_following_sim.py`（只读）、冻结 `overtake_env.py`（**动作语义与布局基准**）
- 上游：`plan_phaseC_probe_v2.md`（W5 结论）、`findings_phaseC_selftest.md`
- 产出：`results/2026092x_phaseC_w6/`、`research/ASSUMPTIONS.md` W1–W4
- 流程：本 plan → **独立评审** → 修订 → 执行（`DATA_MANAGEMENT.md` §10）

---

## 待评审确认点

1. **§2 R1**：验收标准由"规则基线"改为"专门训练的快照"，是否同意？
2. **§3.1**：动作语义统一到冻结的 **V2V 基准**（`v_cmd = v_opponent + 0.8a`）是否正确？
   这会改变 W5 探针世界的语义——是否需要在 plan 里同时披露该差异的历史？
3. **§3.2**：观测扩充到 8 维（追加 `lane_other`、异道标志）是否足以表达"对手意图"？
   有无更强的自视角代理？
4. **§3.4**：单方失败计分（不强制同生共死）是否引入了新的"故意撞车"退化策略？
   （若一车宁可撞也要阻止对手，胜负判据是否被钻空子？）
5. **§4 W2**：胜率 ∈ [35%,65%] 的近均衡判据是否合理？
   会不会因对手池过弱而轻松满足、失去区分度？
6. **§4 W4**：任务模式 ≥8/10 的容忍度是否合适（原为 10/10）？
7. **§5.1 锚点门 A**：若冻结 checkpoint 在新 env 下**无法**复现 10/10，
   该判"新 env 不等价"还是"checkpoint 过拟合旧 env"？如何区分？
