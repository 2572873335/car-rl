# Step 0 实测记录 — 判据区分度检验（**结果：三版判据均未通过**）

日期：2026-09-21
性质：`plan_phaseC_w6_v2.md` §3 要求的**判据前置验证**的实际执行结果。
结论：**M1 判据尚未确立**，本文件如实记录三次失败与被发现的结构性问题。

---

## 摘要

W5 与 W6 v1 都以同一方式失败：**判据未经验证即投入使用**（退化策略即可通过）。
v2 因此把"判据验证"设为开训前提（Step 0）。**执行结果是：我提出的三版判据
全部未能区分退化策略**——这恰好证明了 Step 0 的必要性。

| 版本 | 判据 | 退化策略能否通过 | 裁决 |
|---|---|---|---|
| v1（W6 原稿） | 胜率 ∈ [35%, 65%] | **能**（do-nothing 0.52、random 0.52） | ❌ 饱和 |
| Step 0-A | 碰撞率 ≤ 10% | **能**（zero-action 0.0%、full-throttle 0.0%） | ❌ 可游戏 |
| Step 0-B | 碰撞率 ≤ 10% **且** 里程 ≥ 阈值 | **能**（full-throttle 里程 38.48 m ≈ 活跃策略 37.99 m） | ❌ 可游戏 |
| Step 0-C | 非对称对局下的胜率 > 0.5 | **部分能**（zero-action 0.54 > 0.5） | ❌ 仍饱和 |

---

## 1. 实测数据

### Step 0-A：碰撞率可被"不动"满足

```
       policy            coll%
zero action (degenerate)    0.0%    ← 通过
   full throttle            0.0%    ← 通过
   random                  83.3%
```

**根因**：两辆都不动的车**永远不会相遇**，碰撞率恒为 0。
"低碰撞"可以由**不作为**换来，与"策略学得好"无关。

> 对照：已知不稳定的两条训练序列均**失败**该判据
> （W5 joint 末段 96.1%、V2V joint 22.5%）——**说明该判据能识别"坏训练"，
> 但也能被"什么都不做"骗过**。判据必须同时排除"不作为"。

### Step 0-B：里程也不能作为"不作为"的护栏

```
       policy           coll%   dist(m)
zero action              0.0%      0.00
   full throttle         0.0%     38.48   ← 与活跃策略相当
   random               68.3%      4.32
lane-flip only           0.0%      0.00
frozen ckpt (active)     1.7%     37.99   ← 参照
```

**根因**：`full-throttle`（两车都全油门）**里程 38.48 m，与活跃策略 37.99 m 几乎相同**，
且碰撞率 0.0%——它只是绕圈，不做任何博弈。
**"跑得多"≠"学得好"。**

### Step 0-C：非对称对局下的胜率**仍然饱和**

（learner = car 0，对手 = 固定的 zero-action 车）

```
                 learner     win    loss    coll     dist
  zero action (degenerate)  0.54    0.46    0.00     0.00   ← 仍 > 0.5
   full throttle            0.00    1.00    1.00     2.73
        random              0.08    0.80    0.80     4.72
      lane-dive only        0.66    0.34    0.16    32.11
  frozen ckpt (active)      0.54    0.46    0.46     0.91
```

**两条关键观察**：

1. **zero-action 仍得 0.54 > 0.5**——即使面对固定对手，"终局谁在前"
   依然主要**由初始条件决定**（中性起跑下，两车都不动时先起跑的那辆在前）。
2. **"活跃"参照本身是坏的**：冻结 checkpoint 在此世界中**只走 0.91 m**
   （≈ 不动），碰撞 46%——因为该 checkpoint 是按**冻结 env 的观测/动作语义**
   训练的，**在本世界不可用**（与 `ERRATUM` §1.2 的结论一致，互为佐证）。

---

## 2. 结构性发现（**本次最重要的产出**）

### F14：**对称自博弈的评估在构造上是退化的**

> 当**同一个**（确定性）策略同时驱动两辆车时，对局结果**完全由初始条件决定**——
> 没有对手来体现技能差异。因此**任何**在"策略 vs 自身"上计算出来的指标
> （胜率、终局差、碰撞率）**都无法区分策略优劣**。

这是 W5 F5（胜率饱和）、W6 v1 W2（同）、以及本轮 Step 0-C（非对称仍饱和）
的**共同根因**，也是它们的一般形式：

| 现象 | 具体表现 | 本发现给出的解释 |
|---|---|---|
| W5 F5 | 「对规则机胜率」未训练即 20/20 | 初始位形决定胜负 |
| W6 v1 W2 | [35%,65%] 带，do-nothing 0.52 | 对称对局的结果 ≈ 随机 |
| Step 0-A | 碰撞率 0.0%（不动即通过） | 不作为不被惩罚 |
| Step 0-B | 里程 38.5 m（绕圈即通过） | 该指标与博弈质量正交 |
| Step 0-C | 非对称下 zero-action 仍 0.54 | 初始条件主导终局位置 |

**推论**：M1 的验收**不能**建立在"终局相对位置"类指标上（无论对称与否），
除非**初始条件被消除为变量**。

### F15：可用的判据设计方向（**待验证，尚未成立**）

由 F14 反推，可行的方向必须**让初始条件的贡献可分离**：

1. **固定"落后起跑"**：让被测策略**始终从落后位置出发**，
   则"是否取得领先"必须靠**真实超越**产生（W5 评审 F5 修复方案 1 即此思路，
   但本轮 Step 0-C 用的仍是中性起跑，故未检验）；
2. **任务级判据**（最稳）：直接采用**冻结 env 自己的成功定义**
   （从落后起跑、在 T 秒内完成超越），而非自造对抗指标；
3. **分阶段难度**：对手从"静止"→"慢速规则"→"快照"，逐级证明策略能应对，
   **每级单独判定**，而非一次性用单一胜率阈值。

**这三个方向本轮均未验证**——它们是 M1 判据设计的**待办**，不是结论。

---

## 3. 对 plan v2 的影响（**须修订**）

`plan_phaseC_w6_v2.md` §3/§5 把 M1 判据写成"已设计"，
但 Step 0 证明**它们尚未成立**。须修订为：

- §3：判据仍为**未完成项**；Step 0 的结论是"三版均饱和/可游戏"，
  并附 F14 的结构性解释；
- §5：M1.1/M1.2 **暂不登记**——判据未验证前登记阈值违反本项目
  "阈值须有依据"的既有规范（H1–H7 的登记均附理由）；
- **新增前置任务**：按 F15 的三个方向**重新设计并验证**判据，
  **验证通过后**才登记假设、才开训。

> **这正是 Step 0 的价值**：它在**训练开跑前**拦下了第三、四次
> "判据饱和"错误，而不是等到训练结束才发现无法判定。

---

## 4. 复现

```bash
cd /home/zy/car_rl/code0919
PYTHONPATH=$PWD:/tmp uv run python results/20260921_phaseC_w6/step0_a_collision.py
PYTHONPATH=$PWD:/tmp uv run python results/20260921_phaseC_w6/step0_b_progress.py
PYTHONPATH=$PWD:/tmp uv run python results/20260921_phaseC_w6/step0_c_asymmetric.py
```

---

## 5. 流程教训（新增）

**F16：判据设计必须"反例先行"。**

三版判据的失败模式完全相同：**先想一个看起来合理的指标，
再用它去评估真实策略**。正确的顺序是**反过来**——
先构造**已知的退化/平庸行为**（不动、绕圈、乱动），
**确认它们在判据下失败**，然后才把判据用于真实策略。

本轮若不做 Step 0，M1 会跑完一周并得到"碰撞率 0%，判定通过"的**荒谬结论**
（zero-action 的结果），或"胜率 0.54，判定通过"。

**建议写入 `DATA_MANAGEMENT.md`**：任何判据（含假设登记里的阈值）
**投入使用前**必须附"**退化反例表**"——列出至少两个已知劣质行为
及其在该判据下的取值，证明判据能排除它们。
