# ERRATUM 3 — 判据 v2/v3/v4 全部作废 + F20（本赛道占优策略是几何，不是博弈）

日期：2026-09-21
性质：对 `plan_W6_criterion_v2.md` 及其后继（v3 `FULL`、v4 `COMPLETE`）的撤回。
触发：review2 **Reject**（4 条拦截级）；作者**逐条独立复现**。
关联：`ERRATUM2_criterion.md`、`reviews/20260921_W6_criterion_review2.md`、
      `DATA_MANAGEMENT.md` §10.4

---

## 0. 一句话

**我连续四版判据（W6 v1、criterion v2/v3/v4）都被"两行几何策略"击穿。**
这不是判据没调好，而是**本赛道的一个结构性事实**：
**在单圈赛道 + 内圈更短的几何下，"占住内圈"是占优策略，
超车能力可以被几何完全替代**（F20）。四版判据的失败是这条事实的**四种表现**。

---

## 1. 已独立复现的拦截级（review2）

### R1 ★ 两行几何策略拿到满分，与正例**逐位相同**

评审给出的策略（**无任何调速技能，`a_speed` 恒 0**）：

```python
def geom(o, L):
    d = o[0] * 2.5
    return np.array([0.0, 1.0 if d < 0.30 else -1.0])   # 落后切内圈，领先回外圈
```

**我的独立复现（三组 seed，各 30 局）**：

| seed0 | 策略 | FULL | ot | 碰撞 | t_overtake |
|---|---|---|---|---|---|
| 123000 | 正例 ckpt | 1.00 | 1.00 | 0% | **1.62 s** |
| 123000 | **geom（两行）** | **1.00** | 1.00 | 0% | **19.37 s** |
| 200000 | 正例 ckpt | 1.00 | 1.00 | 0% | 1.67 s |
| 200000 | **geom** | **1.00** | 1.00 | 0% | 20.14 s |
| 300000 | 正例 ckpt | 1.00 | 1.00 | 0% | 1.61 s |
| 300000 | **geom** | **1.00** | 1.00 | 0% | 19.53 s |

**确认**：`FULL` 与正例**无法区分**。
根因：`FULL` 只要求"曾领先 + 回一次外圈"，
而**"回外圈"对任何已领先的策略是免费的**——`geom` 靠内圈几何取得领先后回外圈即可。

### R2 ★ `FULL=1.00` 可与 **93–100% 碰撞**共存

复现：`geom_slow`（恒定 `a=-0.3`）FULL=1.00，**碰撞率 93–100%**。
根因：`m_full()` **从不检查碰撞**——因变量与安全性**完全解耦**。

### R3 ★ 送审 plan 正文**没有**"正例已找到"一节（**作者的文件错误**）

- repo `plan_W6_criterion_v2.md` §0/§2 白纸黑字：
  > 「**目前没有任何合法正例**——所有候选正例的 `FULL` 均为 **0.00**」

  与 commit `db5b5f1` 的 "valid positive pole found, FULL=1.00" **直接矛盾**。
- **根因（我的操作错误）**：我先修改了 repo 副本并写入 §2b，
  **之后又用 `_stage` 的旧版覆盖了 repo 副本**，把 §2b 抹掉，然后提交。
  评审核实的正是"被覆盖掉的那一节"。
- **教训**：**`_stage` 与 repo 两份 plan 互为副本**——
  与 F18/F19 的 `/tmp` 双副本**同源**。写回时必须确认**哪一份是权威**。

### R4 ★ `FULL` 把"做得更好"的策略判 0（套圈回绕 bug）

复现（规则机，逐步轨迹）：

```
  t=2.26  delta=+0.238  → 首次领先（t_first_ahead=2.26）  ✅
  t=4.02  delta=+0.935
  t=8.02  delta=+2.310  → 持续拉开（在套圈）
  t=9.36  delta=-2.737  ← 恰好 ≈ -L/2 = -2.7425（wrap 边界）
  ...
  FULL = 0.00   ← held_after_return 在第 1 行被翻为 False
```

**规则机 30/30 局完成"超越 + 回外圈 + 零碰撞、2.2 s"**，
却因 **delta 在 ±L/2 回绕**被判"失去领先"，FULL=0.00。

冻结 env 对此**有专门守卫**（`overtake_env.py:229-231`：`ahead_flag` **永不复位**），
**`FULL` 没有继承该守卫**。

---

## 2. ★F20：本赛道的占优策略是**几何**，不是博弈（**本轮最重要的结论**）

### 2.1 v4 的最后一次尝试（如实记录）

为修 R1/R2/R4，我实现 v4：
- **wrap-safe**：改用**未回绕的累计弧长**算 `delta_true`（修 R4，**成功**）；
- **碰撞闸**：`complete AND collision ≤ 10%`（修 R2，**成功**）；
- 结果（seed 123000）：

```
   zero-action    COMPLETE 0.00   coll   0%   med_t  nan    fail   ← 对照✅
   full-throttle  COMPLETE 0.00   coll 100%   med_t  nan    fail
   loiter         COMPLETE 1.00   coll   0%   med_t 20.01  PASS
   geom 2-line    COMPLETE 1.00   coll   0%   med_t 20.01  PASS   ← 仍通过
   geom_slow      COMPLETE 0.00   coll 100%   med_t  nan    fail
   rule machine   COMPLETE 1.00   coll   0%   med_t  2.63  PASS
   ckpt (+)       COMPLETE 1.00   coll   0%   med_t  1.64  PASS
```

**zero-action 归零 ⇒ 度量本身是健康的**；**规则机恢复 1.00 ⇒ R4 已修**。

### 2.2 加时间界后的**决定性结果**

观测量里唯一有分辨力的是 `t_overtake`。加 `t ≤ T` 后：

```
                  T=3.0  T=5.0  T=8.0   coll%   med_t
   ckpt (+)        PASS   PASS   PASS      0%    1.64
   rule machine    PASS   PASS   PASS      0%    2.63
   geom 2-line     fail   fail   fail      0%   20.01   ← 被时间界挡住
   loiter          fail   fail   fail      0%   20.01   ← 被挡住
   geom_dive_early PASS   PASS   PASS      0%    1.64   ← ★ 又通过
   geom_smart      PASS   PASS   PASS      0%    1.64   ← ★ 又通过
   zero-action     fail   fail   fail      0%     nan
```

**`geom_dive_early`（`[全油门, 临近就切内圈]`）与 `geom_smart` 同样拿到 1.64 s**，
**与正例完全一致**。

### 2.3 结论（F20）

> **在"单圈 + 内圈更短 + 对等速度"的赛道几何下，
> "抢到内圈 + 保持速度"就足以在约一个单圈时间内完成"超越"，
> 且不需要任何调速/博弈决策。**

因此在**本赛道**上：
- **任何**以"是否取得领先 / 何时取得领先"为因变量的判据，
  **都必然被两行几何策略满足**（已用四版判据验证）；
- 想区分"几何"与"技能"，判据必须引入**几何无法替代的能力维度**——
  例如**对手会反击**（对抗）、或**更快的对手**（需真实速度控制）、
  或**多圈竞速**（几何优势被对手的应对抵消）。

**这不是判据实现的失败，是任务设计的事实。**
**M1 用"能否学会超车"作验收，在本赛道上是无法与"写两行几何"区分的。**

---

## 3. 仍然成立的资产（**不受本次撤回影响**）

| 资产 | 状态 |
|---|---|
| **B1 适配器修复**（`(-delta) % L`）与**保真度单测 v2**（守卫真实适配器） | ✅ 评审确认 |
| **B2 随机源**（`uniq_delta` 1 → 30/30） | ✅ 评审确认 |
| **R4 的 wrap-safe 修法**（未回绕累计弧长） | ✅ 本轮实现并验证 |
| **碰撞闸**的必要性 | ✅ R2 证实 |
| **正例本身是真的**（1.6 s 完成机动、零碰撞、三 seed 可复现） | ✅ 评审确认 |
| **F18/F19 修复**（脚本可从仓库复现 + 回归守卫） | ✅ 已提交 |
| `t_overtake` 是**唯一**表现出分辨力的量 | ✅ 本轮实测 |

---

## 4. 处置

- **criterion v2/v3/v4 全部作废**；M1 阈值**不登记**；
- **M1 的验收判据须重新定义为几何不可替代的维度**
  （对抗反击 / 更快对手 / 多圈），**待评审**；
- R3 的**文件覆盖错误**已定位（`_stage` 与 repo 双副本），须在写回流程上加确认；
- 本文档与 review2 一并入库。

---

## 5. 流程教训（新增）

**F21：`_stage` 与 repo 的双副本 = F18 的同型问题。**
本轮 plan 正文被旧副本覆盖，导致**送审内容与 commit 内容不一致**（R3）。
**对策**：plan 的**唯一权威副本应在 repo 内**，
`_stage` 只作草稿且**写回后立即废弃**，不得反向覆盖。

**F22：判据连败四版 ⇒ 应质疑"任务能否支撑该判据"，而非继续调判据。**
四版判据每次都换个因变量，**每次都失败**——
正确的反应是**停下来质疑任务设计**（本轮即 F20），
而不是第五次发明新指标。
