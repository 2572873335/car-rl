# Phase C 探针 plan — 独立实测发现（plan 作者自查）

日期：2026-09-20
性质：**plan 作者在送审同时自行做的实测**，与独立评审（`review_phaseC_probe_1.md`）
      互补。所有数字来自本机真实命令输出（铁律 1）。
状态：**7 项发现，其中 2 项为拦截级**

---

## 摘要

送审后自查发现 **plan 里 3 条结论是错的**，且都属"引库警告/想当然代替实测"这一类
（正是本项目铁律 1 打击的对象）：

| # | plan 原文 | 实测结论 | 级别 |
|---|---|---|---|
| F4 | §4「对称零和 ⇒ 平均回报恒 0，不可作进度指标」 | **错**。回报确实携带信号 | **拦截级** |
| F5 | §4 P2「对规则对手胜率 ≥50%」 | **错**。判据饱和；**且规则机根本不是对手**（40/40 局零次领先易手、零次用内圈），不可修复，须换判据 | **拦截级** |
| **F9** | §6.1/§6.2「按此设计跑 3 天探针」 | **完整自博弈不收敛**（1.28M 步后碰撞率钉死 88–100%）；**冻结一方即学会**（100%→24%）。障碍是**非平稳性**，不是机制 | **拦截级（决定探针主路径）** |
| F6 | §6.3「`device='cpu'`」（引 SB3 警告） | **选择正确但论证错**；初测被并发污染误判 cuda 更快，**复测确认 CPU 快 27–40%** | 重要 |
| F2 | §1.2「1.28M 步 ≈ 1–3 分钟」 | 算术低估；实测 8 路并行效率仅 36% | 重要 |
| F8 | —（plan 未提） | **性能测量须串行隔离**，否则数字反转（F6 教训） | 流程 |
| F1 | §3.2 镜像观测 | **正确**（已实测确认） | — |
| F3 | §1.4 obs 前导维摊平坑 | **正确且已复现** | — |
| F7 | §5 铁律 4 算术 | **正确**（100k@16行=24.4 次更新）；但评审 I5 指出探针**本就豁免**铁律 4 | — |

**结论**：探针**仍应进行**，但 §4 的判据必须重设计（见 F4/F5 修复方案），
§6.3 改 cuda，§1.2 时间预算改为 ~4 分钟（仍是 3 天预算的零头）。

---

## F1（确认正确）§3.2 镜像观测数学

实测（真实赛道几何）：

```
L_outer=5.485 m  L_inner=4.542 m
obs(car0) = [ 0.1521  0.      0.      0.      0.      0.9449]
obs(car1) = [-0.1521  0.      0.      0.      0.      0.9449]
delta terms sum to ~0? +0.000000 (should be 0)
v terms swapped? True
```

镜像规则**成立**。plan 的有符号弧长方案正确。

**同时确认不对称性是真的**（非假设）：

```
inner is 17.2% SHORTER => asymmetry is real, not hypothetical
```

## F2（重要）§1.2 时间预算低估

实测并行吞吐（冻结 `OvertakeEnv`，规则策略驱动全部行）：

```
n_envs= 1:  1.00x  (efficiency 100.0%)
n_envs= 4:  1.81x  (efficiency  45.3%)
n_envs= 8:  2.89x  (efficiency  36.1%)
n_envs=16:  3.40x  (efficiency  21.3%)
```

→ 并行扩展**远非线性**（env 步进是纯 Python/numpy，IPC 与 GIL 开销主导）。
plan 说「8 路并行后 ~1–3 分钟」偏乐观；实测 1,228,800 步应按
**~5,600 步/s → ≈3.7 分钟** 报。**量级结论不变（仍远低于 3 天）**，但数字要改真。

## F3（确认，且需补进计划）SB3 obs 前导维摊平 + SubprocVecEnv 需 `__main__` 守卫

两个**真实踩到的**机械坑：

```
坑1: observation_space=Box(shape=(2,4)) -> RuntimeError:
     mat1 and mat2 shapes cannot be multiplied (2x4 and 8x64)
     正解：声明单 agent 形状 (6,)，用 num_envs=2N 表达多行
坑2: SubprocVecEnv 顶层直接建 -> EOFError: unexpected EOF
     (forkserver 反复 re-import 本模块)
     正解：训练脚本必须有 if __name__ == "__main__": 守卫
```

两条都要写进 `selfplay_vecenv.py`/`train_selfplay.py` 注释。

## F4（**拦截级**）§4「平均回报恒 0，不可作指标」是错的

plan §4 称：对称零和 ⇒ 平均回报 ≈0 ⇒ 不可作进展指标。

**实测证伪**（4 种质量不同的策略各 40 局）：

```
policy           mean(r0+r1)  collision%  offtrack%   predicted
zero                     0.0        0.0%       0.0%        -0.0
random                -825.0       82.5%       0.0%      -825.0
rule                     0.0        0.0%       0.0%        -0.0
full-throttle            0.0        0.0%       0.0%        -0.0
```

**根因**：碰撞（−500）与出界（−100）罚**同时施加给两个 agent**，
故这些项**不是零和**（两车各 −500 ⇒ 该步 r0+r1 = −1000）。于是

```
mean(r0+r1) = −(1000·p_collision + 200·p_offtrack)          ← 与实测逐行吻合
```

**含义**：平均回报**恰恰携带"失败率"信号**——而失败率正是 roadmap 给探针定的
通过标准（"哪怕只是不再撞墙"）。plan §4 让执行者忽略这个**最直接**的指标，
属自毁武功。

**修复**：
1. §4 改为：**平均回报 = 失败率代理量，是有效进度指标**；保留"策略熵下降"
   作为辅助，但不再宣称回报无效。
2. **明确声明**：本博弈仅在**非终止项**上零和；终止罚是**共享**的。
   若 W6 要严格零和，可把碰撞罚拆成对称 −500/−500（r0+r1=−1000 本就零和），
   或引入"责任判定"区分肇事/受害（需额外规则，W6 再议）。

## F5（**拦截级**）§4 P2 胜率判据饱和，测不出东西
实测（我自己的 pilot gate）：

```
--- win rate BEFORE training (untrained shared policy) ---
  vs rule-based : W20 L0 T0        ← 未训练就 20/20
```

**追查根因**（两道）：

```
CHECK 1: delta0 at reset over 20 episodes: mean=+0.428 m, min=+0.180, max=+0.591
         all positive (car 0 always starts ahead)? True
CHECK 2: car0(do-nothing) still ahead in 20/20   (两种 obs 映射下均是)
```

1. **car 0 每局都结构性领先 0.43 m**（我按 `s_A − i*gap0` 布置起跑位）；
   而 P2 判"胜"= 视界结束时 `delta_self > 0` ⇒ **站着不动就赢**。
   判据**饱和**，P2 的 70%/50% 阈值形同虚设。
2. **规则对手被喂了错位的观测**：冻结 `baseline_action_ot` 读
   `obs[0]=gap`、`obs[1]=delta`、`obs[4]=lane`，而我的自博弈观测布局是
   `obs[0]=delta`、`obs[1]=v_self`、`obs[4]=lane` ⇒ 语义错位。
   （即便修正映射，CHECK 2 显示仍是 20/20，说明**根因 1 更致命**。）

**修复**：
1. **起跑位改为中性**：两车相差 ±gap0/2（或随机化领先方），使初始 `delta≈0`，
   胜负必须靠**真实超越**产生。
2. **显式写 obs 适配层** `selfplay_obs_to_frozen(obs)`，把自博弈观测映射成
   冻结布局后再喂规则对手——**且必须单测**（同状态两种编码给同一对手，
   动作应一致）。这类"静默语义错位"正是本项目"环境漂移"坑的同类。
3. P2 改为：**随机化初始领先方**后，对规则对手胜率 ≥50% **且**
   对随机对手胜率 ≥70%，并在"对手被赋予领先"的子集上单独报告。

### F5 后续（**比 F5 本身更严重**）：修复方案部分失效，暴露对手问题

按上条修复（随机化领先方 + 显式 obs 适配层）后**复测**，结果：

```
policy           OLD (biased start)    NEW (neutral start)
do-nothing                W60 L0 T0              W60 L0 T0
random                    W60 L0 T0              W56 L0 T4
full-thr                  W60 L0 T0              W32 L28 T0
```

→ **`full-throttle` 去饱和了**（60/60 → 32/60），说明随机化起跑**部分有效**；
但 **`do-nothing` 仍 60/60**。

**追根**：让**规则机 vs 规则机**在中性起跑下对打 40 局：

```
lead changes per episode: mean=0.00, max=0
episodes with ZERO lead change: 40/40
did either car USE the inner lane? 0/40 episodes
```

**结论（W6 设计必须知道）**：冻结的规则状态机**根本不是博弈对手**——
它在 40/40 局里**从未使用内圈、从未发生领先易手**。它只会**跟车**，
只在"前车比自己慢得多"时才超车；**对等对手面前它不竞争**。

**因此**：
- **P2 的"对规则对手胜率"判据不可修复**，必须替换为
  **(a) 对训练快照（self-play 联盟成员）的胜率**，或
  **(b) 任务级判据**：从**落后**起跑，能否在 T 秒内完成超越
  （沿用冻结 env 自己的 success 定义）。
- 这条同时是 **W6 联赛设计的输入**：self-play 联盟 ≠ "vs 脚本"，
  脚本对手不能充当陪练的对抗基线。

## F6（重要）§6.3 `device='cpu'`：**初测判错，复测确认 CPU 更优**

plan 引 SB3 的 UserWarning（"GPU utilization will be poor"）作依据——
**这正是铁律 1 禁止的"引库警告代替实测"**。

**初测（错误）**：16 行自博弈 `VecEnv`，得 `cpu 783 / cuda 1470 steps/s`，
一度以为警告不成立。**该测量被污染**——当时另一个 1.28M 步训练正在同机
并发运行，抢占了 CPU。

**复测（GPU 空闲、两套 VecEnv 设计各测一遍，且与独立评审交叉验证）**：

```
[A] SubprocVecEnv（8 个独立 env 进程，评审的配置）
    cpu  :  2,587 steps/s
    cuda :  1,845 steps/s        -> cpu 快 40%
[B] 单进程自定义 VecEnv（8 worlds = 16 rows，Phase C 自博弈**必须**用这个设计）
    cpu  :  2,386 steps/s
    cuda :  1,881 steps/s        -> cpu 快 27%
```

**结论**：两套设计下 **CPU 均更快**。评审的独立测量（CPU 快 ~19%）方向一致。
plan §6.3 的 `device='cpu'` **选择正确**（虽然论证方式该改成引用本表）。

> **方法论教训（重要）**：这是本项目"并发测量污染"的首次出现——
> 我最初那次反直觉的 cuda 优势来自**未隔离的并发负载**。
> 已在 F8 记为流程教训：**任何性能测量前须确认机器空闲**，否则数字不可信。

## F8（**流程教训，新增**）性能测量必须串行隔离

承接 F6：本轮的 device 结论曾因**并发训练**而反转。建议在
`DATA_MANAGEMENT.md` 的测量规范里补一条：**性能基准测试必须在无其他
训练进程时运行，并在 RUNLOG 中记录"机器空闲"状态**，否则数字不可比。

## F9（**决定性发现**）完整自博弈训练**失败**，冻结一方**成功**

这是本轮最重的结果——**探针问题已在数小时内被实测回答，无需 3 天**。

### F9.1 完整联合自博弈（plan §6.1 原设计）——**失败**

按 plan 的配置（8 worlds=16 rows、共享参数、零和、`n_steps=256`）
训练 **1,280,000 步（312.5 次更新，满足铁律 4）**：

```
      steps   mean_ret  ~collision%  entropy_loss   value_loss
      1,728     -740.0        74.0%
      2,928      -10.0         1.0%     ← 早期确有改善
      4,128        0.0         0.0%
      ...
  1,247,552     -880.0        88.0%     -3.369        1.97e+04
  1,254,000    -1000.0       100.0%     -3.370        1.88e+04
  1,259,824     -880.0        88.0%     -3.371        8.14e+03
```

**碰撞率在 4k 步时降到 0%，随后持续反弹，最终钉死在 88–100%**；
平均回报钉在 −880 至 −1000；熵**不降反升**（2.84 → 3.37 nats）。

**P1/P2/P3 判定**：P1 熵判据**失败**（且如 B2 所述该判据本身设计有误）；
P2 对随机对手 (32,2,6)、对 do-nothing (20,20,0)、对自身快照 (20,20,0)
——**判据全部饱和/无区分度**；P3 通过（std 0.17）但无意义。

### F9.2 冻结一方（roadmap 自己的"联赛退化档"）——**成功**

按评审建议的最廉价证伪：**冻结一方，只训另一方**
（200 次更新 / 409,600 步，对手 = 20 次更新后的快照）：

```
  episodes  mean_return  collision%
        51       -500.1      100.0%
     30432          0.3        0.0%
     98512         -0.3        0.0%
    166592        -60.4       12.0%
    268712        -60.4       12.0%
    404872       -120.8       24.0%
```

**碰撞率 100% → 24%（多数时段为 0%）**，平均回报 −500 → 近 0。
**学习确实发生了。**

### F9.3 结论与对 plan 的裁决

| 问题 | 实测答案 |
|---|---|
| 机制能否接通？ | **能**（F3：共享参数、features_dim=6、循环不崩） |
| 完整自博弈能否收敛？ | **不能**（F9.1：碰撞率钉死 88–100%） |
| 障碍是"机制"还是"非平稳性"？ | **非平稳性**（F9.2：冻结一方即学会） |

**因此对 plan 的裁决**：
1. **不要按 §6.1 跑完整联合自博弈探针**——已在数小时内实测证明它不收敛，
   3 天预算花在这里是浪费。
2. **直接采用冻结一方的"联赛模式"作为探针主路径**（roadmap 已列此预案，
   F9.2 证明它有效）。
3. 完整自博弈的收敛问题**降级为 W6–W9 的研究内容**（非平稳性缓解：
   对手池、快照历史、混合任务/对抗模式），而非探针门的通过条件。
4. **附带好消息**：F9.2 的"冻结一方"路径**不需要**自定义 VecEnv、
   不需要镜像观测、不需要同生共死——**可复用 `train_ot.py` 脚手架**
   （标准 SB3 单智能体任务），工程成本远低于 plan §6.1 的四件产物。

## F7（确认）§5 铁律 4 算术

实测确认：

```
8 worlds (16 rows),   100,000 steps ->   24.4 updates  VIOLATES rule 4
8 worlds (16 rows), 1,280,000 steps ->  312.5 updates  OK
4 worlds ( 8 rows),   614,400 steps ->  300.0 updates  OK
```

plan §5 的算术与建议正确。配合 F2（4 分钟成本）、F6（cuda 更快），
**没有理由为省算力而破铁律 4** —— 建议维持 1.28M 步。

---

## 对 §待评审确认点 6 的自答

1. **IPPO 代替 MAPPO**：可接受，探针验的是机制；但须在 plan 里**显式登记**
   "本探针不验证集中式 critic"，W6 若要 MAPPO 需另评。（见评审意见待并）
2. **100k → 1.28M**：同意提升（F7 算术 + F2/F6 成本极低）。
3. **镜像方案**：正确（F1 实测）。
4. **P1–P3 阈值**：**P2 必须重设计**（F5）；P1 的"策略熵"可执行性待评审核实
   SB3 日志字段；P3 阈值 0.05 仍属拍脑袋，建议改为"对同一观测批动作非恒定"
   的定性判定 + 记录标准差作参考。
5. **最小改动差异清单**：**不完整**——遗漏了 F5 的**起跑位/领先方随机化**与
   **obs 适配层**两条（它们既是对手接口，也影响博弈语义）。
6. **更廉价的证伪方式**：**已给出并已跑通**——本文件 F1/F3/F4/F5 所依据的
   pilot gate（`/tmp/_pilot_gate.py`）在 **~2 分钟**里验证了
   "机制能否接通"（features_dim=6、共享参数、训练循环不崩），
   并顺带抓出 F4/F5 两个判据级错误。**"能不能做"这个问题今天就能答**，
   3 天预算应全部留给"能不能收敛"。
