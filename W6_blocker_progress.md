# 阶段记录 — W6 封堵者判据 v5（进行中，**未完成**）

日期：2026-09-21
性质：按裁决（选 1：脚本封堵者作对手）实现的第 1 天工作记录。
状态：**封堵者已实现并验证有效；反制脚本（正例）尚未成功**。
时间盒：总计 2 天；**本文件记录已用 ~1 天后的状态**。

---

## 1. 裁决要点（复用）

- **M1 对手 = 脚本封堵者**（非训练对手）——避免"用产品验证产线"的循环论证；
- 正例必须是**无法用几何替代**的**时机型反制脚本**（利用换道冷却的承诺窗口）；
- 若反制脚本赢不了封堵者 → **调封堵者参数**，而非调判据；
- **2 天限时**；打不穿则启动**逃生舱**（降级为"封堵基准 + 课程阶梯"）。

---

## 2. 已完成

### 2.1 封堵者实现（`_blocker_v2.py`）

```
规则（纯几何，但占据争夺中的车道）：
  follower 在后方 [guard_lo, guard_hi] 带内  ->  封堵者占据【内圈】
  否则                                      ->  回【外圈】
巡航速度 cruise_v 与 follower 竞争力相当（可扫参）
```

### 2.2 封堵者**有效**：几何族被击穿 ✅（裁决的核心论据成立）

对封堵者（cruise 0.45–0.70、guard_hi 0.90）实测：

| 策略 | 无封堵者（记录在案） | **有封堵者** |
|---|---|---|
| zero-action | 0.00 | 0.00 |
| **geom 2-line** | **1.00** | **0.00** |
| **geom_dive_early** | **1.00** | **0.00** |
| **geom_smart** | **1.00** | **0.00** |
| rule machine | 0.00 | 0.00 |
| ckpt (+) | 1.00 | 0.00 |

**关键**：**两行几何策略从 1.00 掉到 0.00** ——
**封堵者确实让"几何抢线"不再免费**，正是裁决所预期的效果。

---

## 3. 未完成 / 阻塞点

### 3.1 反制脚本（正例）**尚未成功**（当前 complete=0.00）

按裁决实现的时机型反制（读封堵者的 lane + cooldown，抢 1.5 s 承诺窗口），
在 cruise 0.45–0.70 × guard_hi 0.70–1.20 的 9 组参数下**均为 complete=0.00**。

### 3.2 **根因已定位：我的世界把换道瞬态建模成了"瞬间跳跃"**

逐步轨迹显示：碰撞发生在
```
lane_f=0, lane_b=1   （两车在不同车道）
switch_timer=[0.56, 0.54]  （★ 两车【同时】处于换道瞬态）
```
即**两车同时变道、路径在瞬态中交叉**时，2D 距离低于碰撞半径 0.12。

**冻结 env 对此有明确处置**：`overtake_env.py:29` 注释
> `OFFTRACK = 0.4   # soft cliff: lane sep is 0.15, **switch transients peak ~0.3**`

即冻结 env **知道换道横向瞬态会到 ~0.3 m**，并据此设了宽容的 OFFTRACK；
而**我的世界把 lane 当作离散路径瞬时切换**，没有横向过渡过程，
于是两车同瞬态时**几何上重合** → 误判碰撞。

**这不是任务性质，是我世界的建模缺口。**

---

## 4. 剩余工作与时间盒判断

| 项 | 估计 |
|---|---|
| 给 WorldV2/Blocker 加**横向换道瞬态**（0→0.15 m 的渐变，~0.3 peaking） | 0.5 天 |
| 重跑反制脚本，扫封堵者参数直至其获胜 | 0.5 天 |
| 加"同瞬态"碰撞宽容（对齐冻结 env 的 OFFTRACK 口径） | 含上 |

**时间盒状态**：已用 ~1 天；**剩余 ~1 天**。
**未触及逃生舱条件**——逃生舱的触发是"**封堵者打不穿几何**"或
"**反制脚本赢不了**"，而当前的失败已定位为**可修的建模缺口**，不是任务不可解。
故按裁决"调封堵者/世界参数，而非调判据"，继续修复。

---

## 5. 诚实记录：本轮我犯的错误

1. **封堵者 v1 逻辑错**：用 `gap_forward` 判"接近"并切内圈，
   却与 follower **同向进内圈**（`l0=l1=1`），**没有形成任何拒止**；
   且 follower 在 V2V 先验下 `a=0` 即与封堵者同速，
   表现为"永跟在后面"（delta 钉在 −gap）——是"未尝试"，不是"尝试失败"。
2. **反制脚本首版**未考虑 `switch_timer` 同瞬态问题，因此 9 组参数全 0。
3. **两次把模块放错位置**：`_blocker_world.py`/`_counter.py`/`_blocker_v2.py`
   先只写到 `/tmp`，被 import 时报 `ModuleNotFoundError`
   （**F19 的同一类问题，已第二次**）。

---

## 6. 下一步（继续按 v3）

1. 修世界的换道瞬态（横向渐变 + 同瞬态宽容），**对齐冻结 env 的 OFFTRACK 口径**；
2. 重跑反制脚本；若获胜 → 正例成立 → 写 plan v3（含常驻几何退化族、R4 纪律）；
3. 若修完仍赢不了 → 按时间盒启动逃生舱，并如实记录"封堵者过强"。

**未提交**：本文件与 `_blocker*.py`/`_counter.py` 将随后续 commit 一并入库。


---

## 7. Day 2 进展：反制脚本 v2 + 封堵者强度扫描

### 7.1 修好反制 v1 的两个 bug

1. **同瞬态对撞**：v1 在封堵者**换道瞬态中**（`switch_timer > 0`）也发切道指令，
   两车同时横移、2D 距离跌破 0.12 —— 是**自己撞的**，不是封堵者的功劳。
   修法：只在封堵者**已完成换道且冷却剩余充足**时才进攻。
2. **进攻窗口判断错**：v1 只要冷却>0 就切，而正确窗口是
   **封堵者已定居外圈 + 冷却剩余 > 0.6 s**（它来不及再回内圈拒止）。

### 7.2 修好后的**关键结果**：出现稳定僵持（standoff）

修好后碰撞降到 0%，但完成率也是 0.00 —— 轨迹显示：

```
  t=7.0  gap=1.42  l0=0 l1=1  cd_b=0.00   ← 封堵者守内圈
  t=8.0  gap=1.62  v0=0.95    l1=0 cd_b=1.12 ← 封堵者回外圈（冷却开始）
  t=8.5  gap=1.33  v0=0.95    cd_b=0.62
  t=9.0  gap=1.24  v0=0.55    cd_b=0.12
  t=9.5  gap=1.20  l1=1 cd_b=1.12        ← 封堵者【重新守内圈】
  （循环）
```

**follower 能加速（0.95）也能逼近，但始终进不了防守带；
封堵者总能在冷却结束前重新占住内圈 ⇒ 稳定僵持，谁也无法强行突破。**

### 7.3 封堵者强度扫描（回答"是封堵者太强，还是反制太怂"）

| cruise | guard_lo | guard_hi | 完成率 | 碰撞 |
|---|---|---|---|---|
| 0.40 | 0.08 | 0.35 | 0.00 | 0% |
| 0.40 | 0.08 | 0.50 | 0.00 | 30% |
| 0.40 | 0.08 | 0.90 | 0.00 | 0% |
| **0.40** | **0.45** | **0.70** | **0.30** | **0%** |
| 0.55 | 0.08 | 0.35 | 0.00 | 0% |
| 0.55 | 0.08 | 0.50 | 0.00 | 30% |
| 0.55 | 0.30 | 0.60 | 0.00 | 70% |
| 0.55 | 0.45 | 0.70 | 0.00 | 5% |

**结论**：
- **任务可解**（窄防守带 0.45–0.70 时反制完成率 0.30），**不是无解**；
- 但**当前封堵者参数过强**：常识性的防守带（如 0.08–0.90）导致**永久僵持**；
- 按裁决"**调封堵者参数，而非调判据**"，需把防守带收窄到任务可解区间。

### 7.4 时间盒状态（**如实**）

- 已用 **~2 天**（裁决给 2 天）；
- **未达成**"反制脚本稳定获胜"（最佳 0.30，且仅在一个窄参数点）；
- **未触发**逃生舱的**根本**条件（任务并非不可解），
  但**时间盒已到**。

**下一步须由人裁决**（见 §8），不宜由我自行延长或改判据。
