# Phase F — 对抗干扰下的安全跟随（plan **v1.1**，评审通过）

日期：2026-09-25（v1.1）；2026-09-24（v1）
性质：**重启 RQ3 的新表述**（原"对手学习化"→"对抗干扰下的安全跟随"）。
上游：owner 裁决（2026-09-24 提案 + 2026-09-25 六项裁决与两项修订）。

> **v1.1 变更摘要（评审后）**：
> - §3 H-A1' 定稿（双防御方即门，非平凡性检验降为**报告性诊断**）——Q1
> - §4.5 **B 组参数化定义落盘**，A 组定义一并钉死——Q2
> - §1.4 升级为 **F24 独立发现**，报告单列一节——Q3
> - §5 Step 0.5 **重定义为"预演正赛"**（非随机幅值冒烟）——Q4
> - 命名 **Phase F**，Phase C 保持关闭原样——Q5
> - §4 正例获批准（2 天盒 + 附加验收 + 正例本身走评审）——Q6
> - **A-1**：新增 **Step 3.5 包络重测绘**（新防御方须重跑 11 格扫描）
> - **A-2**：headline 数字走 **30 seed**，边际结果扩 50
关联：`ERRATUM3_criterion_v234.md`（F14/F20）、`plan_phaseC_m1.md`（可复用的防泄漏纪律）、
      `DATA_MANAGEMENT.md` §10.4/10.5b/10.5c（判据纪律）、`docs/scenario_scope_statement.md`（速度上界）

---

## 0. 为什么这条路可能成立（owner 的论证，我的复核）

owner 的判断：自博弈死的三层墙（对称性 F14 / 几何占优 F20 / 循环论证），
在"撞车对手"下**逐一被拆**：

| 原墙 | 撞车对手为何拆得掉 | 我的复核 |
|---|---|---|
| F14 对称性：位置类指标无法区分水平 | 双方目标不同（一方求安全通过，一方求制造碰撞）⇒ "谁赢"不再是发车位置的函数 | ✅ 成立（目标非对称） |
| F20 几何占优：抢内圈即可满分 | 主动对手会**预判并拦截**抄近路 | ⚠️ **部分成立，见 §1.2** |
| 循环论证：聪明对手是训练产物 | 攻击者的目标"撞上没有"**天然可数**，且可用**已冻结的** v1/P+FF 当靶子验收 | ✅ 成立 |

---

## 1. 出计划前的实测（**三项发现改变了设计**）

**在写 plan 之前先验了前提**——沿用 `V_MAX` 前提与 ISO 引用两次的教训。
脚本落盘 `results/20260924_phaseF_probe/`（6 个），以下全部实测。

### 1.1 环境无法直接支持"会追的对手"（**需新 env 模块**）

`overtake_env.py` 与 `follow_env.py` 的 leader 均为**硬编码脚本**：
- overtake：`self.leader.step(..., self.outer)` —— **钉死在外圈**，无法横移拦截；
- follow：`_leader_v()` 由 `(v_set, behavior)` 解析生成。

两车道几何**允许**碰撞（中线距 0.15 m，`COLLISION_2D`=0.12 m，仅需 0.03 m 相向横移，
占 off-track 预算 7.5%），但**冻结 env 堵死了两个必要条件**：leader 不能横移、
跟随方观测**无对手横向槽位**（`[gap_ref, delta, v_self, e_lat, lane_self, v_leader]`）。

⇒ **不修改冻结文件**：新建 `adversary_env.py`，**继承** `FollowEnv` 只覆写 `_leader_v()`。
**Step 0 闸门已通过**：leader 指令范围 0.050–0.780、169 个不同值、
`follow_env.py` sha256 **前后一致**（`941c447b969c2fa1`）。铁律 2 未触碰。

### 1.2 ★ 高速发散不适用于幅值轴（需实测澄清）

D0 的结论是"速度**上界**超过 ~0.5 m/s 训练不稳定"。攻击者制造的是**速度幅值的大变化**
（±0.45 m/s 摆幅），与 D0 的"分布整体上移"**不是同一个轴**。
本方向在 0.25–1.0 m/s **幅值摆幅**内的可训性**未测**，须列 Step 0.5 闸门（§5）。
**不得**据 D0 预判本方向也不可训——两个轴不同。

### 1.3 ★★ 正例存在，但**是平凡的**——H-A1 近乎空判据

**实测（`_probe_positive_pole.py`，20 局/格，名义参数）**：

| leader 脚本 | vs P+FF（规则） | vs v1 RL |
|---|---|---|
| **step-down 0.1 s**（方波 0.95↔0.05） | **20/20** | 0/20 |
| **step-down 0.2 s** | **20/20** | 0/20 |
| **step-down 0.4 s** | **20/20** | 2/20 |
| step-down 0.8 s | 0/20 | 12/20 |
| osc f=0.5 Hz（0.5±0.45 正弦） | 0/20 | **20/20** |
| sawtooth / crawl / cruise-then-stop | 0/20 | 0/20 |

**两条结论**：

1. **H-A1（攻击者对规则防御方 ≥60%）会被"一行方波"以 100% 通过** ⇒
   **该假设近乎空判据**：一个学出来的攻击者只要重新发现这个平凡脚本就能通过。
   这正是 **F20 的同型复发**（两行脚本击穿判据）。**H-A1 必须改写**（§3）。
2. **两个冻结防御方各有致命弱点，且弱点不同**：

   | 防御方 | 死于 | 不死于 |
   |---|---|---|
   | P+FF（规则） | step-down 方波（20/20） | osc f=0.5（0/20） |
   | v1 RL | osc f=0.5（20/20） | step-down 方波（0/20） |

   ⇒ **不存在"天然鲁棒的基线"**。这对实验是**利好**（两边都有提升空间），
   但**H-A2 的增益必须分防御方各自测**，不能合并成一个数。

### 1.4 ★ **F24（正式命名）**：v1 RL 存在训练频带之外的**频域盲区**

v1 RL 在 `osc f=0.5 Hz` 上 **20/20 撞车**，且**不是"退避保平安"**——
settled 误差 **40.35 obs-cm**、mean gap 0.384 m（目标 0.20 m），即**既撞又跟丢**。
对照名义工况（误差 2.28 obs-cm、gap 0.211 m），差 17 倍。

> ⚠ 该 `f=0.5 Hz` / `amp 0.45` **超出训练分布**（训练 `freq∈[0.05,0.15]`、`amp∈[0.2,0.4]`）
> ⇒ 属**分布外**攻击。但意味着"对抗训练"可能退化为"把训练分布加宽"——
> **那正是 D0 撞墙的轴**。**这是本方向最大的失败模式**，见 §5-R3。

**F24 正式表述（owner 裁决 2026-09-25，Q3）**：

> **v1 RL 存在训练频带之外的频域盲区**——0.5 Hz 正弦扰动下 settled 误差
> **40.35 obs-cm vs 名义 2.28（17×）**，且 20/20 撞车；
> **其脆弱性谱与 P+FF 互补**（P+FF 死于**阶跃**、RL 死于 **OOD 频率**）。

**处置**：
- **无需 erratum**——论文数字本就在训练带内、已加限定词（D0 的贡献）；
- **报告单列一节**：与 W1（执行器退化鲁棒性）同主题的**第二块拼图**——
  即"鲁棒性沿不同轴有不同边界"；
- **F24 同时是 Phase F 最强的动机证据**（对手能找到人找不到的盲区）。

---

## 2. 设计

### 2.1 三方结构（**非对称，这是拆 F14 的关键**）

```
攻击者 (leader) : 学出来的策略，动作 = 领车速度指令 ∈ [0.05, 1.0]
                  奖励    = 跟随方跟踪误差（密集） + 碰撞奖励（稀疏）
防御方 (follower): ① 冻结 v1 PPO  ② 冻结 P+FF  ③ 对抗训练后的新策略
                  任务    = 保持 gap = D_DES 且零碰撞（原跟车任务）
```

**攻击者奖励**（与代码库 PBRS 惯例一致）：
`r_att = |e_follower| 的归一化值 + 100·1[collision] - 0.01`（末项防原地不动）。

> ⚠ **实现注意（owner 指出的评审盯防点）**：**跟车误差越大，攻击者奖励越"高"**——
> 但注意**符号约定**：本代码库的误差表示为 `e = gap - D_DES`，
> 而 reward 里以**惩罚**形式出现（`-(e/0.12)**2`）。
> 攻击者的奖励是**负的惩罚的相反数**，即**误差绝对值越大越奖励**。
> **该符号在代码中必须显式注释**，防实现时把攻击者写成一个"帮助跟车"的控制器
> （那会让 H-A1' 变成不可能通过的假门）。

**不由攻击者自己定义"赢"**——它的目标是**可数的物理事件**（碰撞），
这正是 owner 论证中"敌人越简单直接，验收越容易"的落点。

### 2.2 防泄漏（沿用 `plan_phaseC_m1.md` §1.2 三条硬约束）

1. **评估对手 = 训练对手**（同一实例同一参数，无 train/eval gap）；
2. **观测不含特权信息**：跟随方只能见**物理可观测**量（对方的车道/横向位置可进观测；
   对方的**内部状态**（如攻击者网络的隐状态）不可）；
3. **不得读取攻击者的动作输出**当作输入。

### 2.3 判据：**held-out 泛化**（防平凡解，防"只打了补丁"）

**核心设计**：攻击族分 A / B 两组。

```
A（训练侧） := 冻结的学习攻击者（单一实例，参数随 Step 1.5 冻结存档）
B（held-out）:= 评审侧构造的脚本攻击族，**训练代码永不 import**
```

**措辞钉死（Q2）**：**防御方在 A（冻结的学习攻击者）上训练**；
攻击者本身的训练是 Step 1（对两个冻结防御方）。
v1 原文"攻击者在 A 上训练"**已作废**——A 指的是防御方的训练对手。

**主判据 = 防御方在 B 组（held-out）的撞车率相对未训练基线显著下降。**
若只有 A 组下降、B 组不动 ⇒ 学到的是补丁，**不是鲁棒性** ⇒ 如实判负。

### 2.3.1 判据口径的两个陷阱（**review1 重要项**）

**(i) d=1.0 档存在「执行器受限死格」——那是对领车执行器的度量，不是防御方性能。**

实测（review1 C）：`d=1.0 / step-down 0.2s` 的 settled err **78.28 obs-cm**
（20 局均值口径；单局 seed 2000 为 78.65，review2 已复现二者皆真，非矛盾）
**不是**防御方"没在跟"，也**不是**它的失败——**P+FF 给出同值**。
真因：领车自己的执行器跟不上 0.2 s 方波（指令均值 0.501，**实际**速度均值仅 0.240），
跟随车 `v_cmd` 有 **50% 的步**被 clip 在 0 ⇒ **gap 永久停在 0.607，任何策略都到不了 1.0**。

⇒ **该格的「0 碰撞」是空信用**（零动作同样 0/20）：它度量的是**领车执行器**。
**`d=1.0` 档的 step-down 三格（40.41 / 78.28 / 22.71）同病。**
**评估脚本须对每格同时报告领车【实际】速度均值/峰峰**，或改用 `gap_actual` 作基准；
至少须标注该列**不可解释为防御方性能**。

**(ii) 正例对 B 组的零碰撞是【同分布确认】，不是【泛化证据】。**

`holdout_B.md` 的 **B2 频率 {0.3, 0.8} Hz 正是 `positive_pole.py` 自己的验证频率**；
B3 的 chirp 频带、B1 的周期亦然。（不违反 §2.4——B 组的 held-out 是相对 **Step 2 训练代码**，
正例不是训练代码——但两件事不是独立证据。）
⇒ **正例只锚「A 组风格」攻击**；报告措辞须据此限定。

**(iv) ★ 评估脚本必须逐回合 `reset()` 有状态的策略（本轮实测的新陷阱）。**

正例**是有状态的**（`prev_v` 跨步保留）。若评估循环**不调用 `policy.reset()`**，
每个回合边界会喂给它一个**虚假的 `dv`**，实测可把结果从 **0/20 翻成 20/20**——
即同一个策略、同一个攻击，仅因 harness 少一行而复现相反结论。
（作者在复核 review2 F2 时**自己踩了这个坑**：ad-hoc 探针缺 reset，一度得出
与评审相反的读数；经隔离确认后，评审的 0/60 成立。）
⇒ **Step 3 的评估脚本必须 `if hasattr(policy,'reset'): policy.reset()`**，
且**冻结的 v1/P+FF 虽无状态，也须保持同一 harness**，避免口径分叉。

**(iii) 正例常量是在被验收的探针集上网格搜出的 ⇒ 归档表不自证。**
缓解证据（review1 E）：D2 网格 **12 组全部 0/140**（平台非尖峰）+ 本轮全部样本外测试通过
（1500 随机波形 0 崩、6 个家族外攻击类 0/20、B 组 0/100）。
**⇒ 引用顺序应改为：先给样本外证据，`_out_pole_final.txt` 只作回归基线。**

---

### 2.4 B 组定义（**评审侧构造，2026-09-25 落盘**，Q2）

> 落盘位置：`reviews/20260925_phaseF_holdout_B.md`。
> **训练代码绝不 import 该定义**；仅在 Step 3 评估时由评估脚本读取。

```
B 组（held-out 脚本攻击族，20 局/项，seed 50000+）：
  B1  方波：周期 ∈ {0.3, 0.7} s，占空比 ∈ [0.3, 0.7]
  B2  正弦：f ∈ {0.3, 0.8} Hz，幅值 ∈ {0.3, 0.5}，随机相位
  B3  chirp：频率 0.1→0.6 Hz 线性扫频
  B4  随机保持：段长 ∈ [0.2, 0.8] s 的分段常值，值域 [0.05, 1.0]
  B5  双脉冲：间隔 ∈ {0.2, 0.5} s 的两个窄脉冲
```

**与 A 的区别**：A 是**学习**出来的单一攻击者；B 是**脚本**族且**覆盖更宽的频率与波形**。
故 B 同时检验"泛化到未见波形"与"泛化到未见频段"（后者直面 R3/F24）。

---

## 3. 假设与阈值（**训练前定死**）

| # | 假设 | 阈值 | 说明 |
|---|---|---|---|
| **H-A1'** | 攻击者相对**两个**冻结防御方**都能**达到高撞车率 | **对 P+FF ≥60% 且 对 v1 ≥60%** | **v1.1 定稿（Q1）**——因一行方波即可满足原版（§1.3），**双防御方**才排除"只会打一个"的平凡解 |
| **H-A2** | 对抗训练产生鲁棒性增益 | 对**冻结攻击者**撞车率：训练后方 ≤ **1/3 未训练方**，且 ≤ **10%** | owner 原阈值保留；**分防御方各自报** |
| **H-A3** | 增益**泛化到 held-out 攻击族** | B 组撞车率相对未训练方**下降 ≥50%** | **新增——本 plan 的核心**。仅 A 组下降不算 |
| **H-A4** | 不牺牲名义性能 | 名义工况 settled 误差 ≤ 基线 **1.5×** | 防"为鲁棒牺牲精度" |

**训练前登记进 `research/ASSUMPTIONS.md`**；落边界补 seed，**不放宽阈值**。

> **Q1 裁决（owner 2026-09-25）：双防御方即为门，不再加硬性非平凡性检验。**
> 依据：**已知平凡族中没有任何一个能同时打穿两个防御方**
> （方波对 v1 为 0/20；0.5 Hz 对 P+FF 为 0/20，§1.3）。
> 攻击者必须找到"两者通吃"的策略，**这超出全部已知平凡族** ⇒ 判据非空。
>
> **但附一份报告性诊断（不设门）**：随判定公示攻击者策略与
> **各已知平凡族的轨迹相似度**。若不相似度异常接近某个已知族，
> 即使过了门也须在报告中显式说明。**这是知情披露，不是通过条件。**

> **A-2（owner 修订）：所有 headline 数字（H-A2 / H-A3）走 30 seed**，
> 落在阈值边界的结果扩到 50 seed 确认。**沿用老规矩，不放宽阈值。**

#### H-A3' 五条合取（**review1 R1，拦截级改写**）

**为何改写（实测）**：H-A3 原形式「B 组撞车率相对未训练方下降 ≥50%」有三个缺陷叠加，
最致命的是**零动作在 B 组拿到 0/100**（与正例同值）——**撞车率这一列与目标脱钩**
（`DATA_MANAGEMENT.md` §10.5c 同型：判据可被"什么都不做"满分通过）。
V2V 速度匹配先验使 `a ≡ 0` 天然保距。

```
H-A3'（修订版，五条合取，全部须满足）：
  (a) 命名基线：baseline := 该防御方血缘在对抗训练之前的同一条策略。
      A-2 的 30 seed 须用在【成对】攻击实例上（B 组同一批实例、同一 seed，
      训练前后各跑一次），而非"基线 20 局 + 训练后另 20 局"。
  (b) 绝对上限：B 组撞车率 ≤ 10%（不依赖基线）。
  (c) 相对下降 ≥50% vs (a) 的命名基线，且【同时公示基线的 seed 波动带】
      （实测：同一 v1 仅换攻击种子块即 36/100 → 40/100）。
  (d) 【必列零动作对照行】（§10.4/§10.5c 硬性要求）：B 组评估表逐项列出
      zero-action 的撞车率【与跟踪误差】。若训练后的防御方在撞车率上
      不优于零动作（zero-action 已触顶 0/100），该项无分辨力，不得据其判定。
  (e′) 【跟踪质量门，**review2 F1 修正锚**】：B 组 settled 误差 ≤ 1.5 × 正例
      在【同一批 B 实例上】的值。**实测正例 B 组 pooled = 3.99 ⇒ 门 ≤ 5.98。**
      ⚠ 原写「正例 1.5×（2.19 ⇒ 3.28）」是**锚错了量**——分子是 B 组量、
      分母代入的是正例的**名义**量。按字面 3.28 时**正例五项里四项不过自己的门**
      （B1 6.17 / B3 3.72 / B5 3.55 均 > 3.28）；按意图 5.98 时又不能分辨（见 (f)）。
  (f) 【硬族条款，**review2 F2 新增，承重**】：把 `step-down 0.1/0.2/0.4 s`
      补入 B 组（或把 (b)(c)(e′) 的判定域改为 **B ∪ 硬族**）。
```

> **零动作实测（review1 R1，独立复核一致）**：B 组五项 0/100、名义/扰动误差 83–110 obs-cm。

#### 为何还要 (f)：**修好撞车率列之后，过门者换成了 P+FF**（review2 F2）

review1 指出「零动作能过撞车率列」。review2 实测显示**上移了一层**：
把撞车率与跟踪质量都设在 B 组上之后，**P+FF 通过全部条款**——而 P+FF
是一条**没有任何抗扰逻辑、且已被证否**的固定规则（在硬族上 **60/60 崩溃**）。

**根因（实测）**：**B 组不含正例与 P+FF 的唯一分离族**。

| | step-down 0.1/0.2/0.4 s（硬族） | B 组（B1–B5） |
|---|---|---|
| 正例 | **0/60** | 撞车 0/100，误差 3.99 |
| P+FF | **60/60 崩** | 撞车 3/100，误差 4.23 |

⇒ 两者在 B 组**打平**；正例的全部可分辨性都落在 B 组之外的硬族上。
**缺 (f)，(b)(c)(e′) 拦不住 P+FF**——这是 §10.5c 的同型复发（判据与目标脱钩）。

**另**：**(c) 在本口径下对一切候选都打 True**（基线 36/100 把门槛降到 ≤18/100，
连零动作 0/100 都满足）；**(b) 对一切"不撞"的策略无分辨力**。二者须与 (f) 合用。

**基线波动带更正（review2 E）**：实测三个攻击种子块下 v1 为 **34 / 36 / 40**，
换 env-seed 约定又得 **41 / 42** ⇒ 实为 **34–42**，原公示「36→40」偏窄，
**「±2 点」应改为「±4–8 点」**。

---

## 4. 退化反例表（§10.4 / 10.5b / 10.5c，**开训第一天常驻**）

| 退化策略 | 预判 | 性质 |
|---|---|---|
| 零动作（跟随方不动） | 被撞 | 被动退化 |
| 全油门 | 自己追尾 | 被动退化 |
| 恒低速（远离领车） | 不撞但任务失败 | **"保平安"型**——必须被 A3/A4 排除 |
| 随机动作（跟随方） | 被撞 | 随机扰动类（10.5c） |
| 随机动作（攻击者） | 撞车率低 | 随机扰动类（10.5c） |
| **step-down 方波**（§1.3 的平凡攻击） | 对 P+FF 100% | **几何/平凡族**（10.5b）——防"判据被一行脚本击穿" |
| osc f=0.5（§1.4） | 对 v1 100% | 平凡族 |
| **正例：手工跟随脚本**（P+FF + 达成收距率包络全权刹车；**非**"预判"，见 §3 更正） | 应零碰撞通过 | **阈值锚点，须先做出**（owner 硬性前置） |

> **正例优先（Q6 已批准，2 天盒）**：`plan_phaseC_m1.md` 的教训是"缺正例 ⇒ 判据无锚"。
> 手工跟随策略已建成（`results/20260925_phaseF_positive/positive_pole.py`）。
>
> **⚠ 机制陈述更正（review1 R2，拦截级）**：原称"**预判**-避让"——
> **"预判"那一半是死代码**。`YTHRESH=0.20` 而单步速度最大变化量
> `a_max·DT ≤ 0.04 m/s`（差 5–10 倍）⇒ **物理不可达**，实测跨 11 族 × 3 间距**触发 0 次**；
> 命令夹持支路也**从不生效**（名义 0/1001 步）。
> **准确表述**：**"P+FF + 一条按【达成】收距率与间隙相关包络比较就全力刹车"的规则**
> （触发率 2.4–8.1%）。`ythresh/yg/ymax/recover` 四个常量是**无效参数**。
> **消融定论**：`clamp only`（只夹持命令）**= 60/220 崩**；`brake only`（只留全权刹车）**= 0/220 全过**。
> ⇒ 承重的是「对**已达成量**的全权刹车」，不是「预判」。
>
> **附加验收（owner 加）**：
> 1. 正例必须对**两个**探针攻击（`step-down 0.1s` 与 `osc 0.5Hz`）**都零碰撞通过**；
> 2. **不能被"恒低速远离"退化冒充**——**H-A4 同样套在正例上**（名义性能门）；
> 3. **正例本身走一遍评审**——它是阈值锚点，**锚点必须先被审过**（Phase C 教训）。

---

## 5. 风险

| # | 风险 | 概率 | 预案 |
|---|---|---|---|
| **R1** | **攻击者只学到一个平凡解**（§1.3 的方波），H-A1' 仍被平凡通过 | 中 | H-A1' 已改为**双防御方**；再不行则改判据为"攻击者**不能**是已知平凡族的成员"（轨迹相似度检验） |
| **R2** | 防御方训练**发散** | 中 | Step 0.5 闸门（下）；若发散，**如实报为"对抗训练在本平台不可行"**，与 D0 上界并列 |
| **R3** | ★ **对抗训练 = 变相加宽速度分布 ⇒ 撞上 D0 的上界** | **高** | **本方向最大失败模式**。Step 0.5（v1.1 重定义，见下）**直接预演正赛工况**；若不可训，落在 R2 的如实报告。**F24 使此风险具体化**：攻击者已在 OOD 频率上取胜，防御方要赢就必须覆盖那些频率 ⇒ 正是"加宽分布" |
| **R4** | held-out 攻击族设计被攻击者"猜到" | 中 | B 组构造**不写入训练脚本**；由评审独立构造（对齐 Phase C 的评审介入惯例） |
| R5 | 判据被"保平安型"退化通过（不撞但也不跟） | 中 | H-A4 名义性能门 + §4 的恒低速项 |

---

## 6. 时间盒与流程

| 阶段 | 内容 | 门禁 |
|---|---|---|
| **Step 0** | env 子类化 + 冻结校验 | ✅ **已通过**（§1.1） |
| **Step 0.5**（**v1.1 重定义，Q4**） | **预演正赛**：跟随方 vs **脚本随机波形攻击者**（A 族风格随机版）训 **1M 步** | **双门**：std 不发散 **且** 完成率合理 → 放行 Step 2 |
| **Step 1**（3 天） | **攻击者先行**：训练攻击者打两个冻结防御方 | **H-A1'**；不达标即停（**探针有干净通过标准**，非 W5 式模糊） |
| **Step 1.5** | 冻结攻击者；**评审独立构造 B 组** | B 组在训练脚本外 |
| **Step 2** | 训练防御方（对抗） | 铁律 4 更新次数 ≥300 |
| **Step 3** | 双组评估（A 见过 / B held-out）+ **30-seed 终评**（A-2） | **H-A2 / H-A3 / H-A4** |
| **Step 3.5**（**A-1 新增**） | ★ **包络重测绘**：对**对抗训练后的新防御方**重跑 D0 的 11 格扫描 | 新防御方的声明**按其自身包络**划定，**不得继承 v1 的包络** |
| **Step 4** | 图与审计表 | 五类终止原因全审计 |

> **A-1 为何必要（owner）**：对抗训练后的防御方是一个**新策略**，
> 它在 v ≤ 1.0 的运行**不能自动继承 D0 给 v1 画的包络**。
> 若不重测就沿用 v1 的包络声明，就是**把"未加限定词的声明"这个坑再踩一次**
> ——那正是 D0 存在的理由。**Step 3.5 的产物是一份新包络表**，
> 论文中"对抗训练后的策略"一节按新表加限定。

**总预算 ~2–3 周**（含 Step 1 的 3 天硬门）。

---

## 7. 执行顺序（**v1.1 定稿**）

```
plan v1.1（本文件）→ 存档
   ↓
Step 0.5（预演正赛，Q4）──┐
   ↓                        │ 并行
正例 2 天盒（Q6）──────────┘
   ↓
评审正例（锚点须先被审）
   ↓
Step 1  攻击者训练（3 天硬门，H-A1'）
   ↓
Step 1.5  攻击者冻结 + B 组落盘（reviews/，训练代码不 import）
   ↓
Step 2  防御方对抗训练
   ↓
Step 3 + 3.5  双组评估（30 seed）+ 包络重测绘
   ↓
判定会
```

**与 Phase C 的关键差别（owner）**：本次探针**有干净的通过标准**
（H-A1' 双 60%），**不会再出现"跑一周才知道行不行"**。

---

## 8. 送评审的待确认点（**v1.1 已全部裁定，存档**）

| # | 问题 | 裁定 |
|---|---|---|
| 1 | H-A1' 双防御方够不够排除平凡解 | ✅ 够（已知平凡族无一能同时打穿两者）；非平凡性检验降为报告性诊断 |
| 2 | B 组由谁构造 | ✅ 评审侧，参数化定义已落盘 `reviews/20260925_phaseF_holdout_B.md` |
| 3 | F24 是否独立发现 | ✅ 是，命名 **F24**，报告单列一节，无需 erratum |
| 4 | 是否有比 Step 0.5 更早的判据 | ✅ Step 0.5 即最早，但**重定义为预演正赛工况** |
| 5 | 命名 | ✅ **Phase F**（Phase C 保持关闭原样） |
| 6 | 正例由谁做 | ✅ agent，2 天盒，附加验收 + 正例本身走评审 |

**两项修订**：**A-1** Step 3.5 包络重测绘；**A-2** headline 走 30 seed（边际扩 50）。

---

## 9. 与既有结论的关系（**勿混淆**）

- D0 的**速度上界**（v > 0.5 训练不稳定）**不直接适用于**本方向（§1.2，幅值轴 ≠ 上移轴）；
- Phase C 的 **F14/F20 教训直接适用**（本 plan 的判据设计全部据此）；
- RQ2/RQ1 与电子赛基准结果**不受影响**（不碰冻结文件）。
