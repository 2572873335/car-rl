# Plan — Phase C W6: 联赛机制（v2，修订评审后）

日期：2026-09-21
对象：`results/20260921_phaseC_w6/`
关联：`plan_phaseC_w6.md`（v1）、`reviews/20260921_phaseC_w6_review1.md`（**Reject**）、
      `ERRATUM_phaseC_findings.md`、`research/ASSUMPTIONS.md`、`DATA_MANAGEMENT.md` §10

> **v2 性质**：v1 被评审 **Reject**（5 条拦截级）。本版按评审意见**重构为阶梯式**
> （评审 S1），并撤回 v1 中建立在**已被证伪依据**上的设计。
> 作者已**独立复现**评审的 4/5 条拦截级（见 `ERRATUM` §2）。

---

## 0. v1 被拒的核心原因（先记住教训）

**v1 的五项变更（对手池 + 70/30 混合 + 8 维观测 + 单方失败计分 + R1/R2/R3）
若一起上马，失败无法归因。** 且 v1 的两处立论依据是**我自己的适配器 bug**
造成的伪影（`ERRATUM` §1）。

**v2 的原则**：**一次只动一个变量**，每级单独判定；**判据必须先证明有区分度**
（用退化策略验证它落带外）才可使用。

---

## 1. 前置事实（**已实测，含本轮更正**）

| # | 事实 | 状态 |
|---|---|---|
| 1.1 | 冻结一方**能学习**：W5 100%→24%；V2V 复现 100%→0% | ✅ **跨基准可迁移** |
| 1.2 | 完整自博弈**定性**方向一致（先好后坏），但**"钉死 88–100%"不迁移**到 V2V（96% vs 26.8%） | ⚠️ 见 ERRATUM §2.1 |
| 1.3 | 冻结 checkpoint 在**原 env 任务模式** = **10/10、0 碰撞**（seeds 2000–2009） | ✅ 实测确认 |
| 1.4 | 该 checkpoint 是 **6 维**；8 维 env 会直接抛 `ValueError` | ✅ 实测 |
| 1.5 | 冻结 checkpoint **作对等对手表现差**（22/40 碰撞；终局领先 ≈ 中性基线） | ✅ 保留列 |
| ~~1.6~~ | ~~规则机不是博弈对手~~ | ❌ **已撤回**（适配器 bug，ERRATUM §1.1） |
| 1.7 | 碰撞**肇事者不可归因**（仅 2D 距离，无相对速度/航向） | ✅ 机制性 |
| 1.8 | 「胜率 ∈ 近平衡带」判据**饱和**：do-nothing 0.52、random 0.52 | ✅ 实测 |
| 1.9 | 探针世界**缺曲率限速**（冻结 env 有）；差异清单不止速度基准一项 | ✅ 实测 |

---

## 2. 阶梯式执行（**v2 核心改动**）

### M1 — 最小可行联赛（**本轮唯一目标**）

**只动一个变量**：把 W5 的"单个冻结快照对手"升级为**小型对手池**。
**保持**：冻结 6 维观测布局、冻结动作语义、无混合模式、无新增计分。

- **对手池**：容量 3；成员 = 训练过程中保存的快照；**淘汰最旧**；
  初始成员 = 20 次更新的热身快照；
- **动作语义**：**沿用 W5 的自身速度基准**（`v_cmd = v_self + 0.8a`）。
  **理由**：M1 要回答的是"对手池能否消除 W5 的振荡"，故**必须与 W5 可比**——
  这正是 v1 §3.1 统一到 V2V 所牺牲掉的东西。
  与冻结 env 的可比性**推迟到 M3**（届时用阳性对照解决，见 §4）；
- **观测**：保持 6 维（不加 `lane_other`）；
- **终止**：保持两车同生共死（W5 已实现），**不引入"单方判负"**
  （因 §1.7 不可归因）；
- **训练量**：≥300 更新（产出结论的训练，铁律 4 适用）；
  `8 worlds × 2 = 16 行`、`n_steps=256` ⇒ ≥1,228,800 步。

**M1 要回答的问题**：对手池是否使碰撞率**稳定下降**（而非 W5 的先好而后振荡）？

**M1 验收（**判据须先验有区分度**，见 §4）**：
- **绝对判据**（v1 缺失，评审 I6）：末段（后 20% 训练）对抗模式碰撞率 **≤ 10%**；
- **稳定性判据**（评审 I3，替代 v1 的桶均值相对比）：
  末段碰撞率的 **std ≤ 15%**（W5 的 joint 末段 std 达 18.7%，V2V 16%，均不稳）；
- **窗口口径定死**：一律**按步数窗口**（首/末 20% 步）计算，**不用桶均值**
  （评审实测：同数据两种口径裁决相反，113× vs 1.19×）；
- **保留 value_loss 子句**（评审 §五-5）：非 NaN 且末段不单调增长。

### M2 — 混合模式（**仅当 M1 通过**）

加 70% 任务 / 30% 对抗。**但**：
- **比例须给依据或做敏感性**（评审 I5）：至少跑 **80/20 与 50/50 两档**，
  或明写"比例为待调超参"；
- **灾难性遗忘判据**（v1 的 W4 分辨率不足，评审 I4）：
  任务模式评估用 **≥30 seed**（本项目 H6 规范：边界结论补 seed），
  **不用** 10 seed；
- 混合模式的**分模式指标**必须分开报告。

### M3 — 与冻结 env 对齐（**仅当 M2 通过**）

才考虑动作语义统一到 V2V + 观测扩充 + 曲率限速对齐。
**且必须先做 §4 的 2×2 阳性对照**，确认 env 等价性。

---

## 3. 判据设计（**本轮最重要的方法论要求**）

> ⚠️ **Step 0 执行结果（2026-09-21）：本节的 M1 判据尚未成立。**
> 实测三版判据**全部未能区分退化策略**：
> - 碰撞率 ≤10%：**zero-action 得 0.0%、full-throttle 得 0.0%**（不作为即可通过）；
> - 碰撞率 + 里程下限：**full-throttle 里程 38.48 m ≈ 活跃策略 37.99 m**（绕圈即可通过）；
> - 非对称胜率 >0.5：**zero-action 仍得 0.54**（初始条件主导终局）。
>
> **结构性根因（F14）**：同一策略同时驱动两车时，对局结果**完全由初始条件决定**——
> 任何建立在「终局相对位置」上的指标**在构造上都无法区分策略优劣**。
>
> **因此 §5 的 M1.1/M1.2 阈值暂不登记**——判据未验证前登记阈值，
> 违反本项目「阈值须有依据」的既有规范。
> 完整记录：`step0_criteria_findings.md`；可复现脚本：
> `results/20260921_phaseC_w6/step0_{a,b,c}_*.py`。
>
> **下一步（并入 M1 前置任务）**：按 F15 的三个方向重新设计判据并验证——
> ①**固定落后起跑**使胜负必须靠真实超越产生；②**任务级判据**
> （直接沿用冻结 env 的成功定义）；③**分级对手难度**逐级判定。
> **验证通过后**才登记假设、才开训。

评审的核心教训（也是 W5 的教训）：**判据本身必须先被验证**。

**新增硬性前置步骤（Step 0）**：**任何判据投入使用前**，
先用**退化策略**证明它**能区分**：

| 判据 | 退化策略（必须落带外） | 验证方式 |
|---|---|---|
| 碰撞率 ≤ 10% | do-nothing / random 对手 | 实测二者碰撞率 >> 10%（预期） |
| 稳定性 std ≤ 15% | 上述振荡训练 | 用 W5 joint 数据回放确认它 FAIL |
| 胜率判据（**M2/M3 若用**） | do-nothing 0.52、random 0.52 | **已知 FAIL** → 禁用该形态 |

> **若某判据无法被退化策略区分 → 该判据作废**，不得写入验收。
> （v1 的 W2 [35%,65%] 就是反面教材，评审 B4。）

**已废除的判据**（保留记录，不再使用）：
- ~~「对规则对手胜率 ≥50%」~~（依据已撤回 + 判据饱和）
- ~~「胜率 ∈ [35%,65%]」~~（饱和：退化策略在带内）
- ~~「熵比 ≤ 0.8×」~~（W5 已废，方向相反）
- ~~「桶均值相对比 ≤1.2×」~~（口径不稳健）

---

## 4. 环境等价性判别（评审 Q7 的可执行方案）

M3 若要把新 env 与冻结 env 对齐，**必须用阳性对照把"env 不等价"与
"checkpoint 过拟合"解耦**：

1. **建 6 维 shim env**（只暴露前 6 槽）——否则 6 维 checkpoint 直接抛
   `ValueError`（§1.4，v1 的锚点门 A 必败的原因）；
2. **加阳性对照**：在**新 env 任务模式**下**从零训一个 PPO**（分钟级），
   得"新 env 的原生策略"，**注意维度要与被比较者一致**；
3. **四格判别**（每格 ≥10 seed，固定 seed 集）：

| 被测策略 | 新 env 任务模式 | 原冻结 env | 判读 |
|---|---|---|---|
| 冻结 ckpt（6 维 shim） | 10/10 | 10/10 | **等价** ✅ |
| 新训策略（同维） | 10/10 | 10/10 | 等价（交叉确认） |
| 冻结 ckpt 低 / 新训高 | 3/10 | 10/10 | **checkpoint 过拟合旧 env** |
| **两者都低** | 2/10 | 10/10 | **新 env 与冻结 env 不等价**（env bug） |

**逻辑**：新训策略是"**新 env 的能力上限探针**"——它也上不去，问题在 env；
它轻松满分而冻结 ckpt 不行，问题在 ckpt。

**已知可用事实**（§1.3）：冻结 ckpt 在**原 env** 已确认 10/10，
故"原冻结 env"那一列已有基准值。

---

## 5. 假设登记（**训练前定死**）

| # | 假设 | 判定阈值（**定死**） | 若证伪 |
|---|---|---|---|
| **M1.1** | 对手池使对抗训练**稳定**（不重演 W5 振荡） | 末 20% 步窗碰撞率 **std ≤ 15%**，且 value_loss 非 NaN 且末段不单调增长 | 对手池无效 → 查陈旧度/容量 |
| **M1.2** | 对手池使碰撞率**降到可用水平** | 末 20% 步窗碰撞率 **≤ 10%** | 如实记录（24% 的 W5 值对物理演示不可用） |
| **M1.3** | 策略**未塌缩** | `train/std` 末 10 次 **> 0.05** 且末段不单调趋 0 | 查熵正则 |

**M2/M3 的假设待 M1 通过后再登记**（避免一次性铺开，评审 S1）。

> ⚠️ **上表 M1.1/M1.2 暂缓登记（2026-09-21）**：Step 0 实测证明这两个判据
> 均可被退化策略通过（详见 §3 与 `step0_criteria_findings.md`）。
> **判据重新设计并通过区分度验证后**，本表才成立。

> **判据前置验证（§3 Step 0）为开训前提**：退化策略必须落带外，
> 否则该判据作废。此条**先于训练执行**。

---

## 6. 执行步骤

| 步 | 内容 | 交付 |
|---|---|---|
| **0** | **判据验证**：用退化策略证明 M1 各判据有区分度 | 一段实测输出 |
| 1 | 实现对手池（**复用 W5 探针世界 + `train_ot.py` 脚手架**，不新增 env 特性） | `train_league.py` |
| 2 | 训练 ≥300 更新；周期保存快照入池 | `results/20260921_phaseC_w6/` |
| 3 | 按 M1.1–M1.3 判定 | 裁决 |
| 4 | （若 M1 过）登记 M2 假设 → 混合模式 | — |

**实现注意（沿用已验证的坑规避）**：
- 自定义 `VecEnv` 的 `observation_space` 声明**单 agent 形状**（否则摊平崩溃）；
- 训练脚本须 `if __name__ == "__main__":` 守卫；
- 性能测量须**机器空闲**（F8）；
- **任何跨布局适配器必须附 `adapter_fidelity_test.py` 式保真度单测**（F12）。

---

## 7. 风险与预案

| 风险 | 概率 | 影响 | 预案 |
|---|---|---|---|
| 对手池仍振荡（M1.1 失败） | 中 | W6 受阻 | 如实记录 → 说明非平稳性非"池"可解，须换方法（如连续快照权重） |
| 碰撞率降不到 10%（M1.2 失败） | 中高 | 不可用于物理演示 | 记录绝对值；考虑任务模式为主、对抗为辅的产品化路径 |
| 判据本身又饱和 | 中 | 重蹈覆辙 | **§3 Step 0 前置验证**（本版专为此设） |
| 阶梯太慢 | 低 | 进度 | 每级成本低（训练分钟级），实际瓶颈是判据设计而非算力 |

---

## 8. 时间预算

| 级 | 估计 |
|---|---|
| Step 0 判据验证 | 0.5 天 |
| M1（实现 + 训练 + 判定） | 1.5 天 |
| M2（若过） | 2 天 |
| M3（若过，含 2×2 阳性对照） | 2 天 |

---

## 9. 关联

- 上游：`plan_phaseC_w6.md`（v1，**Reject**）、`reviews/20260921_phaseC_w6_review1.md`、
  `ERRATUM_phaseC_findings.md`、`plan_phaseC_probe_v2.md`（W5）
- 流程：本 plan → **独立评审** → 修订 → 执行（§10）
- 更正记录：本版撤回 v1 的 R1 依据与 W2 判据，均已如实登记

---

## 附：对评审 5 条拦截级的处理对照

| 级别 | 评审项 | 处理 |
|---|---|---|
| 拦截 | **B1** W5 结论不可迁移到 V2V | ✅ §1.2 登记；**M1 改用自身基准**以保与 W5 可比；V2V 推到 M3 |
| 拦截 | **B2** 适配器 bug 致结论失真 | ✅ **已公开撤回**（`ERRATUM`），代码已修，新增保真度单测 |
| 拦截 | **B3** 锚点门 A 必败（6 vs 8 维） | ✅ §4 改为 6 维 shim + 2×2 阳性对照 |
| 拦截 | **B4** W2 判据饱和 | ✅ §3 废除；新增 Step 0 判据前置验证 |
| 拦截 | **B5** 单方失败不可归因 | ✅ §2 M1 不引入；§1.7 记录机制性理由 |
| 重要 | I1 理由自相矛盾 | ✅ §2 M1 动作语义理由重写 |
| 重要 | I2 证据未归档 | ✅ 已归档（11→21 脚本 + 2 raw） |
| 重要 | I3 W1 口径不稳健 | ✅ §2 M1.1 定死步窗口径 + 改 std 判据 |
| 重要 | I4 W4 分辨率不足 | ✅ §2 M2 提到 ≥30 seed |
| 重要 | I5 70/30 无比值依据 | ✅ §2 M2 要求敏感性或声明待调 |
| 重要 | I6 缺绝对验收 | ✅ §2 M1.2 加 ≤10% 绝对判据 |
| 建议 | S1 阶梯化 | ✅ §2 全文重构为 M1/M2/M3 |
| 建议 | S2 观测扩充推迟 | ✅ 推到 M3 |
| 建议 | S3 保留 SB3 约束说明 | ✅ §6 |
| 建议 | S4 §3.3 措辞 | ✅（M1 沿用既有共同参考，不再宣称"新修复"） |
| 建议 | S5 待确认点 7 入正文 | ✅ §4 |
