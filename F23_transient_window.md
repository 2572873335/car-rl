# F23 — 承诺窗口的物理来源是横移过程本身（与冷却无关）

> # ⛔ 【2026-09-21 二次更正 — 本节结论作废】
>
> 独立评审（`reviews/20260921_phaseC_m1_review1.md`）Reject；作者已逐条复现确认。
>
> **本文关于「横移窗口 / lift 3.32×（或 6.45×）」的表述作废。**
> 实测：**90 个「命中」全部是 t≈0.44–0.52 s 的开局切入**，
> 由初始条件**机械保证**（`gap0 ~ U(0.40,0.90)` 而 `guard_hi=0.90`
> ⇒ follower 开局 100% 在防守带内 ⇒ 封堵者首步必命令内圈）。
> 晚段 112 次切入**命中率为 0**。
>
> 且该口径**与获胜反相关**：**只在所谓「命中态」切入 → 完成率 0.00**；
> **随机切入**的命中率（84.6%）**高于**正例（44.6%）。
>
> **仍然成立**：封堵者基准有效（几何族 0.00）、反制脚本完成率 0.77、
> 真正的获胜条件是「封堵者**物理位于外圈**时切入」（`lane_b==0`）。
>
> 详见 `ERRATUM4_f23_correction_wrong.md`。


日期：2026-09-21
级别：**F22 级机制发现**（与 F14、F20 同级），须入档并配实测数字。
来源：W6 封堵者判据的修复过程（`W6_criterion_v5_established.md`）。

---

## 1. 发现

**"封堵者的承诺窗口"不是纸面上的换道冷却（1.5 s），
而是车身物理横移的 0.61–0.76 s。**

**反直觉的实测**（反制脚本，巡航 0.55、防守带 0.08–0.90）：

| 反制策略 | 完成率 |
|---|---|
| **等封堵者换道完成再切** | **0.00** |
| **在封堵者横移途中切** | **0.70–0.83** |

**机理**：封堵者车身横移时**物理上不在任何一条车道中心**，
其拒止能力"悬空"——此时切入**无法被回应**。
这解释了为什么"等冷却结束再进攻"是错的：
冷却结束时封堵者**已经**物理到位，窗口早已关闭。

**测量先行**（`_measure_transient.py`）：

```
车 1 被命令 outer→inner 后，车身物理到达内圈中心耗时：0.86 s
SWITCH_COOLDOWN = 1.5 s（纸面）   switch_timer = 0.8 s（限速）
修复后实测物理瞬态：0.61–0.76 s
```

---

## 2. 对 M1 的科学含义（**已写入 W6 plan 引言位**）

题目现在有了**已知标准解法**（反制脚本，完成率 0.78）。
M1 的研究问题从"RL 能否学会超车"**升级**为：

> **"RL 能否独立发现这个横移窗口——或发现更好的东西（如诱导封堵者
> 换道的佯攻）？"**

反制脚本是**下限**（手写解法即可 0.78）；RL 的学习目标 ≥ 0.55（下限的 70%）。
佯攻等"更好的东西"若出现，是**超越手写基线的涌现行为**，属 RQ3 素材。

---

## 3. 双约束判据模板（**F22 级流程模板，此后沿用**）

封堵者同归于尽是任务性质（碰撞对双方都是失败），
单指标验收无法区分"学会"与"激进撞赢"。

**模板（本项目后续判据沿用）**：

```
验收 = 主指标 ≥ 主阈值  且   安全指标 ≤ 安全阈值
  主指标锚定正例成绩（如 正例 × 70%）
  安全阈值取正例的最差观测（诚实反映任务性质）
  边界结果补 seed，不放宽阈值
```

**实例（M1）**：

| 指标 | 阈值 | 依据 |
|---|---|---|
| 完成率 | ≥ 0.55 | = 反制脚本 0.78 × 70% |
| 碰撞率 | ≤ 0.25 | = 反制脚本最差观测 23% |

**格式优于单指标的理由**：单指标可被"激进撞赢"钻空子；
双约束要求**既学到正例级能力、又维持正例级安全**。

---

## 4. 复现

```bash
cd /home/zy/car_rl/code0919
PYTHONPATH=$PWD uv run python results/20260920_phaseC_probe/scripts/_measure_transient.py
PYTHONPATH=$PWD uv run python results/20260920_phaseC_probe/scripts/_counter3c.py   # 正例 0.70–0.83
```
