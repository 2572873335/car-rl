# Plan — Phase C W5: RQ3 自博弈探针（修订版 v2）

日期：2026-09-20
对象：`results/2026092x_phaseC_probe/`
关联：`research/roadmap.md` §Phase C；`DATA_MANAGEMENT.md`（评审流程，见 §10 缺口）；
     交接文档 §4；**评审** `review_phaseC_probe_1.md`；**自查实测** `findings_phaseC_selftest.md`

> **v2 修订说明**：本版并入 (a) 独立评审的 3 条拦截级 + 6 条重要问题，
> (b) plan 作者自查的 9 项实测发现（含一项决定性结果）。
> **最重要变化**：探针主路径由「完整联合自博弈」**改为「冻结一方的联赛模式」**，
> 依据是 §2.4 的实测（完整自博弈 1.28M 步不收敛；冻结一方即学会）。

---

## 0. 本轮要回答什么

**探针问题**：在本项目现有技术栈上，**能不能**跑通"两车共享参数 + 零和对抗"的
多智能体训练？

**这是硬门，不是实验**：3 天内给通过/不通过；不通过则放弃 RQ3、Phase D 提前。

**状态**：**该问题已在 2026-09-20 当天被实测回答**（§2.4），**无需占用 3 天**。

---

## 1. 前置事实核实（**全部实测**）

| # | 事实 | 证据 |
|---|---|---|
| 1.1 | `pettingzoo`/`supersuit`/`ray`/`marllib` **全部缺失** | 导入探测 |
| 1.2 | 真实环境吞吐 2,417 steps/s（单环境） | 200 集实测 |
| 1.3 | 并行扩展**非线性**：4/8/16 路效率仅 45%/36%/21% | 实测 |
| 1.4 | SB3 可用**自定义 VecEnv + 共享参数**驱动两 agent | 实测（features_dim=6，同 obs 同动作） |
| 1.5 | `observation_space` 前导维会被**摊平**（`(2,4)`→8 维，崩） | 实测复现 |
| 1.6 | `SubprocVecEnv` 需 `__main__` 守卫（否则 `EOFError`） | 实测复现 |
| 1.7 | 冻结 5 文件 sha256 **全部 OK** | `sha256sum -c` |

**吞吐结论**：1.28M 步在 8 路上约 **14 分钟**（实测 814 s @ cuda / 832 s @ cpu）。
**算力不是瓶颈，工程时间才是**——这决定了对重依赖库的取舍。

## 2. 选型与**决定性实测结果**

### 2.1 选型：不装 PettingZoo / RLlib / MARLlib

| 方案 | 裁决 |
|---|---|
| RLlib / MARLlib + MAPPO | **否决**：重依赖、与 gymnasium 1.3.0 的 pin 冲突风险（同 d3rlpy 事故） |
| PettingZoo + 自写 MAPPO | **否决**：仍需自写损失/GAE/对手采样，收益仅"接口好看" |
| **SB3 独立 PPO + 参数共享** | **采用**：零新依赖，1.4 已实测 |

**评审收紧（S2）**：1.4 的证据**只支持"探针阶段不需要"**，**不支持**"W6–W9 也不需要"。
SB3 `VecEnv` 有**硬约束：所有行必须共享同一 `observation_space`/`action_space`**
（`base_vec_env.py:59-86`）。W6 若引入"非对称角色/可变 agent 数"，会**先撞这堵墙**；
对手池需自行实现"一行跑冻结快照"，**有额外工程量**（§7 风险表原低估了这点）。

### 2.2 已接受的偏差：IPPO 代替 MAPPO（评审 Q1）

`AGENT_HANDOFF.md §4` **预先背书**"独立 PPO + 参数共享的最简实现"。
探针问的是**机制**，非"MAPPO 本身好不好"。**显式登记**：本探针**不验证集中式
critic**；W6 若要 MAPPO 需另评。

### 2.3 观测设计的**正确表述**（修正评审 B3）

**删去"两视角严格互为镜像"**——该断言**不成立**（两车跨车道时 `lane_self ≠ lane_other`，
而镜像规则要求 `lane` 不变；证明见评审 B3）。

**真正需要且正确的性质**是：
> **两行使用同一个 self-view 编码函数**，由**有符号相对弧长**保证编码与角色无关。

保留有符号 `d`（这一步是对的、有价值的），但**不再宣称对称性**，
并**删去 §4 一切依赖"对称"的推论**。

**三处必须统一的细节**：
1. **开闭区间**：与冻结 `overtake_env.py:126-127` 逐字一致，用
   `[−L/2, L/2)`（plan v1 误写 `(−L/2, L/2]`，**恰好相反**）；
2. **测量参考路径**：`d` 必须把**两车 s 都投影到外圈**（共同参考），
   否则两车 s 属不同总长（5.4849 vs 4.5424 m）的坐标系，`wrap(s_other−s_self)`
   **无意义**——这是"静默学不动"最可能的触发点（评审 §五-2）；
3. **第六槽** `(L/2−|d|)` 是 `|d|` 的函数，镜像下**不变**，**需显式声明**。

### 2.4 ★决定性实测：完整自博弈**不收敛**，冻结一方**成功**

**（A）完整联合自博弈**（plan v1 §6.1 原设计，8 worlds/16 rows、共享参数、
零和、1,280,000 步 = 312.5 更新，满足铁律 4）：

```
      steps   mean_ret  ~collision%  entropy_loss   value_loss
      1,728     -740.0        74.0%
      2,928      -10.0         1.0%     ← 早期改善
      4,128        0.0         0.0%
      ...
  1,247,552     -880.0        88.0%     -3.369        1.97e+04
  1,254,000    -1000.0       100.0%     -3.370        1.88e+04
  1,259,824     -880.0        88.0%     -3.371        8.14e+03
```

→ 碰撞率 4k 步时到 0%，**随后持续反弹并钉死在 88–100%**；回报钉在 −880~−1000；
熵**不降反升**（2.84 → 3.37 nats）。**P1 失败；P2 三个对照全部饱和。**

**（B）冻结一方（联赛退化档）**——对手 = 20 次更新后的快照，只训另一车
（200 更新 / 409,600 步）：

```
    episodes  mean_return  collision%
          51       -500.1      100.0%
       30432          0.3        0.0%
       98512         -0.3        0.0%
      166592        -60.4       12.0%
      404872       -120.8       24.0%
```

→ **碰撞率 100% → 24%（多数时段 0%）**，回报 −500 → 近 0。**学习发生了。**

**裁决**：

| 问题 | 答案 |
|---|---|
| 机制能否接通？ | **能**（1.4/1.5） |
| 完整自博弈能否收敛？ | **不能**（A） |
| 障碍是机制还是非平稳性？ | **非平稳性**（B 冻结一方即学会） |

→ **探针主路径改为 (B)**；完整自博弈的收敛问题**降级为 W6–W9 研究内容**
（对手池/快照历史/混合模式），**不再作为探针门通过条件**。

---

## 3. 环境设计：`selfplay_env.py`

### 3.1 最小改动原则（**评审 I1 收紧**）

v1 称"与冻结 env **同一观测编码语义**、W6–W9 可**直接可比**"——**评审 I1 证伪**：
逐槽比对，冻结 6 维 `[(gap−0.2)/0.5, delta/2.5, v_f/1.3, e_lat/0.25, lane, v_l/0.5]`
与本 plan 6 维 `[d/2.5, v_self/1.3, v_other/1.3, e_lat/0.25, lane, (L/2−|d|)/2.5]`
**只有 slot2/3(e_lat)、slot4(lane) 一致**，其余**物理量或归一化都不同**。

**修订**：**保留冻结 6 维布局**（使 `baseline_action_ot` 可直接复用，
也保住可比性），把自博弈所需的量**塞进第 6 槽**或**新增维度并出对照表**。
**必须**给出"冻结布局 vs 自博弈布局"逐槽对照表，并**下调"直接可比"措辞**。

### 3.2 规则对手接口（**评审 I2**）

冻结 `baseline_action_ot`（`overtake_env.py:59-61`）按冻结布局解码
（`gap=obs[0]*0.5+0.2, delta=obs[1]*2.5, lane=round(obs[4])`）。
**实测**：喂 plan 布局的 obs 会得到**全垃圾**。**修订**：若保留冻结布局则
可直接复用；否则**必须写显式适配器并单测**（同状态两编码 → 同动作）。

### 3.3 奖励与零和

```
r_self = +w(δ_self)     r_other = −w(δ_self)
```

**明确声明**（评审 B1 + 自查 F4）：本博弈**仅在非终止项上零和**；
**碰撞（−500）/出界（−100）罚同时施加给两车**，故那些步 `r0+r1 ≠ 0`。
**这不是缺陷，而是有用信号**：

```
实测 mean(r0+r1) = −(1000·p_collision + 200·p_offtrack)   ← 逐行吻合
random −825 → rule 0.0
```

### 3.4 终止与计分（**评审 I3/I4**）

- **I3 套圈**：外圈 5.4849 m，30 s 内 0.7 m/s 的车跑 ~21 m ≈ **3.8 圈**。
  v1 的"胜 = 终局 `delta>0`"在**套圈后失效**（delta 在 ±L/2 回绕）。
  冻结 env 靠 `ahead_flag`（**永不重置**，`overtake_env.py:229-231`）处理。
  **修订**：胜负必须引入**累积圈数/ahead_flag 式**量，或缩短视界到 ≤ 一圈。
- **I4 同生共死**：v1 让两行 `dones` 同时置位，两个后果未处理：
  (a) **无辜车被连坐**（一车出事却惩罚另一车）；
  (b) **抹掉"封堵/逃脱"信号**（"致对手出局"正是不对称性使封堵有意义的来源）。
  **修订**：给出"单方失败即分胜负"的终止与计分规则，或**显式论证**探针阶段
  为何放弃并列入 W6 待办。**并写清**：同生共死的真实动机是 **SB3 约束**
  （`dummy_vec_env.py` 每行独立 auto-reset，shared world 下 done 不同步会相位错），
  它是 **workaround 而非博弈设计的自然选择**。

---

## 4. 假设登记（**训练前定死**）——P1/P2/P3 全部重设

**评审 Q4 结论**：v1 三条阈值**均需重设**（B1 回报、B2 熵、I6 P3）。

| # | 假设 | 判定阈值（**定死**） | 依据 |
|---|---|---|---|
| **P1** | 策略**未塌缩**、训练**未发散** | `train/std` 末 10 次 **不单调趋 0**（判塌缩用 **< 0.05**）；`value_loss` **非 NaN 且末段不单调增长** | 评审 B2/S4：实测 `ent_coef=0.01` 下熵**贴最大熵不降**（ratio 1.00–1.04），0.8× 熵比**方向相反会误杀**；SB3 字段是 `train/entropy_loss`（**负值**），熵 = `−entropy_loss` |
| **P2** | 习得**真实博弈技能** | **冻结一方**模式下：碰撞率末段较首段**下降 ≥ 50%**（相对），**或**平均回报上升至 **> −100** | **F9.2 已实测达成**（100%→24%，回报→近 0）。**不用**"对规则对手胜率"（F5：规则机 40/40 局零次领先易手，非对手） |
| **P3** | 动作**非恒定**、有**状态依赖** | `train/std` 末 10 次 **> 0.05** 且对固定观测批动作**非恒定** | 评审 I6：v1 阈值 0.05 **过松**（实测 std≈1.0）；须**定义评估批**并区分 aleatoric/epistemic |

**有效进度信号（修订后清单）**：
1. **per-agent / per-role 回报**（B1：非对称设计下**有效**，实测 E[R_i] = ±317）；
2. **碰撞率**（F4：`mean(r0+r1) = −(1000c + 200o)`）；
3. `train/std`（塌缩）、`value_loss`（发散）。

**已废除的判据**：~~"对称零和 ⇒ 回报恒 0，不可作指标"~~（B1 证伪）、
~~"熵比 ≤0.8×"~~（B2 证伪）、~~"对规则对手胜率"~~（F5 证伪）。

---

## 5. 铁律 4 的**适用范围**（评审 I5 修正 v1 论证）

**算术正确**：`1,280,000/(16×256)=312.5` ✅；`100,000/4096=24.4` ✅。

**但 v1 的论证错了**：铁律 4 的语境是"**产出结论的训练**"，
而项目**自己**在 `Makefile:4-6` 写死：
> "the short runs **intentionally violate** the project's '>= 300 gradient
> updates' rule. It is a **functional smoke test, NOT a result reproduction**."

v1 **已经**用这条逻辑给 5k smoke 开了豁免，却拒绝把同一逻辑用到
"定义上就是机制 smoke"的探针上——**自相矛盾**。

**修订为**：
> 铁律 4 对探针**形式上豁免**（`Makefile` 先例）。本项目**主动选择** ≥300 更新
> （1.28M 步 @16 行），**因为它便宜（~14 分钟）且使进度信号更清晰**——
> **而非因为铁律 4 要求**。

---

## 6. 执行方案（**按 §2.4 重写**）

### 6.1 探针主路径 = Step 1（冻结一方），**跳过** v1 的完整自博弈

**Day 0（~2 小时，已完成）**：本文档 §2.4 —— 已实测回答探针问题。

**Day 1 — 在冻结一方路径上做正式训练**
- 复用 `train_ot.py` 脚手架（标准 SB3 单智能体任务，**不需要**自定义 VecEnv、
  镜像观测、同生共死——**工程成本远低于 v1 §6.1 的四件产物**）；
- 对手 = 快照（先跑短程共享参数热身取得）；
- ≥300 更新；按 P1/P2/P3 判据评估。

**Day 2 — 稳健性**
- 多个对手快照（简化对手池）；验证 P2 在换对手后仍成立。

**Day 3 — 裁决 + 归档**
- 逐条判 P1/P2/P3；轨迹图；RUNLOG；进 `reviews/`。

### 6.2 产出物

| 文件 | 说明 |
|---|---|
| `selfplay_env.py` | 双车共享 world（**保留冻结观测布局**，§3.1） |
| `train_selfplay.py` | 训练 + 周期评估 + 日志（含 `__main__` 守卫，1.6） |
| `results/2026092x_phaseC_probe/` | config / 原始 log / 曲线 |

**不修改**任何冻结文件；新文件训练后补 `DATA_MANAGEMENT.md` §8 哈希表。

### 6.3 训练配置（**评审 S3/S8 修正**）

沿用冻结 PPO 超参（`[128,128]`、lr 3e-4、n_steps 256、batch 512、
γ0.99、GAE 0.95、clip 0.2、ent_coef 0.01、seed 0）。

**`device='cpu'` —— 选择正确，但论证改为引用本表实测**（评审 S3 + 自查 F6/B8）：

```
[A] SubprocVecEnv (8 进程):   cpu 2,587  vs cuda 1,845 steps/s  -> cpu 快 40%
[B] 单进程 self-play VecEnv:  cpu 2,386  vs cuda 1,881 steps/s  -> cpu 快 27%
```

> **注意**：`train_ot.py:50` **未传 device**（走 SB3 默认 auto→cuda）。
> 故此处 device 是**有意改动**，非"沿用"，须注明。
> **测量纪律**：基准测试**必须在无其他训练进程时运行**（见 §8 F8）。
> 本项结论曾因并发负载而**反转**，复测才得正确方向。

---

## 7. 风险与预案

| 风险 | 概率 | 影响 | 预案 |
|---|---|---|---|
| 完整自博弈非平稳性 | **已确认发生** | 不收敛 | **主路径改为冻结一方**（§2.4B 已验证）；对手池留 W6 |
| 自定义 VecEnv 撞"共享 space"墙 | 中 | W6 返工 | §2.1 已登记；探针阶段不用 |
| 观测布局与冻结不一致 → 不可比 | 中 | 可比性失效 | §3.1 保留冻结布局 + 对照表 |
| 规则对手接口错位 → 静默学不动 | 中 | 白烧时间 | §3.2 适配器 + 单测 |
| 套圈判据失效 | 中 | 胜负无意义 | §3.4 圈数/ahead_flag 或缩视界 |
| CPU 更快结论随规模变化 | 低 | 性能 | 已登记适用条件，规模变更须复测 |
| 基准测量被并发污染 | **已发生** | 结论反转 | §8 F8 纪律 |

---

## 8. 流程教训（**本项目新增**）

**F8 性能测量须串行隔离**。本轮 device 结论曾因**并发训练**而反转
（初测 cuda 快 1.9× → 复测 cpu 快 27–40%）。
建议补入测量规范：**基准测试须在无其他训练进程时运行，
并在 RUNLOG 记录"机器空闲"状态**。

**评审流程价值**：这是**第五次在动手前拦截真问题**（评审自评）。
B1+B2 若带入执行，会在 Day 3 得到"回报不动 + 熵没降"的**双假阴性**，
**错误判定 RQ3 失败并提前启动 Phase D**——代价两周。

---

## 9. 关联

- 输入：`car_following_sim.py`（只读导入）、冻结 `overtake_env.py`（布局基准）
- 产出：`results/2026092x_phaseC_probe/`
- 假设登记：`research/ASSUMPTIONS.md`（P1–P3）
- 评审：`reviews/20260920_phaseC_probe_review1.md`（**待归档**）
- 自查：`findings_phaseC_selftest.md`
- **待办（非本 plan 范围）**：`DATA_MANAGEMENT.md` **§10 评审流程章节缺失**
  （全文仅 §1–§9，却被 12+ 文件反复引用）——建议补写或统一改引用。

---

## 附：对评审意见的处理对照

| 级别 | 评审项 | 处理 |
|---|---|---|
| 拦截 | B1 §4 回报恒 0 | ✅ §3.3/§4 改为非对称口径，per-role 回报入有效信号 |
| 拦截 | B2 P1 熵判据 | ✅ §4 换 `train/std` + `value_loss`，标注熵=−entropy_loss |
| 拦截 | B3 §3.2 严格镜像 | ✅ §2.3 删"严格镜像"；统一 `[−L/2,L/2)`；补第六槽声明 |
| 重要 | I1 观测布局 | ✅ §3.1 保留冻结布局 + 对照表 |
| 重要 | I2 规则对手接口 | ✅ §3.2 适配器 + 单测 |
| 重要 | I3 套圈判据 | ✅ §3.4 |
| 重要 | I4 同生共死 | ✅ §3.4（含"是 SB3 workaround"声明） |
| 重要 | I5 铁律 4 | ✅ §5 改论证 |
| 重要 | I6 P3 | ✅ §4 收紧 + 定义评估批 |
| 建议 | S1–S4 | ✅ 并入 §2.4/§4/§6.3/§8 |
| 建议 | §五 1–9 | ✅ #2→§2.3、#3→§3.1、#4→§3.4/§7、#5→§2.3、#7→§9、#9→§1.7 保留 |
| **自查** | F9 决定性实测 | ✅ §2.4 改写探针主路径 |
