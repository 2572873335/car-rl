# D0 plan — v3（补落 wrapper 裁决 + 口径更正 + regime-extension）

日期：2026-09-22
性质：v2 的**继承修订**。起因：`c0b7b7d` 提交信息声称的「wrapper 硬约束」与「§8」
**从未写进 plan 文件**（该提交只改了 `README.md` 与 `_d0_step0_wrapper_probe.py`）。
本 v3 把 owner 裁决真正落地，并补入 v2 审查后新得的实测口径发现。
关联：`plan_D0_scenario_audit.md`（v1，作废）、`plan_D0_scenario_audit_v2.md`（v2，被本文件取代）、
`reviews/20260922_D0_review1.md`（Reject，5 拦截级）

---

## 0. v3 相对 v2 的四处实质变更

| # | 变更 | 依据 |
|---|---|---|
| **C1** | **实现路径改为 wrapper**：运行时覆写 `follow_env.D_DES`，**冻结文件逐字节不动**。v2 的「改 `D_DES` 常量」（§1.1/§4/§7）作废 | owner 裁决（`c0b7b7d` 转述）；评审 R2 只证明了「checkpoint 跨间距零样本成立」，**不等于**「冻结文件可改」 |
| **C2** | **新增 §8 regime-extension fix experiment**（D0.5–D0.7 + honesty clause） | `c0b7b7d` 声称已加，实际未落地，本文件补齐 |
| **C3** | **新增聚合口径声明**（§2.0）：除单位口径外，**必须同时声明 whole/settled** | 本次实测（下 §2.0.1）证明同一格两口径差 41%；**作者侧**（README/plan v2 `c0b7b7d`）用 whole 且未声明，`review1` 用 settled 自洽 |
| **C4** | **补 §7 遗漏的 `_d0_review3` 落点**：评审报告引用的脚本在仓库内无落点（违反 §5b/F19） | 全仓库检索确认缺失 |

---

## 1. 实验设计（wrapper 路径，C1）

### 1.1 实现约束（owner 裁决，硬约束）

**`follow_env.py` 逐字节不动。** `D_DES` 的覆写走 wrapper：
`reset()` 内设 `follow_env.D_DES = d`，**还原必须走 `try/finally`**（见下 §1.1.1），
勿照抄 `robustness_sweep.RobustnessWrapper` 的朴素 close——

#### 1.1.1 **异常路径泄漏是硬缺陷（review2 B3，已实测复现）**

朴素实现（`reset()` 设值 / `close()` 还原）在**正常交叠下干净**，但
**任一回合在 `reset()` 与 `close()` 之间抛异常 ⇒ `follow_env.D_DES` 滞留**：

```
(b) 异常抛出且 close() 未执行 → fe.D_DES 仍为 0.50
    raw env（未用 wrapper）此时追错设定点: terminal gap=0.463（应为 0.20）   ← 实测
```

D0 将跑 9 格 × 3 策略 × 最多 100 seed = **一次崩溃即静默污染其后全部格子，
而表仍会打出数字**（假数据）。**两条强制要求**：

1. **`try/finally`**：`reset()` 之后无论如何要经 `finally` 还原 `D_DES=0.20`；
   推荐实现为 context manager（`with ddes(d): ...`）。
2. **每次 run() 后断言 `follow_env.D_DES == 0.20`**（廉价守卫，防静默污染）；
   断言失败即中止并报错，**不得继续出表**。

> 附带证伪（review2）：`baseline_action` **不读 `D_DES`**（只对 obs 反归一化），
> 故 P+FF 在**策略层**本身免疫此泄漏；但**环境层**（`_obs()`/`step()`）不免疫——
> 泄漏仍会污染 RL 与任何裸 env。
>
> 同型缺陷：`_verify_r4_grid.py` 在 `run()` 入口设 `D_DES`、只在 `main()` 最末还原，
> 中途崩溃同样泄漏。执行时须一并加固。

> 注意二者的差别并在代码注释中写明：`RobustnessWrapper` 设的是 **instance attribute**，
> 而 `D_DES` 在 `_obs()`/`step()` 里是**裸引用的模块常量**，故必须覆写
> `follow_env.D_DES` 本身（模块级），instance attr 无效。

**Step 0 已完成并实测验证**（`results/20260922_D0_scenario_audit_scripts/_d0_step0_wrapper_probe.py`）：

```
follow_env.py sha256[:16] before: 941c447b969c2fa1
follow_env.py sha256[:16] after:  941c447b969c2fa1      untouched: True
```

⇒ 铁律 2 的冻结文件**未被触碰**，因此**不触发重训**。

### 1.2 跨间距零样本：作为**正值证据**记录（评审 R2）

| 格子 | RL true-cm（whole） | 碰撞 |
|---|---|---|
| d=0.20 v=0.30（as-trained） | 2.73 | 0/20 |
| d=0.50 v=0.55 | 2.69 | 0/20 |
| d=1.00 v=1.00 | 4.59 | 0/20 |

**解释边界（必须写进审计表）**：gap 轴泛化是**架构性**的
（`obs[0]=e/0.5` 是相对误差 + `_leader_v()` 的 V2V 速度先验 `v_cmd=v_l+0.8a`，
零动作即速度匹配）——**不是**「策略学会了新任务」。
⇒ 可作为**部署灵活性论据**（一套控制器适配 0.2/0.5/1.0 m 三种车队间距规格，无需逐规格重训），
但**不可**表述为「泛化能力」。

### 1.3 速度轴：反转（**结论成立，但强度取决于口径——见 §2.0.1**）

---

## 2. 验收判据

### 2.0 聚合口径声明（**C3，本 v3 新增**）

#### 2.0.1 实测：同一格两口径差 41%（本次 30-seed 对照）

`results/20260922_D0_scenario_audit_scripts/_d0_reconcile_conventions.py`

| 格子 | RL whole | RL **settled** | P+FF whole | P+FF **settled** | 胜者(settled) |
|---|---|---|---|---|---|
| d=0.20 v=0.30（分布内） | 2.48 | **0.31** | 4.59 | 1.05 | RL |
| d=0.50 v=0.55 | 2.52 | 1.40 | 4.20 | 2.41 | RL |
| **d=0.50 v=1.00** | 5.63 | **3.99** | 5.09 | **2.87** | **P+FF** |
| d=0.50 v=1.00 sinusoid | 4.93 | 3.44 | 5.02 | 2.87 | **P+FF** |
| d=0.50 v=1.00 brake | 4.29 | 3.52 | 4.30 | 2.75 | **P+FF** |
| d=1.00 v=1.00 | 4.74 | 3.82 | 5.21 | 3.10 | **P+FF** |

（单位：true-cm = 100×|e|；whole = 全回合均值；settled = `e[20%:]` 均值）

**三条必须写进审计与 README 的事实**：

1. **仓库权威口径 = obs-cm + settled**（`train_ppo.py:90` 取 `obs[0]`×100 ≡ 200|e|，且跳前 20%）。
   D0 既有脚本（`_verify_r4_grid.py`、`_d0_step0_wrapper_probe.py`）用 **true-cm + whole**，
   **与仓库数字不可直接并列**。
2. **README 现文（5.63 vs 5.09）是 true-cm + whole**，却与表格里 obs-cm + settled 的
   「1.00 cm / 1.93 cm」并排 ⇒ **口径错配**，须改。
   换算到仓库口径（obs-cm + settled）：**RL 7.98 vs P+FF 5.74**。
3. **settled 口径下，v=1.0 的所有格子 P+FF 全胜**（含 sinusoid 与 brake 格——
   这两格在 **whole 口径**下被**作者侧 plan v2 §1.2** 标为「RL 微弱胜」，
   但在 settled 口径下是 P+FF 胜。**注意：`review1` 从未使用「微弱胜」一词
   （grep = 0），它的 R4 表把两格都标「负」**）⇒ 反转范围比 README 现文写的
   「单格」**更广**：**整个 AGV 速度带上 RL 劣于 P+FF**。

> **更正（作者侧，2026-09-22）**：初稿曾称「评审报告自身混用了口径」——**此说法有误，已撤回**。
> grep 核实：`review1` 全文 **0 次**出现 5.63/5.09；其 R3 段（3.99 true-cm）与
> R4 表（7.98 obs-cm）是**同一格、同一 settled 口径**的自洽数字。
> **混用口径的是作者侧**——`c0b7b7d` 提交信息、README、plan v2 §1.2 使用
> whole true-cm（5.63/5.09）且**未声明任何口径**。
>
> **最尖锐处（口径未声明的代价）**：`d=0.50 v=1.00 sinusoid` 格，
> 作者侧（whole）标「RL 微弱胜」（4.93 vs 5.02），评审表（settled）标「负」
> （6.88 vs 5.74）——**同格结论相反**，唯一原因就是口径未声明。
> v3 起统一为 settled + obs-cm。

**D0 全部输出一律用 obs-cm + settled，并在每张表同时标注 true-cm/whole 两列。**

### 2.1 单位口径（评审 R3）

```
obs-cm  = 200 × |e| [m]   （仓库惯例，论文数字口径）  ← D0 判据一律用此
true-cm = 100 × |e| [m]
```

### 2.2 判据

| # | 判据 | 阈值（obs-cm + settled） | 说明 |
|---|---|---|---|
| **D0.1** | d_des=0.50 精度（分布内） | RL `mean\|e\|` ≤ **5** | v1 的 12.5% 算错（实为 5%） |
| **D0.2** | v=1.0 精度 | RL ≤ **8** 且**零碰撞**（d≥0.50） | **⚠ 见下「边界警报」** |
| **D0.3** | 分布内/外先验划界 | 见 §2.3 | |
| **D0.4** | 相对基线 | **分布内**格 RL 不劣于 P+FF；**分布外（v=1.0）格如实报告胜者** | |

> #### ⚠ 边界警报 + **聚合单位定义**（review2 B1，**拦截级**）

**问题**：v3 原文只写「RL ≤ 8」，**未定义这是对哪个量**（每回合？每 seed？100 seed 均值？）。
**同一数据下两种读法结论相反**：按 100 seed 聚合均值 → **通过**；按单回合 → **未通过**。
这正是 review1 R3「靠猜单位定生死 = 不可判」的同型复发（换成聚合维度）。

**实测（本作者独立复现，与 review2 逐位吻合，`_d0_review2_boundary_100seed.py`，
`d=0.50 v=1.00 constant`，settled obs-cm）**：

```
RL, 100 seeds x 4 blocks:
  seeds 2000-2099: agg_mean=7.973 std=0.160 max=8.386  ep>8.00: 39/100  coll=0/100
  seeds 3000-3099: agg_mean=7.946 std=0.185 max=8.337  ep>8.00: 36/100  coll=0/100
  seeds 4000-4099: agg_mean=7.954 std=0.177 max=8.544  ep>8.00: 37/100  coll=0/100
  seeds 5000-5099: agg_mean=7.979 std=0.188 max=8.463  ep>8.00: 44/100  coll=0/100
P+FF, 100 seeds: agg_mean=5.731 std=0.120 coll=0/100
```

**⇒ 结论：30→100 seed 根本不能判定这个边界。** 30 seed 的 7.98 与 100 seed 的 7.97
**一致**，说明该值**稳定地坐在 8.00 下方约 0.03 cm**——是**结构性贴线**，非抽样抖动。

**定义（本节为 D0.2 的唯一权威口径）**：

> **D0.2 的聚合单位 = 100 个 seed 的 per-seed settled mean 的均值**（"agg_mean"）。
> 判据作用于**该聚合值**，并**同时报告其 ±SE**。
> **单回合越线不作为判负依据**（否则 D0.2 按构造必挂：36–44% 回合越线）。

**依此定义，D0.2 = 通过（7.95–7.98 ± ~0.02）**，但**必须明标**：
- **余量仅 ~0.03 cm（约 1.7σ）**，处于铁律「边界」语义——**不得假装它有余量**；
- 该格 **P+FF 亦满足 D0.2（5.73 ≤ 8）** ⇒ D0.2 是**绝对门、不区分 RL/P+FF**
  （相对比较交 §2.2 D0.4）。**须显式声明，避免被读成「D0.2 证明 RL 在 AGV 速度够用」**。

> **预登记修正**：本节取代 v3 原文的「补 seed 后越过 8.00 即判未通过」——
> 改为「**聚合均值**越过 8.00 才判未通过」。**阈值 8 本身不放宽**。

### 2.3 分布内/外先验划界（评审 R5 裁决）

```
训练分布：v_set ~ U(0.25, 0.50)，D_DES = 0.20（follow_env.py:115/41）
先验划界（训练前定死，进 ASSUMPTIONS.md）：
  分布内格：v ≤ 0.55 且 d_des ∈ {0.20, 0.50, 1.00}
  分布外格：v = 1.00
  v=1.0 的反转是分布外行为，如实报告——既不算失败也不算通过
```

> **0.55 的来源（review2 I1 裁定，实证依据）**：训练期 `sinusoid` behavior
> （`amp ≤ 0.4`，`follow_env.py`）的**领车瞬时速度可达 0.70 m/s**——
> 即 0.55 落在**训练已见速度带内**，而非外推。
> 故 0.55 的锚定依据升级为「**训练分布内**」（强于单纯的「+10% 裕度」）。
> 保留 0.55，**依据改用实证**。
>
> **⚠ 诚实条款（review2 四·3，须并入叙事）**：既然训练已见 0.70 m/s 瞬时速度，
> **v=1.0 只是比它高 43%，并非突变**。故 **D0 报告与 README 不得表述为
> 「1.0 m/s 是截然不同的分布外速度」**——如实说法是「训练分布的**名义**上界是 0.50
> （`v_set` 均匀采样），但 sinusoid 瞬时可到 0.70；1.0 超出名义带，且 RL 残差在此失效」。
> 这是「过度的 OOD 叙事」必须避免的地方。

### 2.4 防「不能输判据」（评审 R5）

- 至少 3 个 AGV 相关格（0.50×0.55、0.50×1.00、1.00×1.00）须输出**正值证据**；
- **实测状态（settled + obs-cm）**：0.50×1.00 = 7.98（贴线）、1.00×1.00 = 7.64、0.50×0.55 = 2.80
  ⇒ 三格均有正值证据，但**首格是边界警报项**；
- 每个失效格给出**量化重训成本上限**；
- 全部**进 `ASSUMPTIONS.md` 预登记**（执行前）。

> **正值证据与 D0.2 解耦（review2 I3）**：「正值证据」= **给出该格的 settled 实测值 + 零碰撞**，
> 是一个**事实陈述**；「是否 ≤ 8」由 **D0.2 单独裁定**。
> 二者结论不一致时（如 0.50×1.00 的 7.98 在 D0.2 判未通过的情况下），
> 审计表**并陈两者**：该格仍**输出正值证据**（有实测值、零碰撞），
> 但 **D0.2 的判定独立如实记录**。不得用前者掩盖后者。

### 2.5 「9 格」的口径定义（review2 I5）

§5/§8.2 的「9 格」= **(d, v) 二维网格**：d ∈ {0.20, 0.50, 1.00} × v ∈ {0.30, 0.55, 1.00}，
**behavior 取 `constant`**。`sinusoid` / `brake` 作为 **(0.50, 1.00) 与高速格的附加行**单列，
**不计入 9 格**。

⇒ **D0.7 的「9 格中 ≥7 格」按此 (d,v) 网格计数**，behavior 变体不参与计数。
（若后续决定对每个 (d,v) 取 3 个 behavior 平均，须在此改口径并重登记 ASSUMPTIONS，
**不许两套口径并存**。）

> **补格（review2 I2）**：`(0.20, 1.00)` 是 review1 R4 表引用的格子，**必须补回 9 格网格**——
> 否则 review1 的表无法由落盘脚本整表复现。reconcile 脚本已同步扩到含该格。

---

## 3. 事实更正（v2 已采，保留）

| 项 | v1 的说法 | 实测 | 修订 |
|---|---|---|---|
| 刹车距离 | v²/2a | **正确**（速度通道是摆率限幅，无 τ） | 保留 |
| 1.0×0.20 物理必撞 | 剪掉 | **假**：恒速零碰撞；仅领头刹车时 RL 撞 6/10、P+FF 不撞 | 不剪枝，保留 9 格 |
| D_DES 进终止 | 声称进终止 | **假**：终止用绝对 gap（0.12/2.0） | 更正 |

> **lost 窗口随 `d_des` 漂移（review1 I3 遗留，review2 I4 确认 wrapper 路径同样存在）**：
> `terminated` 用绝对 `MAX_GAP=2.0`（`follow_env.py:188`），**不随 `d_des` 变**。
> 故 `d=1.00` 时「lost」要到 gap>2.0（**超出设定点 1.0 m**）才触发 ⇒ 「允许松 1 m」。
> wrapper 路径**未改善也未恶化**（`MAX_GAP` 是独立常量）。
> 当前 7 格 settled 误差均小（≤3.99 true-cm）**无实际影响**，但 `d=1.00` 若出现发散策略
> 会被 2.0 阈值**宽容而不判 lost** ⇒ **审计表须对此显式说明**。

---

## 4. 交付物

| 文件 | 内容 |
|---|---|
| `results/20260922_D0_scenario_audit/` | 扫描原始输出、config、包络图 |
| `docs/scenario_audit.md` | 审计表：分层定位，**如实标注 v=1.0 反转格**，**双口径两列** |
| README 措辞核查 | 「2× 更好」加限定（训练分布内）；**数字换到 obs-cm+settled 口径**；补 A1 去中心化 |
| `fig_D0_envelope_v1.png` | 包络图：色阶 = RL `mean\|e\|`，标记反转格 |

---

## 5. 时间预算

| 项 | 估计 |
|---|---|
| wrapper 实现 + 冻结验证（**Step 0 已完成**） | 0.5 天 |
| 9 格 × {RL, P+FF, P} 全跑（wrapper，settled 口径） | 0.5 天 |
| **D0.2 边界补 seed 30→100** | 0.5 天 |
| 包络图 + 审计表 | 2 天 |
| README 措辞 + 口径核查 + 补 A1 | 1 天 |
| ASSUMPTIONS 预登记 | 0.5 天 |
| **§8 regime-extension 实验** | 1.5 天 |
| **合计** | **~7 天** |

---

## 6. 风险与预案

| 风险 | 概率 | 影响 | 预案 |
|---|---|---|---|
| **RL 在 v=1.0 反转（已实测）** | 已发生 | 头条需限定 | 如实报告；README 限定 + 换口径 |
| **D0.2 贴线（7.98 vs 8.00）** | **已发生** | 判据不可判 | **补 seed 30→100**；越过则判未通过并转 §8，**不放宽阈值** |
| **口径再被混用** | 中 | 数字不可比 | §2.0 声明 settled+obs-cm 为唯一口径，两列同标 |
| 分布外划界被滥用 | 中 | 审计无失败格 | §2.3 先验划界 + §2.4 正值证据 + 预登记 |
| §8 修复失败 | 中 | — | honesty clause（§8.3），报为「量化速度上界」而非「修复失败」 |

---

## 7. 关联与流程

- 输入：`follow_env.py`（**不改，wrapper 覆写**）、`ckpt/follow_stage2_final_v1.zip`
- 假设登记：D0.1–D0.7 + 分布内外划界 **进 `ASSUMPTIONS.md`（评测前）**
- **落点补录（C4）**：`_d0_review3`（评审 R3/R4 引用）在仓库内无落点 ⇒ 须入
  `archive/phaseC_w6_reviewer_scripts/` 同例的 `archive/d0_reviewer_scripts/` 并附 README，
  或由本 v3 的 `_d0_reconcile_conventions.py` **替代其作用**（后者已入库，口径更明确）。
- 流程：本 v3 → **review2** → 修订 → 执行

---

## 8. regime-extension fix experiment（**C2，本 v3 新增**）

> 审计若止于「我们发现反转了」，是半成品。治愈动作 = **把训练分布扩到目标速度域，
> 验证优势是否回来**。本节把 `c0b7b7d` 未落地的 §8 补成实际可执行条款。

### 8.1 假设与设计

**H-D0.5**：RL 在 v=1.0 的劣势源于**训练分布未覆盖该速度域**（非架构性上限）。
⇒ 将 `follow_env` 训练期 `v_set ~ U(0.25, 0.50)` 扩为 **`U(0.25, 1.50)`**（覆盖 AGV 典型区，
经 wrapper/课程，**不改冻结文件**），**5M 步**重训后 `d=0.50 v=1.00` 的 RL 应回升至**不劣于 P+FF**。

> ⚠ **owner 裁决原文**：训练分布扩到 **U(0.25, 1.50)**、**5M 步**、复用 `train_ppo.py`
> 配方（stage1 easy → stage2 full）。**这属于重训**（改了训练分布），走铁律 4
> （更新次数 ≥300）。**是否触发铁律 2 提交 review2 裁定**（不改冻结文件，但训练分布改变）。

**设计**：以 wrapper 注入目标速度域（同 C1 路径），重训一个新 checkpoint
（`ckpt/follow_stage2_v2_speedext.zip`），**与 v1 checkpoint 同 seed 同表对比**。

> **Step 0.5（review2 I6，前置可行性验证）**：`train_ppo.make_env` 在**子进程**里构造
> `FollowEnv`（`SubprocVecEnv`），`v_set` 在**子进程内的 `reset()`** 采样。
> 要把训练期 `v_set ~ U(0.25, 1.50)` 做对，wrapper 必须在**子进程侧**生效——
> 在 fork/spawn 下需专门设计（`if __name__ == "__main__"` 守卫 +
> 子进程可 import 的模块级补丁）。
> **开跑前先做 Step 0.5 冒烟**：单 env 确认扩展分布**真的被采样**（打印 `env.v_set` 分布），
> 未通过不得开跑重训。

### 8.2 预登记阈值（**owner 裁决原文，非交接者起草**，执行前登记）

> 来源：`archive/d0_owner_ruling_text/_d0_wrapper_mandate.py` §8.3（已入库存档）。

| # | 判据 | 阈值 | 依据 |
|---|---|---|---|
| **D0.5** | AGV 区优势恢复 | v=1.0 格 RL `mean\|e\|` **≤ P+FF**（obs-cm） | 反转的直接修复 |
| **D0.6** | 低速档不退化 | v≤0.55 各格 RL `mean\|e\|` 不劣于**原 checkpoint**（允许 ≤10% 放宽） | 防"修好高速、弄坏低速" |
| **D0.7** | 包络图闭合 | 9 格中 **≥7 格** RL 不劣于 P+FF 且零碰撞 | 工作包络 |

> **出处已核对（review2 E）**：`archive/d0_owner_ruling_text/_d0_wrapper_mandate.py`
> §8.3 **逐字含有** D0.5–D0.7（含 D0.6「≤10% 放宽」、D0.7「≥7 格」），
> **D0.5–D0.7 即 owner 裁决原文**，非交接者起草。review2 已核对无篡改、无遗漏。
> （本条旧文本曾误称「为交接者起草」，系入库存档前的陈旧文字，已删。）

### 8.3 honesty clause

若扩展后 D0.5 **仍不成立**，结论**不是**「修复失败」，而是：

> **本平台在 1.0 m/s 存在一个量化速度上界**：在此之下 RL 精度优势成立，
> 之上结构先验（P+FF）占优。该上界本身是 RQ4（域随机化能否预测差距）的正结果。

---

## 待评审确认点（v3）

1. §2.0.1 统一到 **settled + obs-cm** 是否正确？（作者侧 README/plan v2 曾用 whole 未声明；review1 自洽 settled）
2. §2.3 的 **0.55 锚定**如何裁定？（v2 遗留，本 v3 未擅自定死）
3. §2.2 的 **D0.2 贴线处置**（补 seed 30→100，越线即判未通过）是否恰当？
4. §8.1 以 wrapper 注入速度域重训——**是否触发铁律 2**？（不改冻结文件，但改变训练分布 ⇒ 属新实验，须新 RUNLOG 条目）
5. §8.2 的 D0.5–D0.7 **owner 裁决原文**（`_d0_wrapper_mandate.py`）是否可直接生效，还是需 review2 复核数值？
6. §7 的 `_d0_review3` 落点如何处置（补 archive 还是以 `_d0_reconcile_conventions.py` 替代）？
   —— **review2 已裁定**：以 `_d0_reconcile_conventions.py` 替代，充分；并已补 `(0.20,1.00)` 格（I2）。
   **另**：本 v3 §0 引用的 `plan_D0_scenario_audit.md`（v1）**经 `git log --all` 核实亦无落点**，
   与 `_d0_review3` 同为 §5b 缺口，**并案处理**（v1 内容已由 v2/v3 §0 概括，无须单独归档）。
