# D0 plan — 评审后修订（v2，送重审）

日期：2026-09-22
性质：按 `reviews/20260921_D0_review1.md`（Reject，5 拦截级）修订。
关联：`plan_D0_scenario_audit.md`（v1，作废）、`D0_stageA_selfcheck.md`（作者自查，部分被评审证伪）

---

## 0. v1 为何作废（一句话）

v1 的「阶段 A 零样本省钱设计」**技术前提不成立**：
只换测量口径（不改 `D_DES`）测的是**常值偏移**（claim 0.50 → 恒定 30cm「误差」，
与策略能力无关），而正确测量必须改 `D_DES`——改完**零样本直接成立**，
所谓的「贵」根本不存在。且验收阈值有**单位口径未定义**、**算错**、**在 AGV 相关格
RL 必反转**三处硬伤。

---

## 1. 修订后的实验设计（评审 R1/R2 裁决）

### 1.1 正确的做法（评审实测确立）

| 动作 | 是否必须改冻结文件 | 实测结论 |
|---|---|---|
| **改 `D_DES`（0.20 → 0.50/1.00）** | **是**（`follow_env.py` 模块常量） | **必须做**，且是唯一正确测量 |
| 既有 checkpoint 在该改动下**零样本** | — | **直接成立**：d=0.50 → 0.40 真cm、d=1.00 → 0.45 真cm，零碰撞（V2V 先验 + 相对误差归零 ⇒ gap 轴天然泛化） |
| 重训 | — | gap 轴上**可证明多余** |

**⇒ 铁律 2 的正确用法不是"重训"而是"改常量 + 只重评"**。
改 `D_DES` 后须更新 sha256 表（`DATA_MANAGEMENT.md` §8），
但因 **gap 轴零样本成立**，**不触发重训**——评审已实测证明。

> **必须先声明的解释边界**：gap 轴泛化是**架构性**的（相对误差 + V2V 先验），
> **不是**"策略学会了新任务"。**速度轴**则是另一回事（见 §1.2）。

### 1.2 速度轴：**不能零样本，且结果是反转的**

| 格子 | RL（真cm） | P+FF（真cm） | 胜者 |
|---|---|---|---|
| d=0.20 v=0.30（as-trained） | 2.48 | 4.59 | **RL** |
| d=0.20 v=0.50 | 3.61 | 5.18 | **RL** |
| d=0.50 v=0.55 | 2.52 | 4.20 | **RL** |
| **d=0.50 v=1.00（AGV 相关）** | **5.63** | **5.09** | **P+FF** ← 反转 |
| d=0.50 v=1.00 sinusoid | 4.93 | 5.02 | RL（微弱） |
| d=1.00 v=1.00 | 4.74 | 5.21 | RL |

**⇒ 头条结论"RL 2× 优于手工规则"在 AGV 相关的 1.0 m/s 格子上反转。**
这是 **D0 最重要的发现**——它回答了负责人担心的"情景适配性"：
**RL 的精度优势集中在低速区；到 AGV 典型速度边界（1.0 m/s）优势消失。**

**处置（诚实，不洗地）**：
- README/博客的"2× 更好"**必须加限定**："在训练分布的速度区间（≤0.5 m/s）内"；
- 审计表把该格如实标为"**RL 优势消失区间**"；
- **论文 v3 素材**：这张反转图本身比"RL 全胜"更有信息量
  （对应 RQ4 的"域随机化能否预测差距"叙事）。

---

## 2. 验收判据修订（评审 R3/R5）

### 2.1 单位口径（**必须声明，评审 R3**）

```
obs-cm  = 200 × |e| [m]   （仓库既有惯例，所有论文数字口径）
true-cm = 100 × |e| [m]
```

**D0 全部判据一律用 obs-cm**（与既有数字可比），并在所有输出中同时标注两列。

### 2.2 判据（修订后，修正算错与单位）

| # | 判据 | 阈值（obs-cm） | 修订说明 |
|---|---|---|---|
| **D0.1** | d_des=0.50 精度 | RL `mean\|e\|` ≤ **5 cm**（obs-cm；= 0.20 档实测 4.95 的同级） | v1 的 2.5cm 是**算错的 12.5%**（2.5/50=5%），且把口径混了 |
| **D0.2** | v=1.0 精度 | RL ≤ **8 cm** 且**零碰撞**（d≥0.50） | v1 的 5cm **必挂**（实测 11.26 obs-cm）；按实测放宽但**如实记录 RL 在 1.0 反转** |
| **D0.3** | 分布内 vs 分布外**先验划界** | 见 §2.3 | v1 未划界 → 可被挑着用 |
| **D0.4** | 相对基线 | **分布内**格 RL 不劣于 P+FF；**分布外（v=1.0）格如实报告胜者** | v1"所有格不劣"在 v=1.0 必挂且未预见 |

### 2.3 分布内/外先验划界（评审 R5 裁决）

```
训练分布：v_set ~ U(0.25, 0.50)，d_des = 0.20（follow_env.py:115/41）
先验划界（训练前定死，进 ASSUMPTIONS.md）：
  分布内格：v ≤ 0.55 且 d_des ∈ {0.20, 0.50, 1.00}
  分布外格：v = 1.00（速度超出训练分布）
  v=1.0 格子的"反转"是分布外行为，如实报告——既不是失败，也不算通过
  D0.3 落地 = 阶段 B 范围先验划定为"分布内格输出正值证据"
```

### 2.4 评审 R5 的补救（防"不能输判据"）

- **至少 3 个 AGV 相关格**（0.50×0.55、0.50×1.00、1.00×1.00）须**输出正值证据**
  （≤阈值且零碰撞）才算审计产出；
- 每个失效格给出**量化重训成本上限**（估算 train 步数）；
- 全部**进 `research/ASSUMPTIONS.md` 预登记**，不许跑完再划范围。

---

## 3. 事实更正（评审 R1/R2 附带，作者接受）

| 项 | v1 的说法 | 评审实测 | 修订 |
|---|---|---|---|
| 刹车距离 | v²/2a（0.30→3cm，1.0→33cm） | **正确**（`Car.step` 速度通道是摆率限幅，无 τ） | 保留 |
| 1.0×0.20 物理必撞 | 剪掉该格 | **假**：恒速下零碰撞；仅领头刹车时 RL 撞 6/10、P+FF 不撞 | **不剪枝，保留 9 格** |
| D_DES 进终止 | v1 声称进终止 | **假**：终止用绝对 gap（0.12/2.0），`D_DES` 不进终止 | 更正 |

---

## 4. 交付物（不变，但包络图改为**双向信息**）

| 文件 | 内容 |
|---|---|
| `results/20260922_D0_scenario_audit/` | 扫描原始输出、config、包络图 |
| `docs/scenario_audit.md` | 审计表：分层定位（真实映射/缩放后成立/仅测试场），**如实标注 v=1.0 反转格** |
| README 措辞核查 | **"2× 更好"加限定（训练分布内）**；补 A1 去中心化 |
| `fig_D0_envelope_v1.png` | 包络图：色阶 = RL `mean|e|`，**标记反转格** |

---

## 5. 时间预算（1 周，含修正）

| 项 | 估计 |
|---|---|
| 改 `D_DES`（冻结文件，更新 sha256，**只重评不重训**） | 0.5 天 |
| 9 格 × {RL, P+FF, P} 全跑（monkeypatch v_set） | 0.5 天 |
| 包络图 + 审计表 | 2 天 |
| README 措辞核查 + 补 A1 | 1 天 |
| ASSUMPTIONS 预登记 + 阶段 B 范围界定 | 0.5 天 |
| **合计** | **~4.5 天** |

---

## 6. 风险与预案（补 R4 反转、去 v1 漏项）

| 风险 | 概率 | 影响 | 预案 |
|---|---|---|---|
| **RL 在 v=1.0 反转（已实测确认）** | **已发生** | 头条结论需限定 | **如实报告**；README 加限定；这正是审计要暴露的真相 |
| 阶段 B 若被误触发（gap 轴无需重训） | 低 | 浪费 | 评审已证 gap 轴零样本成立，**不重训** |
| 分布外划界被滥用 | 中 | 审计无失败格 | §2.3 先验划界 + §2.4 正值证据要求 + 预登记 |
| 阈值口径混用 | 中 | 判据不可判 | §2.1 声明 obs-cm 为唯一口径，两列同标 |

---

## 7. 关联与流程

- 输入：`follow_env.py`（**改 `D_DES`，铁律 2 流程**）、`ckpt/follow_stage2_final_v1.zip`
- 假设登记：D0.1–D0.4 + 分布内外划界 **进 `ASSUMPTIONS.md`（评测前）**
- 流程：本 v2 → **重审（review2）** → 修订 → 执行

---

## 待评审确认点（v2）

1. **§1.1 的"改 `D_DES` + 只重评不重训"**是否符合铁律 2 的精神？
   （评审说 gap 轴零样本成立、无需重训——是否应把这条正式写进铁律 2 的例外条款？）
2. **§2.2 D0.2 的 8 cm（obs-cm）**是否还是太严（实测 11.26）？
   是否应改为"如实报告反转 + 记录数值"，而非设一个可能又挂的阈值？
3. **§2.3 的分布内外划界**（v≤0.55 分布内）是否合理？0.55 从何而来？
4. **§3 的"1.0×0.20 不剪枝"**——评审说恒速零碰撞，但那格 RL 撞 6/10（领头刹车时）。
   是否应记录"该格是刹车场景的失败"而非跑全？
5. **包络图的双向信息**（精度 + 反转标记）是否是 D0 最恰当的呈现？
