# 评审记录 — Phase F 正例锚点（`positive pole`）：第 12 轮（复审）

- **日期**：2026-09-25
- **对象**：修订提交 `f465485`（H-A3 → H-A3' 五条合取；正例机制陈述更正）
  ——文件 `plan_phaseF_adversary_v1.md` §2.3/§2.3.1/§3/§4、`research/ASSUMPTIONS.md` Phase F 块、
  `RUNLOG.md` 末两条、`results/20260925_phaseF_positive/positive_pole.py`
- **前置**：`reviews/20260925_phaseF_positive_review1.md`（第 11 轮 Reject）
- **评审方式**：逐条**实测**。新增 5 个探针（`_review2_probe_{A,B,D,E,F}.py`），
  全部**只读**冻结文件，仅在运行期 monkeypatch `fe.D_DES`（`finally` 复位）。
  **未修改任何冻结文件**（sha256 见 §四）。
- **机器状态披露（F8）**：每次跑前 `pgrep -af 'uv run python'` 确认为空；
  5 次探针运行期间未观察到并发进程。本轮全部指标是**确定性事件计数**
  （撞车数、稳态误差、支路占用率）与**固定 seed 下的确定量**，
  **不是性能/耗时基准**，故 `measure-performance-in-isolation` 陷阱不适用。
- **指标口径**：沿用 `obs[0] = e/0.5`，即「obs-cm」`= 200 × |e|[m]`，与 `_out_pole_final.txt` 直接可比。
  **B 组实例定义完全沿用 review1 的 `_review_probe_round6_lite.py`**
  （20 局/项，env seed `60000+k`，attack seed `50000+k`），另在 §E 增测两个额外攻击种子块与
  一个替代 env-seed 约定。

---

## 裁决：**Reject（拦截级 2 条）——但 R2 已充分回应，正例本身本轮通过；需返工的是 H-A3'(b)(c)(e) 的口径**

**修订确实回应了 review1 的两条**（见 §三）：R2（机制陈述）三处已一致更正且与代码行为相符；
R1（撞车率被零动作满分通过）被识别并改写成五条合取，方向正确。
**但 owner 指定的核心问题——"改写后的五条合取是否还留有退化通道"——实测答案是「是」**，
且通道的成因与 R1 同型（§10.5c），只是上移了一层：

- **(e) 的锚用错了量**：门写的是「正例 1.5×」，代入的却是正例的**名义**误差 2.19 ⇒ 门 3.28；
  而正例**自己在 B 组的** pooled settled 误差是 **3.99**（逐项 B1 6.17 / B2 3.35 / B3 3.72 /
  B4 3.14 / B5 3.55，**只有 B4 过 3.28**）。**锚点过不了自己定义的门。**
- **(e) 修正到正确锚（1.5×3.99 = 5.98）后，失去分辨力**：`P+FF`（4.23）、
  `clamp only`（4.23）、`freeze gap<0.40`（4.24）、`freeze-envelope`（3.55–5.48）**全部通过**。
- **根因（新发现）**：**B 组不含正例与 P+FF 的唯一分离族**。正例相对 P+FF 的全部优势集中在
  `step-down 0.1/0.2/0.4 s`（正例 **0/220**、P+FF **60/220**，本轮复测 0/60 vs 60/60），
  而 B1 是 0.3/0.7 s、B2 是 0.3/0.8 Hz——**B 组里正例与 P+FF 打平**
  （撞车 0/100 vs 3/100，误差 3.99 vs 4.23）。⇒ (b)(c)(e) 三条都在 B 组上取值，
  **却被一条已知在硬攻击下 60/60 崩溃、且不含任何抗扰逻辑的固定规则（P+FF）通过**。

> **一句话**：R1 指出"零动作能过撞车率列"；本轮实测显示**修好撞车率列之后，
> 过门者换成了 P+FF——一条抗扰能力已被证否的固定规则**。判据仍与目标脱钩。

---

## 一、拦截级问题

### F1 ★★ (e) 的门锚错了量：正例过不了自己的门；改成正确锚后又不排除退化

**(e) 原文**：`B 组 settled 误差 ≤ 正例的 1.5×（正例 d=0.2 为 2.19 obs-cm ⇒ 门 ≤ 3.28）`。

**问题**：分子是 **B 组**量，分母代入的却是正例的**名义**量。两个读数都不成立：

**读数 1（按字面 3.28）——锚点自身不合格。**

```
A  H-A3'(e) SEPARATION TEST -- B-group settled error, d=0.20
   (env seeds 60000+k, attack seeds 50000+k, 20 ep/item)
                       policy     B1     B2     B3     B4     B5    TOTAL  errB_pooled  nom_err
                pole (anchor)    0/20   0/20   0/20   0/20   0/20     0/100        3.99     2.24

  per-item: does the pole pass its OWN gate on every B item?
    B1: pole   6.17  vs gate 3.28 -> FAIL
    B2: pole   3.35  vs gate 3.28 -> FAIL
    B3: pole   3.72  vs gate 3.28 -> FAIL
    B4: pole   3.14  vs gate 3.28 -> PASS
    B5: pole   3.55  vs gate 3.28 -> FAIL
```

正例**五项里四项不过**。plan §4 附加验收要求「H-A4 同样套在正例上」，
即正例被当作判据的**正实例**；(e) 却连正例自己都排除。
这与 `criterion-must-have-a-positive-pole` 是同一形状：**判据缺正极点**。
另外 3.28 = 正例 B 组值 3.99 的 **0.82×**——名义上"1.5× 正例"，
实质是"训练后方必须比手工锚点**好 18%**"，与文本意图（宽容 1.5×）相反。

**读数 2（按意图 1.5×3.99 = 5.98）——不排除退化。**

```
D2  THE FIVE-CLAUSE CHECK (baseline = frozen v1 RL 36/100; (e) gate = 1.5 x pole's B-group err = 5.98)
                          policy   (b)<=10%  (c)>=50%  (e)<=5.98  pooled   VERDICT
                   pole (anchor)     0/100      True      True     3.99      PASS
       catch-then-freeze |e|<.02     4/100      True      True     7.39      fail
       catch-then-freeze gap<.25     3/100      True      True    11.82      fail
     freeze-envelope (brake->a0)     2/100      True      True     3.55      PASS
                     zero action     0/100      True      True   106.93      fail
                            P+FF     3/100      True      True     4.23      PASS
```

`freeze-envelope`（把正例的**全力刹车换成 `a=0`**，即**完全没有刹车能力**）与 `P+FF` 都过。
**且注意 (c) 对所有候选都打 True**——因为基线 36/100 使门槛降到 ≤18/100，
连**零动作**（0/100）都满足 (c)。**(c) 在本口径下无分辨力**（同理 (b) 对一切"不撞"的策略无分辨力）。

**两读数都不成立** ⇒ (e) 作为承重条款**当前不可用**。

### F2 ★★ B 组不含"正例 vs P+FF"的分离族 ⇒ (b)(c)(e) 被 P+FF 通过

**构造 + 实测**：`P+FF` 是一条固定比例-前馈规则，**无任何抗扰逻辑**，
且**已被实测证否**：在 `step-down 0.1/0.2/0.4 s` 上 **60/60 崩溃**（本轮 B5 复测，
与 review1 的 60/220 同族）。

```
A   P+FF 在 B 组：  crash  3/100（B1 3/20，其余 0）   errB_pooled 4.23
B5  P+FF 在硬族：   step-down 0.1s 20/20、0.2s 20/20、0.4s 20/20   total 60/60
    pole 在硬族：   step-down 0.1s  0/20、0.2s  0/20、0.4s  0/20   total  0/60
```

⇒ **正例相对 P+FF 的全部可分辨性，集中在一个 B 组里不存在的族**。
B 组里两者打平（撞车 0 vs 3/100、误差 3.99 vs 4.23）。
因此凡在 B 组上取值的合格条款，都挡不住"一条不具备抗扰能力的固定规则"。
这是 **§10.5c 的同型复发**（"被判据测量的量与任务成功脱钩"），
只是从"零动作过撞车率列"上移成"P+FF 过撞车率列 + 跟踪质量列"。

**为什么 H-A2 不能替代**：H-A2 的对手是**尚未训练**的冻结攻击者（Step 1 的产物），
在本轮**不存在**，无法作退化通道的对照。且若攻击者只对 P+FF/v1 训练成功，
一条在 B 组上"看起来合格"的弱策略仍可能幸存——本轮的 P+FF 就是该情形的先验实例。

**建议修正（两条，改动很小）**：

```
(e′) 锚到同一批 B 实例上的正例值：settled 误差 ≤ 1.5 × 正例 pooled（实测 3.99 ⇒ 门 ≤ 5.98）；
     并明确口径：逐项 + pooled 都报、env-seed 约定、"domain_randomize=False、gap_max=1.2" 钉死。
(f)  硬族条款（新增，承重）：把 step-down 0.1/0.2/0.4 s 补入 B（或把 (b)(c)(e) 的判定域
     改为 B ∪ 硬族）。理由：这是正例与 P+FF 的**唯一**分离族（0/220 vs 60/220）；
     缺它，(b)(c)(e) 拦不住 P+FF。
```

> 可选替代：若不愿扩 B，则须给 (b)(c)(e) **改判据为"相对 P+FF 的增量"**
> （如"在正例 0/220 而 P+FF 60/220 的族上，训练后方 ≤ 1/3 正例"）。
> 但本评审倾向 (f)——直接补族最省事、且与 §2.4"B 覆盖更宽频率与波形"的初衷一致。

**附带**：(c) 还需与基线噪声带一并重估。本轮实测基线的波动**比公示的更宽**：
见 §二.E（三个攻击种子块下 v1 为 **34 / 36 / 40**，换 env-seed 约定又成 **41 / 42**）。

---

## 二、重要问题

### A. 核心问题分项作答（owner 指定）

**问：正例自己在 B 组的误差是多少？门能否分开？**
答：正例 B 组 pooled **3.99**（逐项 6.17 / 3.35 / 3.72 / 3.14 / 3.55）。门 3.28 分不开——**锚点自己不过**。
改成 5.98 后能分开 `保平安` 族与 `catch-then-freeze` 族，但**分不开 P+FF**（4.23 < 5.98）。

**问：还有其他平凡策略能过全部五条？**
答：实测穷举了四个家族，结果见下（**(e) 门取"字面 3.28 / 修正 5.98"两读**）：

| 退化策略 | 撞车 | B 组 pooled err | H-A4 名义 | 过 (e)@3.28 | 过 (e)@5.98 | 判定 |
|---|---|---|---|---|---|---|
| 零动作 | 0/100 | 106.93 | 113.52 | 否 | 否 | **被排除** ✅ |
| 恒低速 `kp=0.15` | 0/100 | 22.67 | 22.58 | 否 | 否 | **被排除** ✅ |
| 大间距 `target 0.50` | 0/100 | 60.58 | 60.01 | 否 | 否 | **被排除** ✅ |
| `catch-then-freeze \|e\|<.02` | 4/100 | 7.39 | 3.80 | 否 | 否 | **被排除** ✅ （名义 3.80 卡在 1.5×2.54=3.81 边缘，H-A4 险过） |
| `catch-then-freeze gap<.25` | 3/100 | 11.82 | 9.49 | 否 | 否 | **被排除** ✅ |
| `smart freeze dv>0.1` | 3/100 | 4.23 | 7.01 | 否 | **是** | 修正锚泄漏 |
| `freeze gap<0.40 c>0.20` | 3/100 | 4.24 | 2.59 | 否 | **是** | 修正锚泄漏 |
| `freeze-envelope`（刹车→a=0） | 2/100 | 3.55–5.48 | 2.54 | 否 | **是**（3 块全过） | 修正锚泄漏 |
| `P+FF`（无抗扰逻辑） | 3/100 | 4.23 | 2.54 | 否 | **是** | **F2 通道** |

**结论**：(e) 确实排除了「零动作」「恒低速」「大间距」「catch-then-freeze」——
**plan 关于 (e) 排除"保平安型"的说法成立**；但**恰恰排除不了"一条已知会崩的固定规则（P+FF）"**，
而 P+FF 正是"不体现抗扰能力"的最强候选（它连 H-A2 的靶子都打不过）。
⇒ **退化通道仍在，且 F2 是根因。**

### B. R2 的回应是否充分：**是**（三处一致，与代码相符；1 处小 nit）

实测三处（`plan §4` / `positive_pole.py` docstring / `RUNLOG`）**表述彼此一致**，
且与代码实际行为一致（`if -de > c_max: return [-1.0]`，其余为 `clip(kp*e/ACT, -1, 1)`）：

| 位置 | 现行表述 | 与代码一致？ |
|---|---|---|
| `plan_phaseF_adversary_v1.md` §4（269–281 行） | 「P+FF + 按【达成】收距率与间隙相关包络比较就全力刹车；`ythresh/yg/ymax/recover` 四个常量是**无效参数**；`clamp only` 60/220 崩、`brake only` 0/220 过」 | ✅ |
| `positive_pole.py` docstring（54–72 行） | 「the anticipate half is DEAD CODE … DEAD PARAMETERS: YTHRESH, YG, YMAX, RECOVER never bind … do not cite them as mechanism」 | ✅ |
| `RUNLOG.md`（1426–1431 行） | 「四个常量…全部无效（死代码）… 不是"预判-避让"，而是『P+FF + …全力刹车』… 消融定论 60/220 / 0/220」 | ✅ |

**我同意"代码无需改、只改文档"**：(a) 代码的可复现性已由 review1 逐字节确认；
(b) 四个常量留着使 review1 的消融仍可逐行对照（提交信息已说明此意图）；(c) 删常量会破坏来源链。
**但注意**：提交信息里"标记为无效参数而非删除"这一处置，**只对 `positive_pole.py` 与 RUNLOG 生效**；
plan §4 的表述是"无效参数"，措辞一致，可接受。

**小 nit（不拦截）**：
1. `RUNLOG.md:1459` 仍留着「**约束是收距速率**」这一**不完整**表述
   （review1 §R2 结论要求的准确形式是"约束是**达成**收距速率的**上界**，且实现是**全力刹车**而非命令夹持"）。
   紧邻的 1465 行已给出正确形式，但 1459 行单独读仍可被误引。建议补 4 个字："**达成**收距速率"。
2. `results/20260925_phaseF_positive/positive_pole_search.py` 与 `positive_pole_v2.py` 的 docstring
   仍称 `"anticipate and yield"`。二者是**被取代的历史候选集**（search.py 里确实含
   `YieldStateful` 这类"预判"候选），**不是对锁定正例的描述**，故不算残留错误陈述；
   建议各加一行"superseded by positive_pole.py（锁定版无预判支路）"以免误引。
3. `grep` 确认 `plan`/`ASSUMPTIONS`/`RUNLOG` 无旧陈述残留（除上述 1459 行）。

### C. H-A3' 的可操作性

- **(a) 成对攻击实例：可实施。** 本轮探针就做到了——同一 `b_fn(kind, seed)`、
  同一 `env.reset(seed=...)`，先把血缘策略、再把训练后策略各跑一遍。
  **但 plan 未钉死两个必要前提**：`domain_randomize=False` 与 `gap_max=1.2`
  （二者改变 `gap0` 的分布，会使"同一 seed"不再是同一实例）。
  **须在 (a) 中写死**，否则成对性名存实亡。另须固定 env-seed 约定（见 E）。
- **(c) 基线 seed 波动带：有现成测量方案，但 plan 公示的带偏窄。**
  本轮实测：

  ```
  E1  v1 RL on B -- env 跨项连续 60000..60099（review1 T3 口径）
      attack seeds 50000+ -> 36/100     77000+ -> 40/100     91000+ -> 34/100
  C   每项各自重置 env seed（60000+k 每项）
      attack seeds 50000+ -> 41/100     77000+ -> 42/100
  ```
  plan/(c) 只公示 **36→40**。**实为 34–42，取决于攻击种子块与 env-seed 约定**。
  建议：(c) 增列 **≥3 个攻击种子块**的基线值 + **明确 env-seed 约定**（建议跨项连续），
  并把"±2 点噪声"改为实测的"±4–8 点"。
- **(b)(e)：可实施**，但见 F1/F2——口径须先改。

### D. §2.3.1 两个陷阱的记录与措辞

- **(i) d=1.0 执行器受限死格：如实记录，机制准确，措辞有一处可挑剔。**
  本轮复现（`E2`）：`d=1.0 / step-down 0.2 s`，正例 20 局均值 err **78.28**、
  P+FF **78.28**、mean gap **0.609**（与 plan 引用的 review1 值一致）。
  **⚠ 措辞**：plan 写「settled err **78.28** … **P+FF 给出逐位相同的 78.65**」——
  两个数**不是同一口径**：78.28 是 20 局均值，78.65 是单局（seed 2000）。
  句子并列会读成矛盾。建议改为「P+FF 同格 20 局均值**亦为 78.28**（单局 seed 2000 两者同为 78.65）」。
- **(ii) B 组非泛化证据：如实记录且准确。** B2 的 {0.3, 0.8} Hz 确为正例验证字典
  （`positive_pole.py` 的 `probes` 含 `osc 0.3Hz`/`osc 0.8Hz`）；B1 周期 {0.3, 0.7} 为
  正例 {0.2, 0.4, 0.8, 1.6} 的插值。措辞"同分布确认 ≠ 泛化证据"准确。

### E. 数字一致性（抽查复现两项以上）

| 数字 | 出处 | 复现 | 结论 |
|---|---|---|---|
| 零动作 B 组 0/100 | plan §3 / ASSUMPTIONS | ✅ 本轮 `A` 独立复现 0/100 | 一致 |
| 零动作名义 83.22 | plan / ASSUMPTIONS "83–110" | ✅ 单局 seed 2000 = **83.22**；B 组逐项 104.19–110.55 ⇒ "83–110" 成立 | 一致 |
| 正例名义 2.19 | plan §3(e) | ✅ 单局 = **2.19**；20 局均值 = 2.24（归档） | 一致（两个口径都对，但 plan 未标注） |
| P+FF 2.49 / 2.54 | review1 2.49；RUNLOG 2.54 | ✅ 单局 2.49、20 局均值 2.54 | 一致 |
| 基线 36 → 40 | plan §3(c) | ✅ 连续 env-seed 口径下**逐值复现** 36/100、40/100；**第三个块 34/100** | 一致但带偏窄 |
| d=1.0 死格 78.28 / 78.65 | plan §2.3.1(i) | ✅ 20 局均值 78.28（正例与 P+FF 同值）；单局 78.65 见 review1 T2 | 一致（口径未标注） |

**未复现项（如实披露）**：`40.41`（step-down 0.1 s）与 `22.71`（0.4 s）两个 d=1.0 格
未逐格复跑；本轮只复现了同族的 0.2 s 格（78.28）。因三者同属"指令摆动快于执行器带宽"
一族，且归档 `_out_pole_final.txt` 与本次复现的 0.2 s 格逐值吻合，**认其可信**，
但在论文引用前建议随 Step 3.5 一并重跑。

### F. 新发现：**B 组的参数范围是 Step 0.5 训练分布的**子集**

`results/20260925_phaseF_step05/step05_train.py` 的 `RandomWaveLeader` 生成器：
方波周期 `U(0.2, 0.8)`、正弦 `f ~ U(0.1, 0.8)`、分段常值段长 `U(0.2, 0.8)`、
chirp `0.1 + 0.15·min(t,4)`（即 0.1→0.7 Hz）。**B 组参数全部落在这些区间内**：

- B1 周期 {0.3, 0.7} ⊂ [0.2, 0.8]；
- B2 `f ∈ {0.3, 0.8}` ⊂ [0.1, 0.8]（**0.8 落在上界**）；
- B3 chirp 0.1→0.6 ⊂ 0.1→0.7；
- B4 段长 `U(0.2, 0.8)` 与 Step 0.5 **逐参数相同**；
- 只有 B5（双脉冲）是 Step 0.5 没有的族。

⇒ **§2.4 "B 覆盖更宽的频率与波形"这一主张，相对 Step 0.5 的随机波形攻击者并不成立**——
B 基本是该分布的一个**子采样**。
Step 0.5 是探针、不产出结论，**但 plan §6 的 Step 2 若复用/沿用该生成器，B 就不是 held-out**。
**建议**：在 §2.4 显式加一条硬约束——"Step 2 的训练对手不得包含脚本随机波形族，
或 B 组须重新抽样到 `period<0.2 s` 或 `f>0.8 Hz` 区间"。
（这条与 F2 相互独立：即使扩到硬族，也须确保硬族不在训练分布内。）

---

## 三、确认正确的事项（逐条实测）

| 项 | 结论 | 实测证据 |
|---|---|---|
| **R2 已被充分回应** | ✅ 三处文档彼此一致、与代码一致；四个常量已标无效 | §二.B 表；`grep` 无残留（除 `RUNLOG:1459` 的措辞 nit） |
| **"只改文档不改代码"处置** | ✅ 同意 | 提交信息理由成立；review1 已确认代码逐字节可复现 |
| **正例仍在硬族上 0 崩溃** | ✅ 硬族 0/60（review1 为 0/220） | `B5`：step-down 0.1/0.2/0.4 s 各 0/20 |
| **P+FF 仍在硬族上 60 崩** | ✅ 硬族 60/60 | `B5`：各 20/20 —— 分离族确认存在 |
| **(e) 确实排除「保平安型」** | ✅ 恒低速 22.67、大间距 60.58、零动作 106.93 全部超门 | `A` 表 |
| **(e) 确实排除「catch-then-freeze」** | ✅ 7.39 / 11.82，超任意锚值 | `D` 表 |
| **真"遇扰冻结"支路确实会触发**（非空规则） | ✅ 占步 10.09% / 13.89% / 14.19% | `B2` 支路占用 |
| **§2.3.1 两个陷阱如实记录** | ✅ 见 §二.D（一处口径措辞待改） | `E2`；`holdout_B.md` vs `positive_pole.py` |
| **数字可追溯** | ✅ 六项抽查全部复现（见 §二.E） | 五份 `_review2_out_*.txt` |
| **冻结文件未动** | ✅ **5 个 sha256 与 `DATA_MANAGEMENT.md §8` 逐字节相同** | 见 §四 |
| **落点纪律（§5b）** | ✅ 正例 16 个文件已入版本库；**本轮 5 探针 + 5 输出待随本评审提交** | `git status --short` 现仅列本轮新增（见 §四） |

---

## 四、机器状态与冻结披露（F8）

```
sha256sum car_following_sim.py follow_env.py overtake_env.py train_ppo.py train_ot.py
bfb1678d7a5bb10c60632b54ee77d3d357ecce6b21a4c3f0384ecca9824bec89  car_following_sim.py
941c447b969c2fa1f153ed57219235bc028f5ffa9e4052d341f92c97f91e4e2e  follow_env.py
78ee12dd9d5cc6b874f5f530cd6808c7d8895d1aa6677a79559fab1423526b98  overtake_env.py
70136c0b761949662bba9ae8d4ceb246a390b7533a86bdf4cb5f21059aef38b9  train_ppo.py
8197daa0c20d469fe8b3aae48466e54f59c0790b647f3b6d8cd933aa0c28f4d1  train_ot.py
```
——**与 `DATA_MANAGEMENT.md §8` 表逐字节相同，铁律 2 未触碰。**
`fe.D_DES` 在运行期 monkeypatch，`finally` 复位；未修改任何冻结文件。

每跑前 `pgrep -af 'uv run python'` 为空；本轮 5 次探针运行期间未观察到并发进程。
本产物**不包含**性能/耗时基准。

`git status --short`（本轮新增，**待随本评审提交**）：
```
?? results/20260925_phaseF_positive/_review2_probe_{A,B,D,E,F}.py
?? results/20260925_phaseF_positive/_review2_out_{A,B,D,E,F}.txt
```

---

## 五、给 owner 的修订清单（按优先级）

1. **【拦截级 F1】改 (e) 的锚**：`≤ 1.5 × 正例在同一批 B 实例上的 pooled settled 误差`
   （实测 **3.99 ⇒ 门 5.98**），并写明"逐项与 pooled 都报"。
   **删除"2.19 ⇒ 3.28"**（名义量错配），或显式声明用的是名义量并解释为何正例自己允许不过门。
2. **【拦截级 F2】补硬族**：把 `step-down 0.1/0.2/0.4 s` 补入 B，
   或把 (b)(c)(e) 的判定域改为 `B ∪ 硬族`。这是正例与 P+FF 的**唯一**分离族
   （0/220 vs 60/220）；缺它，五条合取被 P+FF 通过。
3. **【重要】(c) 的基线带与约定**：公示 ≥3 个攻击种子块的基线值（实测 **34 / 36 / 40**，
   加上 env-seed 约定后 **41 / 42**）；**钉死 env-seed 约定**（建议跨项连续 60000..60099）；
   把"±2 点噪声"更正为实测的"±4–8 点"。
4. **【重要】(a) 钉死成对性的前提**：`domain_randomize=False` 与 `gap_max=1.2` 须写入 (a)，
   否则"同一 seed"不是同一实例。
5. **【重要】§2.4 加硬约束（F）**：Step 2 的训练对手不得复用 Step 0.5 的脚本随机波形族，
   或 B 组重抽样到 `period < 0.2 s / f > 0.8 Hz`；否则"held-out"名不副实。
6. **【小】措辞**：`RUNLOG:1459` 补"**达成**"二字；`plan §2.3.1(i)` 的 `78.28 / 78.65`
   标注口径（20 局均值 / 单局）；`positive_pole_search.py`、`positive_pole_v2.py` 各加一行
   "superseded" 指针。

---

## 六、附：本轮新增探针与原始输出

| 脚本 | 输出 | 内容 |
|---|---|---|
| `_review2_probe_A.py` | `_review2_out_A.txt` | **A：H-A3'(e) 分离检验**（11 策略 × B 组 5 项 × 20 局；逐项/池化误差；mean gap；逐项过门判定） |
| `_review2_probe_B.py` | `_review2_out_B.txt` | review1 口径复现（2.19 / 83.22 / 2.49）；**冻结支路占用**；**三个攻击种子块**；硬族 60/60 |
| `_review2_probe_D.py` | `_review2_out_D.txt` | **"追上即停"退化族**（catch-then-freeze / freeze-envelope / 零动作）+ **五条合取逐条判定** + 冻结 v1 基线 |
| `_review2_probe_E.py` | `_review2_out_E.txt` | **基线 seed 波动带**（连续 env-seed 约定：36 / 40）；**d=1.0 死格** 78.28 复现 |
| `_review2_probe_F.py` | `_review2_out_F.txt` | **排名反转复核**（三块 × 4 策略：`freeze-envelope` 与正例的逐项/池化对比） |

（原 `_review2_probe_C.py` 与 D 重复，已删除，未入版本库。）
以上脚本全部**只读**冻结文件；`git status --short` 对本轮的改动仅为 §四所列新增 untracked 文件。
