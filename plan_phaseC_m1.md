# Plan — Phase C M1: 对封堵者的学习（blocker benchmark）

日期：2026-09-21
对象：`results/20260921_phaseC_m1/`
关联：`W6_criterion_v5_established.md`（判据与阈值来源）、`F23_transient_window.md`、
      `ERRATUM3_criterion_v234.md`（F20）、`DATA_MANAGEMENT.md` §10.4/§10.5b
上游评审：`reviews/20260921_W6_criterion_review2.md`（Reject → 已修复）

---

## 0. 研究问题（**引言位，承接 F20/F23**）

F20 证明：**无对抗时，"能否学会超车"在本赛道不可测**（几何占优，四版判据均被两行脚本击穿）。
F23 给出**已知标准解法**：利用封堵者横移的 0.61–0.76 s 窗口切入（反制脚本完成率 **0.78**）。

> **M1 的问题因此是**：
> **"RL 能否学出手写反制策略（在封堵者位于外圈时切入），或发现更好的东西？"**
>
> ⚠️ **"独立发现"主张已下调**（评审 §二-1）：封堵者是 `(gap,delta)` 的**确定性函数**，
> 而 `gap/delta` 都在 follower 观测里 ⇒ 策略可**解析反推**其换道命令。
> 故只能主张"**学出等价于已知规则的策略**"，**不主张**发现了脚本未知的机制。

反制脚本是**手写下限**；RL 的目标是**逼近并可能超越**它。
若策略自发出现佯攻/诱导类行为，那是**超越脚本的涌现**，属 RQ3 素材（W3 判据）。

---

## 1. 环境与对手（**封堵者基准，归档为可引用资产**）

### 1.1 封堵者基准（参数即资产）

| 参数 | 值 | 说明 |
|---|---|---|
| 巡航速度 `cruise_v` | **0.55 m/s** | 与 follower 竞争力相当 |
| 防守带 `guard_lo, guard_hi` | **0.08, 0.90 m** | follower 进入即占内圈拒止 |
| 到达容差 `ARRIVE_TOL` | 0.03 m | 物理占据的判定 |
| 横移瞬态（实测） | **0.61–0.76 s** | 承诺窗口的物理来源（F23） |
| 冷却 `SWITCH_COOLDOWN` | 1.5 s | 换道最小间隔 |

**命名**：`blocker@v1 (cruise=0.55, guard=0.08–0.90)`。
**该基准即为论文 Phase C 节的 "blocker benchmark"**——一个可复用的对抗性测试床。

### 1.2 ★防泄漏声明（评审清单 #2）

**三条硬约束**：

1. **评估对手 = 训练对手**：同一封堵者实例、同一参数，**无 train/eval gap**；
2. **观测不含特权信息**：策略**只能**看到**物理可观测**量——
   对手的**车道与横向位置**（可见）**可以**进观测；
   对手的**内部控制状态（冷却计时）不得**进观测
   （那是 controller 内部量，非物理可观测量）。
3. **不得读取反制脚本的任何输出**：反制脚本仅用于①产生正例阈值 ②事后轨迹对照。

**观测空间设计**（对 §10.5b(b)"继承 env 原语"的落实）：

```
沿用 6 维（与冻结 env 语义一致，§3 对照表见下）
  slot0  delta_self/2.5      自视角有符号进度差
  slot1  v_self/V_MAX
  slot2  v_other/V_MAX
  slot3  e_lat_self/0.25
  slot4  lane_self
  slot5  clearance/2.5
追加 2 维（物理可观测，且**解决任务所必需**）：
  slot6  lane_other          对手当前物理车道 0/1
  slot7  e_lat_other/0.25    对手横向偏移  ← 横移瞬态的物理表征（F23 窗口可见）
共 8 维
```

> **为何 slot7 不构成泄漏**：对手的**横向位置**是相机/V2V 可观测的物理量；
> F23 已证明**窗口由横移定义**，而横移**就是**横向位置的变化——
> 策略看到的是"对手正在横移"这一**物理事实**，不是"对手的冷却还剩多少"这一**内部状态**。
> **后者不可见、不可得**，因此策略必须**从物理表征推断窗口**，
> 这正是 M1 要检验的"独立发现"能力。

---

## 2. 假设登记（**训练前写死**，评审清单 #1）

| # | 假设 | 判定阈值（定死） | 依据 |
|---|---|---|---|
| **M1.1** | 学出的策略**至少匹敌手写解法** | 完成率 ≥ **0.55**（= 反制脚本 0.78 × 70%） | `W6_criterion_v5_established.md` §4 |
| **M1.2** | 策略**维持正例级安全** | 碰撞率 ≤ **上置信界**（先补测反制 50 seed，取 均值+2SE；当前估计 ~0.19） | 评审 §二-2：原 0.25 是**单次抽样 max**，正例自身有 38% 概率超它 |
| **M1.3** | 策略的**时机能力是可消融的因**（因果判据，替代已作废的命中率口径） | **消融时机能力后完成率显著下降**：将策略的"封堵者在外圈时才切入"能力替换为盲切（`lane_b==0` 即切）后，完成率须从 ≥0.55 降至 ≤0.35（正例实测 0.77 → 0.30，Δ≈0.47） | 评审 R2-c 建议的因果判据；命中率口径已作废（见 ERRATUM4） |
| **M1.4** | 训练**未发散/未塌缩** | `train/std` 末 10 次 > 0.05 且不单调趋 0；`value_loss` 非 NaN 且末段不单调增长 | 沿用 W5 P1（含 value_loss 子句） |

### 2.1 M1.3 的因果判据（**两次更正后确立**）

> **本节曾两次写错，如实留档**（详见 `ERRATUM4_f23_correction_wrong.md`）：
>
> 1. **初稿**：命中率 ≥ 0.60（**拍的**，且高于正例自身成绩）；
> 2. **第一次更正**：改为"内圈再入瞬态"口径，报 lift 6.45×——
>    **实为改标签未改测量**（新旧口径对 202 次切入分类逐位相同），
>    且 lift **完全**由 t≈0.45 s 的开局切入构成（由 `gap0~U(0.40,0.90)`
>    而 `guard_hi=0.90` ⇒ follower 开局 100% 在带内 ⇒ 封堵者首步必命令内圈）。
> 3. **实测证伪**：退化策略击穿该口径——
>
> | 策略 | 命中率 | 完成率 |
> |---|---|---|
> | 反制脚本（正例） | 44.6% | 0.77 |
> | **blind diver**（零时机逻辑） | **60.5%** | 0.30 |
> | **random diver**（随机切） | **84.6%** | 0.13 |
> | 只在"命中态"切入 | 100% | **0.00** |
>
> **随机行为的命中率是正例的近两倍**，而**只在命中态切入从不获胜**
> ⇒ **命中率与获胜反相关**。补充实测（`_shift_invariance.py`）：
> 把切入延迟 1.0 s ⇒ 命中率 **0%**，完成率反升 **0.83**。

**⇒ 命中率类判据全部作废。** M1.3 改为**因果判据**（评审 R2-c 建议）：

```
因果判据：消融策略的「时机能力」后，完成率必须显著下降。
  操作化：把策略的"等封堵者物理位于外圈（lane_b==0）才切入"替换为
          "盲切"（lane_b==0 即切，去掉等待与逼近门控）。
  阈值：  完成率须从 ≥0.55 降至 ≤0.35。
  锚定依据（实测）：正例 0.77 → blind diver 0.30，Δ ≈ 0.47。
```

**理由**：这是**唯一**与获胜挂钩的量——正例 0.77 vs blind 0.30，
而**命中率口径下两者不可区分**（44.6% vs 60.5%，退化的还更高）。

**真正的获胜机制**（评审确证，本轮复现）：
**"封堵者物理位于外圈（`lane_b == 0`）时切入"**——
`dive ONLY if lane_b==0` 得 0.77；`dive ONLY if 目标态` 得 0.00。

**若最终策略用其它机制获胜**：M1.3 作为**因果诊断**须以同样的消融方式
重新操作化（"移除该机制后完成率是否下降"），**不得**沿用命中率口径。

## 3. 观测/动作与原语继承（§10.5b 硬性要求）

| 项 | 处理 |
|---|---|
| **成功语义** | **复用** `BlockerWorld3.complete()`（wrap-safe 累计进度 + 冻结 env 的 `delta>0.3` 语义），**禁止重实现** |
| **套圈守卫** | 已由 `delta_true`（未回绕累计弧长）提供——**这是 R4 的修复**，M1 直接继承 |
| **碰撞判定** | 复用真实 2D 距离 < 0.12，与冻结 env 一致 |
| **动作语义** | V2V 先验 `v_cmd = v_other + 0.8a`（与冻结 env 逐字一致） |
| **动作空间** | `[a_speed, a_lane] ∈ [-1,1]²`（与冻结 env 一致） |

**观测对照表**（§10.5b 要求"逐槽对照"）：

| 槽 | 冻结 env | M1 环境 | 一致性 |
|---|---|---|---|
| 0–3 | `e_gap, de, v_f, e_lat`（跟车）／`gap, delta, v_f, e_lat`（超车） | 见 §1.2 | 语义对齐（自视角） |
| 4 | `lane` | `lane_self` | ✅ 一致 |
| 5 | `v_l/0.5`（超车） | `clearance/2.5` | ⚠️ 不同（探针世界既有设计，W5 已登记） |
| **6–7** | —（无） | `lane_other, e_lat_other` | **新增**，须在下游文档标注"8 维，与冻结 6 维不可直接互换" |

---

## 4. 种子纪律（评审清单 #3）

- **评估 ≥30 seed**（W1 的 10→50 教训适用于一切 headline 数字）；
- **边际结果自动扩到 50 seed**：完成率 ∈ **[0.40, 0.65]** 或碰撞 ∈ **[0.15, 0.25]**
  （原 [0.45,0.65] 在 p=0.5 时仅 ±1.1 SE，约 30% 概率漏扩容）；
- **不放宽阈值**；
- 3 个固定 seed 集（`seed0 ∈ {123000, 200000, 300000}`）× 每集 ≥30 局，**逐集判定**。

---

## 5. 训练预算与早停（评审清单 #4）

| 项 | 设定 |
|---|---|
| 算法 | PPO（`MlpPolicy[128,128]`、lr 3e-4、n_steps 256、batch 512、γ0.99、GAE 0.95、clip 0.2、ent_coef 0.01、seed 0）——**沿用冻结超参** |
| 设备 | `device='cpu'`（F6 实测：本机 CPU 快 27–40%） |
| 上限 | **5M 步**（对齐 `train_ot.py` 的微调预算） |
| 环境并行度 | `n_envs = 8`（`SubprocVecEnv`，与 `train_ot.py` 一致；**须在 config 中记录**） |
| 更新次数（铁律 4） | 5M / (8×256) = **2,441** ✅ |
| **周期评估** | **每 0.5M 步**对封堵者评估 1 次（30 局），**曲线入 RUNLOG** |
| 早停 | 若连续 3 次评估完成率 ≤ **0.30** 且无上升趋势（退化策略 blind=0.30，故 0.10 形同虚设），**停并如实记录** |

**M1 的产出不只 pass/fail，还包括第一条"学习 vs 封堵者"曲线**（评审清单 #4）。

---

## 6. 风险与预案

| 风险 | 概率 | 影响 | 预案 |
|---|---|---|---|
| **探索峡谷**（纯 PPO 学不到，同冻结 env 的前车之鉴） | **中高** | M1 失败 | **BC 热身**：用**反制脚本的轨迹**作演示（与 `train_ot.py` 的 pretrain 同法）。**须先声明**：此为"用手写解法初始化"，若最终策略显著超越反制脚本，仍可主张自主发现；若仅追平，则结论**限定**为"能在引导下学到" |
| 观测扩充破坏可比性 | 中 | 可比性 | §3 对照表已列明；M1 结论**不主张**与冻结基线直接可比 |
| 封堵者可同归于尽 → 碰撞上限压得过紧 | 中 | 验收过严 | 上限 0.25 源自反制脚本实测最差值；若反制本身波动大，**补测反制脚本 50 seed 重定上限** |
| M1.3 因果判据的消融实现主观 | 中 | M1.3 争议 | §2.1 已把消融操作化（盲切替换）与阈值定死；若策略用其它机制获胜，须以同样方式重新操作化 |
| 单种子过拟合 | 低 | 结论不稳 | §4 种子纪律；边界扩 50 seed |

---

## 7. 时间预算

| 项 | 估计 |
|---|---|
| 环境（8 维 obs）+ BC 热身（如需） | 0.5 天 |
| 训练 5M 步 + 周期评估 | 0.5 天（实测吞吐下分钟级） |
| 评估（3 seed 集 ×30 局）+ M1.1–M1.4 判定 | 0.5 天 |
| 轨迹分析（M1.3）+ RUNLOG + 归档 | 0.5 天 |
| **合计** | **~2 天** |

---

## 8. 关联与产物

- 输入：`_blocker_v3.py`（封堵者基准）、`_counter3c.py`（正例/阈值来源）
- 产出：`results/20260921_phaseC_m1/`（config、训练日志、**学习曲线**、评估原始输出）
- 假设登记：`research/ASSUMPTIONS.md`（M1.1–M1.4）
- **资产归档**：`blocker@v1` 参数固化入库（评审清单 #5）
- 流程：本 plan → **独立评审** → 修订 → 执行（`DATA_MANAGEMENT.md` §10）

---

## 9. 自查（对照评审清单）

| 清单项 | 覆盖位置 |
|---|---|
| #1 假设登记（含 M1.1 匹配手写、M1.3 因果消融） | §2（4 条，阈值带数值，口径事先定死） |
| #2 防泄漏（对手一致、无特权信息） | §1.2（含"为何 slot7 不算泄漏"的论证） |
| #3 种子纪律（≥30，边际扩 50） | §4 |
| #4 预算与早停（5M 上限、每 0.5M 评估、曲线入 RUNLOG） | §5 |
| #5 封堵者基准命名归档 | §1.1（`blocker@v1`，参数成表） |
| §10.5b(b) 原语继承 | §3（成功语义/套圈守卫/碰撞判定 全复用） |

---

## 待评审确认点

1. **§1.2 slot7（对手横向偏移）是否真不构成泄漏**？我的论证是"物理可观测量 vs 内部控制器状态"，
   请核此边界。
2. ~~§2 M1.3 的 0.60 比例阈值是否有依据~~ → **已解决**：初稿的 0.60 是猜测值，
   实测后发现**比正例自身成绩还高**（正例按错误口径仅 52%），已作废并改为
   **因果消融口径**（消融时机能力后完成率须从 ≥0.55 降至 ≤0.35）。
   **请核的是**：以"基线提升 1.5×"作为锚定方式是否恰当？
   （替代方案：以正例绝对值锚定，但会得 0.31 并掩盖"其它机制取胜"的正面可能。）
3. **§6 BC 热身的表述**：若用反制脚本初始化，结论须如何限定才不失公允？
4. **§5 的 5M 步上限**是否合适（前车之鉴是 BC 热身 + 5M 微调）？
5. **M1 若在 BC 热身后达到 0.55–0.70**（追平但未超越），论文该如何表述"学习增益"？
