# Plan — W6 判据设计（`plan_W6_criterion.md`）

> # ⛔ 【2026-09-21 撤回 — 请勿使用本判据】
>
> **独立评审 Reject，作者已逐条独立复现，3 条拦截级全部确认。本判据作废。**
> 退化反例表的三条支柱同时断裂：
>
> 1. **正例是伪影** —— §2 的 `FROZEN CKPT = 1.00` 由**同一个适配器 bug
>    的第二次发作**产生（`_ckpt_as_opponent.frozen_layout` 的 gap 行，
>    首次已在 `ERRATUM` 中宣布为 bug，但当时**漏修了这一处**）。
>    修正任意一侧适配器，正例即归零（1.00 → 0.00，100% 碰撞）。
> 2. **世界无随机源** —— `RoleWorld.reset` 从不读 `rng`；确定性策略的 40 局
>    是**同一条 rollout**（unique delta = 1）。**阈值 0.8 在构造上不可达**，
>    "边界补 seed"条款形同虚设。
> 3. **两行退化策略通过并反超正例** —— loiter `[临近刹车, 永远内圈]`
>    final_delta **+2.707** > 正例 **+2.360**，且在 thr=2.5 仍通过。
>    根因：`delta` 被 `wrapL` 限在 ±L/2=±2.7425，**常驻内圈策略的 delta
>    必然爬到 wrap 上界**——所谓"干净 plateau [1.0,2.0]"其实是**饱和带**，
>    它测的是"切得早不早"，不是"超得好不好"。
>
> **完整记录**：`ERRATUM2_criterion.md`；评审：`reviews/20260921_W6_criterion_review1.md`。
> **M1 阈值不登记。** 重设计见 `plan_W6_criterion_v2.md`（待写）。
>
> **仍然成立**：方向（任务级判据 + 固定落后起跑）、§1.2 三处修正、
> §3 的 Caveat 1–3。**Caveat 4 论证方向相反**（越过的是内圈几何，非对手）。

日期：2026-09-21
性质：**判据设计文档**。通过评审并验证后才允许登记 M1 阈值、才允许开训。
关联：`plan_phaseC_w6_v2.md` §3/§5、`step0_criteria_findings.md`（F14/F15/F16）、
      `reviews/20260921_phaseC_w6_review1.md`、`DATA_MANAGEMENT.md` §10.4

---

## 0. 背景：为什么需要单独的判据文档

F14（见 `step0_criteria_findings.md`）指出：**同一策略同时驱动两车时，
对局结果完全由初始条件决定**，故任何建立在"终局相对位置"上的指标
在构造上无法区分策略优劣。这已导致 **四版判据**失败（W5 胜率、
W6 v1 的 [35%,65%] 带、Step 0-A 碰撞率、Step 0-C 非对称胜率）。

**本文档的目标**：给出一版**已通过退化反例验证**的判据（§10.4 的硬性要求）。

---

## 1. 设计（**用户裁决：主方向 = 任务级判据 + 打破对称性**）

### 1.1 核心思想

不发明更复杂的博弈指标，而是**打破对称性**：

1. **角色固定**（role-fixed）：每局固定谁是 follower（被追/追赶方）、谁是 leader；
2. **任务级语义**：follower 的胜负用**冻结 env 自己的成功定义**
   （`overtake_env.py:263`：`delta > 0.3` 判超车成功），leader 反之；
3. **固定落后起跑**：消除"初始条件主导终局"这一半根因；
4. **取 min（双角色）**：技能 = **最坏角色表现**。

### 1.2 三个实测修正（**与初始设计的偏差，均有依据**）

> 用户给出的初始设计是"±0.5 各半 + 阈值 0.3"。**实测发现两处需修正**：

| # | 初始设计 | 实测问题 | 修正 |
|---|---|---|---|
| **C1** | 固定间距 **±0.5 各半** | **−0.5 那半是"follower 被放在前面"**，躺在原地即领先 0.5 > 0.3 ⇒ **zero-action 白拿 0.50** | 改为**固定 +0.5 落后**（`GAP=+0.5` 恒定） |
| **C2** | 阈值 **0.3**（沿用冻结 env） | 同 C1：0.3 在对称起跑下被退化策略满足 | 阈值提到 **≥1.0**（扫描见 §2） |
| **C3** | （未指定动作基准） | **self 基准下正例 = 0.00**（checkpoint 分布外），判据**无正极** | 采用 **V2V 基准**（冻结 env 语义） |

### 1.3 定死的判据

```
被测策略 = follower（car 0），固定从 +0.5 m 落后起跑
对手     = 修复后的规则机（31/40 局用内圈，是真对手）
动作基准 = V2V（v_cmd = v_other + 0.8a，冻结 env 语义）
胜       = 终局 follower delta > 1.0          ← 阈值 ≥1.0（见 §2）
指标     = 对固定对手的胜率（≥30 局，3 seed 集）
```

**必须与对手一起声明**（§3 的 caveat 1）。

---

## 2. 退化反例表（**§10.4 的强制交付物**）

**设计参数**：follower-only、固定 +0.5 落后、V2V 基准、
对手 = 修复后的规则机、40 局/项。

| 策略 | win@0.3 | **win@1.0** | win@1.5 | mean_delta | 碰撞率 | 终止原因 |
|---|---|---|---|---|---|---|
| zero-action | 0.00 | **0.00** | 0.00 | −0.499 | 0.0% | timeout×40 |
| full-throttle | 0.00 | **0.00** | 0.00 | −0.484 | 0.0% | timeout×40 |
| random | 0.07 | **0.00** | 0.00 | +0.109 | **90.0%** | collision×36, timeout×4 |
| **FROZEN CKPT（正例）** | 1.00 | **1.00** | 1.00 | **+2.360** | 0.0% | timeout×40 |

**四项必过项**（用户指定）：

| # | 要求 | 实测 | 结论 |
|---|---|---|---|
| 1 | zero-action → 胜率 ≈ 0 | **0.00** | ✅ |
| 2 | full-throttle → 无胜（撞车） | **0.00**（碰撞率 0%，但从未领先） | ✅ |
| 3 | random → ≈ 0 | **0.00**（碰撞 90%） | ✅ |
| 4 | **正例（1.4s PPO 超车策略）→ 显著 > 0** | **1.00**（mean_delta +2.36） | ✅ |

**阈值扫描**（确定 plateau）：

```
threshold      zero-action   full-throttle   random   FROZEN CKPT(+)
      0.3             0.50            0.50      0.03         1.00   ← 不分离
      0.5             0.50            0.50      0.00         1.00   ← 不分离
      1.0             0.00            0.00      0.00         1.00   ← 分离
      1.5             0.00            0.00      0.00         1.00   ← 分离
      2.0             0.00            0.00      0.00         1.00   ← 分离
      2.5             0.00            0.00      0.00         0.00   ← 正例也掉（过严）
```

**→ 可用区间 thr ∈ [1.0, 2.0]；本 plan 取 1.0（保守下沿）。**

---

## 3. 必须随判据一起披露的 caveat（**不隐藏**）

### Caveat 1：判据是**对手相对**的

**实测**：正例（冻结 checkpoint）**vs 规则机 = 1.00，vs 自己 = 0.00**。

**原因**：两个**完全相同**的策略互搏时，初始的 0.5 m 间距**原样保持**，
谁也无法制造领先 ⇒ 无人越过 +1.0 ⇒ 双双 0 胜。
这正是 F14 的对称退化，**在固定落后起跑下依然存在**（因为对称性未破：
双方用的是同一个策略）。

**后果**：**阈值脱离对手即无意义**。任何报告都必须写成
"对**某固定对手**的胜率"，并固定该对手（M1 用规则机）。

### Caveat 2：**leader 角色不提供区分力**

**实测**（`_why_leader_saturated.py`）：作为 leader 时，
**所有**策略（含 zero-action）胜率都是 **1.00**。
因为"leader 赢"= "follower 未能领先 0.3"，而 **timeout 天然满足**这一条件。

**后果**：`min(双角色)` **实际等于 follower 单列**。
本 plan **仍保留双角色**（作为稳健性检查与 W6 叙事完整性），
但**明确声明**：区分力全部来自 follower 列，
**不得**把"双角色"当成"已经打破对称性"的证据。

### Caveat 3：阈值 0.3 **不可用**

对称起跑下退化策略得 0.50（见 §1.2 C1）。**必须 ≥1.0**。

### Caveat 4：正例的获胜是**真实超越**，非对手失误

**实测**：正例正例 partner 40/40 全部 `timeout`（**零碰撞**），
mean_delta = **+2.360 m**（远超阈值 1.0）。
即"超过去了并保持领先"，不是"对手崩了"。

---

## 4. 假设登记（**评审 + 验证通过后方可写入 `ASSUMPTIONS.md`**）

| # | 假设 | 判定阈值（定死） | 依据 |
|---|---|---|---|
| **M1.1** | 对手池使训练**稳定**（不重演 W5 振荡） | 末 20% 步窗碰撞率 **std ≤ 15%**，且 `value_loss` 非 NaN 且末段不单调增长 | 步窗口径定死（评审 I3）；W5 joint 末段 std 6.4%、V2V 14.7% |
| **M1.2** | 策略在**固定对手**下习得真实超车能力 | **对规则机胜率（thr=1.0，≥30 局）≥ 0.8** | §2 退化表：退化全 0.00、正例 1.00 ⇒ 0.8 有明显区分力 |
| **M1.3** | 策略**未塌缩** | `train/std` 末 10 次 **> 0.05** 且末段不单调趋 0 | 沿用 W5 P1 |

> **M1.2 阈值 0.8 的来历**：退化项实测 0.00，正例实测 1.00，
> 取 **0.8** 留出与"部分成功策略"的区分空间；
> 若训练后落在 **[0.7, 0.9]** 边界，按项目规范**补 seed 确认**，不放宽阈值。

---

## 5. 执行与验证顺序

| 步 | 动作 | 门 |
|---|---|---|
| 1 | 本判据文档 → **独立评审** | 评审须**重点核对退化表是否含正例**（用户指定） |
| 2 | 按评审修订 | — |
| 3 | **复跑退化表**（评审版设计）确认四项仍过 | 未过则回到步 1 |
| 4 | **登记 M1.1–M1.3 阈值** | §10.3 要求训练前定死 |
| 5 | **开训** | 铁律 4（≥300 更新）适用 |

---

## 6. 与已撤回内容的关系

- 本判据**不依赖**已撤回的"规则机不是博弈对手"结论（`ERRATUM` §1.1）——
  相反，它**要求规则机是真对手**（已修复，31/40 局用内圈）；
- **不使用** W5 的 `P2`（对规则机胜率）**原形**：原形失败因**判据饱和**
  （退化策略在近平衡带内，见 `ERRATUM` §2.2），
  而本判据通过**固定落后起跑 + 提高阈值**消除了饱和（§2 实测 0.00 vs 1.00）。

---

## 7. 复现

```bash
cd /home/zy/car_rl/code0919
PYTHONPATH=$PWD:/tmp uv run python results/20260921_phaseC_w6/crit_role_fixed_probe.py
PYTHONPATH=$PWD:/tmp uv run python results/20260921_phaseC_w6/crit_degenerate_table.py
PYTHONPATH=$PWD:/tmp uv run python results/20260921_phaseC_w6/crit_final_table.py
```

---

## 待评审确认点

1. **§1.2 的三处修正**（+0.5 恒定、阈值 ≥1.0、V2V 基准）是否接受？
   尤其 **C1**：用户原设计是 ±0.5 各半，我实测发现 −0.5 那半白送退化策略 0.50，
   故改为恒定 +0.5。**这是对用户设计的偏离，请重点核。**
2. **§3 Caveat 2**：leader 列无区分力，本 plan 仍保留双角色但声明其贡献为零。
   是否应当直接**删掉 leader 角色**（简化）？还是保留以便 W6 叙事？
3. **§4 M1.2 阈值 0.8** 是否合理？依据是退化 0.00 / 正例 1.00。
4. **§3 Caveat 1**：判据对手相对。M1 固定用规则机是否足够？
   是否需要**再备一个对手**（如慢速车）做交叉验证？
5. **§5 步 3** 的"复跑退化表"是否必要，还是 §2 的结果已足够一次评审采信？
6. 有无**遗漏的退化策略**（除 zero / full-throttle / random 之外）应当加入反例表？
   例如：**只会换道不打方向的策略**、**恒定中等速度策略**。


---

## 附录：撤回后的重设计要求（review2 前置）

1. **修 B1**：适配器已修；**所有结论必须在修正后的适配器下重跑**。
   若正例仍为 0.00，则**本判据此刻没有正极**——
   §10.4 未满足，**不得登记 M1 阈值**。
2. **修 B2**：给 `RoleWorld.reset` 加真实随机源
   （`gap0 ~ U(0.30,0.70)`、对手相位/参数扰动），
   并**先验证**确定性策略的 40 局能产生多种终局（unique ≫ 1）。
3. **修 B3 / 换因变量**：`delta` 终值已被证明会被 wrap 饱和，
   **不得再用作判据的因变量**。候选替代：
   - **"超越完成时间"**（沿用冻结 env 自己的 `success` 语义；
     常驻内圈也得先真正超过才算）；
   - **相对未饱和量**（如 `|delta|` 变化率、或"达成的超越次数"）；
   - 任何新因变量**同样要先过退化反例表**（含 **loiter 类**策略）。
4. **删 leader 列**（评审建议，12/12 全 1.00 无区分力）。
5. **修 Caveat 4 的论证**：正例若成立，须证明其获胜来自**真实超越**，
   而非内圈几何红利。
