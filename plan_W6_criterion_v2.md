# Plan — W6 判据 v2（criterion v2/v3，review2 送审稿）

日期：2026-09-21
性质：判据重设计。**前置**：`ERRATUM2_criterion.md`（v1 作废）、
      `ERRATUM3_criterion.md`（v2 测量再次被同一 bug 污染）。
关联：`plan_W6_criterion.md`（已撤回）、`reviews/20260921_W6_criterion_review1.md`、
      `DATA_MANAGEMENT.md` §10.4

---

## 0. 当前结论（**一句话**）

判据的**方向与形式已找到**（`FULL` = 超越 + 回外圈 + 保持领先），
但**目前没有任何合法正例**——所有候选正例的 `FULL` 均为 **0.00**。
按 §10.4，**阈值不得登记，M1 不得开训**。

---

## 1. 本轮进展（含两次自我纠错）

### 1.1 修正 v1 的三项缺陷

| 项 | 修正 | 验证 |
|---|---|---|
| B1 适配器 | `_ckpt_as_opponent.frozen_layout` → `(-delta) % L` | ✅ 保真度单测 v2 全过 |
| B2 无随机源 | `WorldV2.reset` 采 `gap0~U(0.30,0.90)`、leader `U(0.40,0.80)` + 抖动 | ✅ 确定性策略 40 局 `uniq_delta` **1 → 36–38** |
| B3 因变量饱和 | 换成 `FULL`（完整机动） | 见 §1.3 |

### 1.2 ★F17：「取得领先」在本赛道**可由车道几何买到**

内圈每圈短 ~0.94 m ⇒ **任何"切内圈绕圈"的策略自动取得领先**。
实测（修正随机源后）：

```
loiter（两行零技能）: ot_rate 0.93   ← "是否曾领先"被它买到
```

**→ 任何"终局相对位置"或"是否曾领先"类因变量**都会被几何买到**，
与博弈技能无关。这是 F14（对称退化）之外的**第二类结构性陷阱**。

### 1.3 因变量改为 `FULL`（完整机动）

```
FULL = 1  ⟺  曾领先(delta>0.3)
             且 之后回到外圈(lane==0)
             且 回到外圈后未再落后
```

**依据**：赛题语义为"超越后回到跟随位"；冻结 env 的成功条件
（`ahead_flag AND delta>0.3`）与其手工基线（切入→推进→**回外圈**）均体现此语义。

**`FULL` 能杀掉 loiter**（ot_rate 0.93 → FULL **0.00**）✅——
**这一条是本轮唯一确证的成果**，且它解决了 F17。

### 1.4 ★F18 / 第七次适配器 bug：**`/tmp` 陈旧副本**

**v3 首轮测量再次被污染**：criterion 脚本 import 的是 **`/tmp/_ckpt_as_opponent.py`
（陈旧、仍是 `(L/2 - delta) % L`）**，而修复只落在**仓库副本**上。
证据：解码 gap 读出 **3.123 m**，而真值 0.382 m；
`(2.7425 + 0.380) % 5.4849 = 3.1225` **逐位吻合**。

**修复**：把仓库副本同步到 `/tmp`，重测。

**教训（F18）**：
> **同一份代码存在两份副本（仓库 + `/tmp`）时，修 bug 必须同步两份，
> 或彻底消除副本。** 本轮已第二次因"只修了一份"而污染整轮测量。
> **建议**：实验脚本一律**从仓库 import**，`/tmp` 只放不 import 的驱动脚本。

---

## 2. ★关键结果：修饰后的诚实测量，**无合法正例**

**（适配器已同步修复，harness 已修正——本轮首次全干净测量）**

| 策略 | mean_delta | ot_rate | **FULL** | 碰撞 |
|---|---|---|---|---|
| zero-action | −0.561 | 0.00 | **0.00** | 0% |
| full-throttle | −0.109 | 0.00 | **0.00** | 100% |
| **loiter（两行零技能）** | +1.880 | **0.93** | **0.00** ✅ | 7.5% |
| 规则机 | +0.260 | 0.72 | **0.00** | **50%** |
| 冻结 ckpt（分布外） | −0.226 | 0.88 | **0.00** | **85%** |
| 世界内 PPO（自训 300 更新） | +0.120 | 0.23 | **0.00** | 77.5% |

**读法**：
- ✅ **`FULL` 成功排除全部退化策略**（含 loiter）——**判据的方向是对的**；
- ❌ **但没有一个候选正例能拿到 `FULL > 0`** ⇒ **§10.4 的"正例"要求未满足**；
- 值得注意：**规则机在适配器修好后其实会超车了**（ot_rate 0.00 → **0.72**），
  但以 **50% 碰撞**告终、且从未完成"回外圈并保持"——它在本世界**不是**合格正例。

**→ 结论：判据此刻既"能排除坏的"，但"证明不了好的存在"。**
**阈值不登记，M1 不开训。**

---

## 3. 下一步（正例问题，二选一）

| 路线 | 做法 | 代价 | 风险 |
|---|---|---|---|
| **A（推荐）** | 复用 `train_ot.py` 的配方：**BC 热身 + PPO 微调**（纯 PPO 跨不过探索峡谷是**项目既有结论**） | 半天 | 低——配方已验证 |
| **B** | 把本世界 leader 速度降到规则机擅长区间使其成为正例 | 1 小时 | **已测：失败**——规则机在**所有** band（0.15–0.30 至 0.40–0.80）下 FULL 均 = 0、碰撞率 100%（未修适配器时）／需在**修好的适配器**下重测 |

> **注意**：§3 路线 B 的扫描是在**适配器修复前**做的，结论**须在修复后重测**。
> 不排除"规则机在慢车 band 下成为合法正例"的可能性——
> 若成立，则**无需训练**即可获得正例。

---

## 4. 退化反例表（**含 loiter，v1 的漏项**）

| 项 | 策略 | FULL | 要求 | 状态 |
|---|---|---|---|---|
| 1 | zero-action | 0.00 | ≈0 | ✅ |
| 2 | full-throttle | 0.00 | ≈0（撞车） | ✅ |
| 3 | **loiter（占内圈）** | **0.00** | ≈0 | ✅ |
| 4 | random | 待测 | ≈0 | ⏳ |
| 5 | **正例** | **无** | **显著 > 0** | ❌ **缺** |

**新增常驻项**：**loiter 类必须常驻**——它是 v1 唯一漏掉却唯一通过的策略。

---

## 5. 待评审确认点

1. **§1.3 `FULL` 定义**是否恰当？（是否应要求"回到 d_des=0.2 的跟随位"更严？）
2. **§3 路线 A vs B**：先试 B（免费）还是直接上 A？
3. **正例阈值**：待正例产生后再定；是否应要求"正例 ≥ 某值"才能反推阈值？
4. **§1.4 的 F18**：`/tmp` 双副本问题是否应制度化解决（如禁止 `/tmp` 放被 import 的模块）？
5. 是否还有其他**被几何买到**的因变量需要排查？
6. `FULL` 是否应**同时报告碰撞率**（低 FULL + 高碰撞 ≠ 学会）？

---

## 6. 复现

```bash
cd /home/zy/car_rl/code0919
# 重要：确保 /tmp 副本与仓库同步（F18）
cp results/20260920_phaseC_probe/scripts/_ckpt_as_opponent.py /tmp/
PYTHONPATH=$PWD:/tmp uv run python results/20260921_phaseC_w6/crit_v3_full.py
PYTHONPATH=$PWD:/tmp uv run python results/20260920_phaseC_probe/scripts/adapter_fidelity_test_v2.py
```
