# W6 判据 v5 成立记录 — 封堵者 + 反制脚本（**正例成立**）

日期：2026-09-21
性质：按裁决（选项 1，延长半天）完成的**最终状态**。
关联：`W6_blocker_progress.md`（Day 1–2）、`ERRATUM3_criterion_v234.md`（F20）、
      `DATA_MANAGEMENT.md` §10.4/§10.5b

---

## 0. 结论

**判据成立**：脚本封堵者作对手（非循环），**时机型反制脚本作正例并稳定获胜**，
**几何族被稳定击穿**。半球时间的瞬态修复打穿了死锁。

---

## 1. 修复：把"逻辑换道"与"物理占据"分开（`_blocker_v3.py`）

### 1.1 实测先于修复

先测量当前世界的横向瞬态（`_measure_transient.py`）：

```
car 1 被命令 outer -> inner 后，车身【物理】到达内圈中心耗时：0.86 s
SWITCH_COOLDOWN = 1.5 s（纸面）   switch_timer = 0.8 s（限速）
```

**关键**：车身本来就要 0.86 s 才横移到位，
但模型里 `lane` 变量**瞬间翻转** ⇒
**封堵者在"物理上还在外圈"时，逻辑上已经"占据内圈"**。
死锁的每一圈都建立在这个瞬时假设上。

### 1.2 修复（不削弱封堵者规则）

```
lane_target[i]  = 被命令的车道（立即更新）
lane[i]         = 物理占据（仅在车身到达目标路径时更新，阈值 |e_lat|<0.03）
碰撞判定        = 仍用真实 2D 距离
```

**封堵者的决策规则一字未改**，只是它的"占据"变得诚实了。
实测物理瞬态 **0.61–0.76 s**（与预测量级一致）。

**裁决的机制论证成立**：承诺窗口从"纸面 1.5 s"变为
"物理 1.5 s − 横移耗时"，而**反制缺的正是这零点几秒**。

---

## 2. 回归：封堵者**未被修弱** ✅

按裁决要求，**先跑退化表回归再测反制**：

| 策略 | 修复前 | **修复后（v3）** |
|---|---|---|
| geom 2-line | 0.00 | **0.00** |
| geom_dive_early | 0.00 | **0.00** |
| geom_smart | 0.00 | **0.00** |
| rule machine | 0.00 | 0.00 |
| ckpt (+) | 0.00 | 0.00 |

**几何族仍全部 0.00** —— "geom 全 0"这一已验证资产**未被破坏**。

---

## 3. 正例：反制脚本**稳定获胜** ✅

**配置**：cruise 0.55，防守带 0.08–0.90（**常识带，非特调窄带**）。

**五组独立 seed 集（各 30 局）**：

| seed0 | 完成率 | 碰撞 | 中位 t_overtake |
|---|---|---|---|
| 123000 | **0.77** | 23% | 7.70 s |
| 200000 | **0.83** | 17% | 7.66 s |
| 300000 | **0.70** | 17% | 6.36 s |
| 400000 | **0.80** | 13% | 7.70 s |
| 500000 | **0.80** | 7% | 6.38 s |

**均值 ≈ 0.78**，跨 5 个独立 seed 集稳定 ⇒ **正例成立**。

### 3.1 值得记录的一处反直觉

修复后首轮测试发现：**"等封堵者换道完成再切"反而 0.00**，
而**在封堵者横移过程中切**得 0.70–0.83。

**机理**：封堵者车身横移时**物理上不在任何一条车道中心**，
因此**无法拒止**——**真正的窗口是横移过程本身**，不是"定居后的冷却"。
这与裁决的直觉（用冷却窗口）方向一致但更精确：
**窗口的物理来源是横移占用的时间**。

### 3.2 遗留：13–23% 碰撞

反制脚本**获胜但伴随碰撞**（成功率 ~78% 而非 100%）。
即封堵者**仍能通过碰撞"同归于尽"**（碰撞对双方都是失败）。
**这不是 bug，是任务性质**：封堵者的最后手段是撞——
**须在 M1 验收中显式处理**（见 §4）。

---

## 4. M1 判据与阈值（按裁决锚定正例）

按裁决"**阈值锚定反制成绩**"：

```
M1 验收（原语继承：`complete` 复用 wrap-safe 累积进度 + 冻结 env 的成功语义）
  1. 完成率 ≥ 0.55        （= 反制脚本均值的 ~70%，裁决指定口径）
  2. 碰撞率 ≤ 0.25        （= 反制脚本最差值，见 §3.2）
  3. 3 个固定 seed 集，每集 ≥30 局，逐集判定
```

**依据**：
- 完成率 0.55 = 0.78 × 70%，**阈值有正例依据**（§10.4 老规矩）；
- 碰撞上限 0.25 取自反制脚本的最差观测（23%），
  **诚实反映"封堵者可同归于尽"这一任务性质**；
- 若训练后落边界（如 0.50–0.60 或碰撞 0.20–0.30），按项目规范**补 seed**，不放宽。

**须随判据一起披露**：
- 判据**依赖封堵者强度**（cruise / 防守带），报告必须写明；
- 判据**不测"对等竞速"**，测的是**对封堵者的任务级反制**（承接 F20 的限定）。

---

## 5. 时间盒与执行树对照

| 执行树节点 | 状态 |
|---|---|
| 修横向瞬态（≤0.5 天，硬停） | ✅ 完成（含先测量） |
| 退化表回归：三个 geom 仍须 0.00 | ✅ **通过，封堵者未被修弱** |
| 反制 vs 封堵者（常识带 0.08–0.90） | ✅ **稳定获胜 0.70–0.83** |
| → 判据成立 → 登记 M1 阈值 | ✅ 见 §4（锚定反制成绩的 70%） |
| 瞬态修复引入新 bug？ | ✅ 无：退化表与基线数字均未漂移 |
| 逃生舱 | **未启用**（世界修复成功） |

---

## 6. 复现

```bash
cd /home/zy/car_rl/code0919
PYTHONPATH=$PWD uv run python results/20260920_phaseC_probe/scripts/_measure_transient.py
PYTHONPATH=$PWD uv run python results/20260920_phaseC_probe/scripts/_blocker_v3.py   # 回归
PYTHONPATH=$PWD uv run python results/20260920_phaseC_probe/scripts/_counter3c.py    # 正例
```
